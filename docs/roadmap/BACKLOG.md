# BACKLOG — Millo

Documento maestro de funcionalidades y su estado de gobernanza. **Regla:** cada vez
que un agente genere un documento (ARQ, AUD, DEC o IMP) debe actualizar esta tabla.

Leyenda: ✅ hecho · ⏳ pendiente · 🔄 en curso · ➖ no aplica

| ID | Funcionalidad | Prioridad | ARQ | AUD | DEC | IMP | Estado |
|----|---------------|-----------|-----|-----|-----|-----|--------|
| FIN-001 | Inteligencia Financiera — arquitectura de 3 capas (umbrella) | Alta | ✅ ARQ-0001 | ✅ AUD-0001 | ✅ DEC-0001 | ➖ | Decidido — Aprobado con ajustes (umbrella, no implementa directamente). Autoriza avanzar a FIN-002 |
| FIN-002 | Fundaciones de datos (Cuentas/Activos + bus de eventos + snapshots) | Alta | ✅ ARQ-0002 | ✅ AUD-0002 | ✅ DEC-0002 | ✅ IMP-0002 v2 | **Cerrado.** Validado por el CTO contra commit `622bfa1`. Habilita inicio de FIN-003 |
| FIN-003 | Motor Financiero (MVP) | Alta | ✅ ARQ-0003 | ✅ AUD-0003 | ✅ DEC-0003 | ✅ IMP-0003 | **Cerrado.** Validado por el CTO contra commit `bbf9654`. Habilita inicio de FIN-004 |
| FIN-004 | Salud Financiera + Score Millo | Alta | ✅ ARQ-0004 | ✅ AUD-0004 | ✅ DEC-0004 | ✅ IMP-0004 | **Cerrado.** Validado por el CTO contra commit `c85117e`. Habilita inicio de ARQ-0005 (con condiciones) |
| FIN-005 | Copiloto Financiero v2 | Alta | ✅ ARQ-0005 v2 | ✅ AUD-0005 v2 | ✅ DEC-0005 (desarrollo autorizado) | ✅ IMP-0005 | **Cerrado** (modo plantillas/dev). Validado por el CTO contra commit `919f7c2`. Datos reales bloqueados hasta DPA+PIA; producción bloqueada hasta revisión legal final |
| FIN-006 | Memoria financiera + Proactividad (sin RAG) | Media | ✅ ARQ-0006 | ✅ AUD-0006 | ✅ DEC-0006 | ✅ IMP-0006 | **Cerrado.** Validado por el CTO contra commit `994b085`. Habilita inicio de FIN-007 |
| FIN-007 | Simulador financiero + Motor de recomendaciones | Media | ✅ ARQ-0007 | ✅ AUD-0007 | ✅ DEC-0007 | ✅ IMP-0007 | **Cerrado.** Validado por el CTO contra commit `a56f11e`. Habilita inicio de FIN-008 |
| FIN-008 | Gamificación | Media | ✅ ARQ-0008 | ✅ AUD-0008 | ✅ DEC-0008 | ✅ IMP-0008 | **Cerrado.** Validado por el CTO contra commit `c2db461`. Habilita inicio de FIN-009 |
| FIN-009 | Monetización (Free/Premium) + Hardening | Alta | ✅ ARQ-0009 | ✅ AUD-0009 | ✅ DEC-0009 | ✅ IMP-0009 | **Cerrado.** Validado por el CTO contra commit `827c3e9`. **Roadmap original de ARQ-0001 completo (9/9)** |
| FIN-010 | Salida a producción (cierre de gates legales/negocio + checklist operativo) | Alta | ➖ | ➖ | ➖ | ➖ | **Pendiente — sin ciclo de gobernanza propio** (no es código nuevo, es ejecutar la tabla de abajo). Bloquea el lanzamiento con usuarios/datos reales |
| FIN-011 | **Umbrella — Mejoras de deuda, presupuesto y dashboard financiero** (segunda ronda post-roadmap) | Alta | ✅ ARQ-0011 | ✅ AUD-0011 | ✅ DEC-0011 | ➖ | **Cerrado.** Umbrella correctamente ejecutado; dio origen a la regla "Un FIN a la vez" tras detectarse que el Arquitecto diseñó los 5 sub-ciclos en detalle dentro de este documento. Ver comunicado del fundador en `docs/oficial/COMUNICADO-2026-07-06-Un-FIN-a-la-vez.md` |
| FIN-012 | Deuda — abono a capital y pago total anticipado (real, no solo simulado) | Alta | ✅ ARQ-0012 | ✅ AUD-0011-Rev1 + AUD-0012 | ✅ DEC-0012 | ✅ IMP-0012 v2 | **Cerrado, con condición registrada.** Validado por el CTO contra commit `5e8d32f` en checkout aislado. Habilita seleccionar la siguiente funcionalidad por valor. Ver Historial: verificación estática completa + limitación de entorno documentada sobre la ejecución directa del E2E |
| FIN-013 | Deuda — seguros asociados al crédito (obligatorios financiados, endosables) | Media | ✅ ARQ-0013 | ✅ AUD-0013 | ✅ DEC-0013 | ✅ IMP-0013 | **Cerrado (regularización extraordinaria).** Validado por el CTO contra commit `9607c3f` en checkout aislado — sin hallazgos |
| FIN-014 | Dashboard de Inicio v2 — patrimonio, ahorro total, ingresos por categoría (fijo+variable), gastos fijo+variable diferenciados, movimientos completos | Alta | ✅ ARQ-0014 | ✅ AUD-0014 | ✅ DEC-0014 | ✅ IMP-0014 | **Cerrado (regularización extraordinaria).** Validado por el CTO contra commit `1af29b1` en checkout aislado — sin hallazgos |
| FIN-015 | Proyección de ahorro con interés compuesto (simulación ilustrativa, sin producto de rendimiento real) | Media | ✅ ARQ-0015 | ✅ AUD-0015 | ✅ DEC-0015 | ✅ IMP-0015 | **Cerrado (regularización extraordinaria).** Validado por el CTO contra commit `6f622e3` en checkout aislado — sin hallazgos |
| FIN-016 | Periodo financiero / ciclo de pago (día de corte) para Presupuesto y Dashboard | Media | ✅ ARQ-0016 | ✅ AUD-0016 | ✅ DEC-0016 | ✅ IMP-0016 | **Cerrado (regularización extraordinaria).** Validado por el CTO contra commit `40700bc` en checkout aislado — invariante de aislamiento del Score confirmada por grep |
| FIN-017 | Mejora de UX — Login y Dashboard (Lote 01) · **Origen: Mejora de revisión de producto** | Alta | ✅ ARQ-0017 v1.3 | ✅ AUD-0017 | ✅ DEC-0017 (+confirmaciones §7.2) | ✅ IMP-0017 | **Cerrado.** Validado por el CTO contra commit `e914e85` (código) y `52e559a` (IMP-0017): correspondencia exacta DEC→IMP→Código→Evidencia verificada — grep de las 6 cadenas de glosario/pilares con los conteos exactos declarados (`Te queda este ciclo`=1, `lo tuyo, menos deudas`=1, `fijos del mes`=1, `del día a día`=1, `pagado este ciclo`=1, `Flujo estimado`=0, 4 pilares del Login=4), capturas reales antes/después vistas visualmente por el CTO, `DEBT_RATIO_CUTS` (0,20/0,35) confirmado en `score.util.ts`, tarjeta de Deuda con cifra única (`debtPayments`) confirmada en `DashboardScreen.tsx`. Limitación de entorno conocida (ICU/Postgres del sandbox) impide ejecutar la suite en vivo — verificación estática exhaustiva aceptada, mismo precedente que FIN-012/013-016. Próximo paso: revisión integral de la experiencia completa (scroll de principio a fin) antes de decidir si Inicio necesita `FIN-018` o avanza a la Experiencia de Salud |
| FIN-018 | Evolución de la experiencia Inicio (2ª iteración, mismo alcance Login+Dashboard) · **Origen: Mejora de revisión de producto** | Alta | ✅ ARQ-0018 v1.4 | ✅ AUD-0018 + RC-0001 v1.3 | ✅ DEC-0018 (+adendo §14) | ✅ IMP-0018 v1.2 | **Cerrado.** El CPSAO reprogramó la sesión real de RC-0001 como parte de una futura **RC integral** al final de toda la hoja de ruta UX (valida el recorrido completo por las 6 experiencias, no una pantalla aislada) — el diseño de `RC-0001-Inicio.md` v1.3 queda preservado para reutilizarse entonces. Consultado el Fundador, se cierra FIN-018 ahora con la evidencia ya validada por el CTO: 8 piezas implementadas y verificadas contra código/capturas en 4 iteraciones, más la revisión de comprensión de primer nivel (walkthrough cognitivo) cuyos 2 hallazgos válidos ("ciclo", "Abono a capital") ya fueron corregidos y confirmados. Ver `docs/oficial/DEC-0018-Evolucion-Inicio.md` §14. Habilita abrir la Experiencia de Salud |
| FIN-019 | Evolución de la experiencia Salud (`HealthScreen.tsx` — Score Millo) · **Origen: Mejora de revisión de producto** | Alta | ✅ ARQ-0019 v1.1 | ✅ AUD-0019 | ✅ DEC-0019 (+adendo §8) | ✅ IMP-0019 | **Cerrado.** Validado por el CTO contra commit `e865640` (diff confirma cero backend, dos archivos tocados). Verificación independiente propia: `HealthScreen.tsx` confirma `worstIndicator()` sobre indicadores (no pilares), `humanValue()`, `SIM_BY_KIND` (3/4 kinds reales), `coldStart.remainingDays`, `narrative()` con caso de primera medición, y ruta `'Insights'`→Copiloto. Capturas `despues-salud-01-scroll-completo.png` y `despues-salud-02-coldstart.png` inspeccionadas visualmente por el CTO: 4 pilares en barra neutra sin semáforo, tarjeta siempre verde con "Estable" en chip, "715 de 1.000" sin "v1", "Lo que más te frena: fondo de emergencia" coincide con el indicador rojo. Corrección a `VALIDACION-0019-Experiencia-Salud.md` §4: la "observación menor abierta" sobre la frase residual de `ARQ-0019` §10 ya no aplica — grep directo del documento vigente confirma que la frase fue corregida ("frase corregida en v1.1 conforme al detalle señalado en la confirmación del CTO"), consistente con el mensaje del commit `e865640` ("ARQ v1.1 §10: frase residual corregida (detalle del CTO)"). El Auditor validó en un momento previo a esa corrección y su documento no se actualizó — corrección de vigencia, no de método; notificada al Auditor para que ajuste su registro. Sin impacto en el veredicto APROBADO ni en el cierre. **Actualización del Auditor (2026-07-12):** verificación independiente propia (`sed`/lectura directa de `ARQ-0019` líneas 206-210) confirma la corrección; `VALIDACION-0019-Experiencia-Salud.md` §4/§7/§8 actualizados retirando la observación menor. Habilita abrir la Experiencia de Presupuesto |
| FIN-020 | Experiencia de Presupuesto | Alta | **Cerrado (técnico + producto).** Validado por el CTO en checkout aislado contra commit `125c5c6` (ancestro de HEAD): `SpendableService` confirmado como única fuente; §4.1-bis/§4.1-ter verificados carácter a carácter. Suites reejecutadas por el CTO: unit 303/303, e2e 9/9, `tsc` limpio. Revisión de producto del CPSAO (`docs/correspondencia/FIN-020-Experiencia-de-Presupuesto.md`) devolvió 2 ajustes menores — implementados y verificados por el CTO en commit `614ef81` (política §4.1-bis visible al usuario + fecha de vencimiento en cuotas de P6) — y 1 hallazgo serio derivado (3 fórmulas divergentes de "fondo de emergencia", §32), canalizado a `FIN-021` sin bloquear el cierre de esta FIN. CPSAO declaró Presupuesto **Aprobada** en el sentido de producto. Observación no bloqueante para la futura Experiencia de Copiloto: `context-assembler.ts` deberá consumir `SpendableService`. Habilita abrir la Experiencia de Deudas — pero el CPSAO instruyó abrir primero `FIN-021` |
| FIN-021 | Única definición del fondo de emergencia (§32) · **Origen: Deuda técnica (§27), prioridad inmediata por decisión del CPSAO** | Alta | ✅ ARQ-0021 v1.0 (`77ddffd`) | ✅ AUD-0021 · ✅ VALIDACION-0021 — **APROBADO** | ✅ DEC-0021 (P3 Alt C, 1 cambio obligatorio §5) | ✅ IMP-0021 v1.0 (`6ee4d9d`) | **Cerrado (técnico + producto).** Validado por el CTO en checkout aislado contra `6ee4d9d` (ancestro de HEAD): `emergency-fund.constants.ts` única fuente de hitos; `recommendations.service.ts` ya no usa `emergencyBalance` en vivo; `grep` confirma la fórmula solo en `core-metrics.ts:61`. Suites reejecutadas por el CTO: unit 310/310, e2e 12/12, `tsc` limpio. CPSAO declaró Aprobada en producto (capturas revisadas, contradicción "~4 vs 3" ya no reproducible) y autorizó abrir la Experiencia de Deudas. Detalle en `docs/correspondencia/FIN-021-Fondo-de-Emergencia.md` |
| FIN-022 | Experiencia de Deudas · mora diferida por 3ª vez (explícito, ver FIN-024) | Alta | ✅ ARQ-0022 v1.0 (`23d9967`) | ✅ AUD-0022 · ✅ VALIDACION-0022 — **APROBADO** | ✅ DEC-0022 (P1/P3/P4/P5 sin condiciones; P2 con 4 cambios obligatorios §5) | ✅ IMP-0022 v1.0 (`0f75a5c`) | **Cerrado (técnico + producto).** Validado por el CTO en checkout aislado contra `0f75a5c` y `8bd4c67` (ajuste de zona horaria post-cierre, verificado por el CTO: `formatDate()` UTC para fechas puras / `formatLocalDate()` para instantes reales, `tsc` limpio): `attackOrder()` único helper del orden (no puede divergir), `extraBudget:0` fijado, gate `length<2`. Suites: unit 313/313, e2e 15/15. CPSAO declaró Aprobada en producto tras confirmar la corrección de fecha ("vence 28 jul" idéntico en Inicio y Deudas). Mora diferida con condición del CPSAO (ver `FIN-024`). Habilita `FIN-023` (desembolso real + cuota de manejo, §32, prioridad inmediata) |
| FIN-023 | Desembolso real de deuda como "lo comprometido" (§32) — **incluye cuota de manejo de tarjetas** (requisito del Fundador, dato aportado por el usuario, no asumido) · **Origen: Deuda técnica (§27), prioridad inmediata por decisión del CPSAO** | Alta | ✅ ARQ-0023 v1.0 (`3f74873`) | ✅ AUD-0023 · ✅ VALIDACION-0023 — **APROBADO** | ✅ DEC-0023 (P4/P5 incluidos; 4 cambios obligatorios §5) | ✅ IMP-0023 v1.0 (`c7b9804`) | **Cerrado.** Validado por el CTO en checkout aislado contra `c7b9804` (ancestro de HEAD): `DebtOutlayModule` confirmado como módulo hoja real (sin imports propios); `payment-breakdown.util.ts` es la única función pura, compartida por `DebtOutlayService` y `DebtInsuranceService`; rechazo 400 de `endorsed`/`insurer` para `cuota_manejo` confirmado; `monthlyPremium` sin default. Suites reejecutadas por el CTO: unit 318/318, e2e 20/20 (incl. `fin023-desembolso-real`), `tsc` limpio. Cuarta FIN consecutiva con fuente única por construcción. CPSAO declaró Aprobada en producto (capturas revisadas, aritmética cruzada confirmada: $519.134 == 127.099+392.035). Secuencia decidida: sigue `FIN-024` (mora), no Simulador |
| FIN-024 | Mora de deudas (iteración 1, fijos fuera de alcance) — **bug fundacional encontrado y corregido como P1: doble escritor de `debt.nextDueDate`** | Alta | ✅ ARQ-0024 v1.1 | ✅ AUD-0024 · ✅ VALIDACION-0024 — **APROBADO** | ✅ DEC-0024 (3 cambios obligatorios §5) | ✅ IMP-0024 v1.0 (`faebc2a`) | **Cerrado (técnico + producto).** Validado por el CTO en checkout aislado contra `faebc2a`: código + suites — unit 326/326, e2e 23/23, `tsc` limpio. Quinta fuente única por construcción. CPSAO declaró Aprobada en producto (capturas revisadas, tono §29.2 confirmado: "no hay un pago registrado", CTA de acción no de regaño) y autorizó abrir la Experiencia de Simulador |
| FIN-026 | Experiencia de Simulador — **bug de navegación activo encontrado: `abono_extra` cae en el escenario opuesto** | Alta | ✅ ARQ-0026 v1.0 | ✅ AUD-0026 — **APROBADO CON OBSERVACIONES** | ✅ DEC-0026 (3 cambios obligatorios §5) | ✅ IMP-0026 v1.0 (`68588c8`) | **Cerrado (validación técnica del CTO).** Verificado por el CTO contra el commit de referencia `68588c8` (cero backend, confirmado por `--stat`): **(1)** ambos mapas `SIM_BY_KIND` incluyen `abono_extra` (`HealthScreen.tsx:218`, `BudgetScreen.tsx:210`) y el fallback mudo murió — `unknownScenario` (`SimulatorScreen.tsx:139`) muestra banner visible "⚠️ No encontré el escenario…" (`:246-252`) en vez de caer callado en `SCENARIOS[0]`; **(2)** P2 frontend-only real — `extraBudget` con `allowZero:true` (`:87`) y validación `f.allowZero ? v<0 : v<=0` (`:215`) acepta 0, `DebtsListScreen.tsx:194-196` precarga `extraBudget:0`, sin `backend/` en el diff; **(3)** titular por delta — `headline()` (`:418-422`) lidera con "Tu Score pasaría de X a Y" y omite la frase con delta 0. `tsc --noEmit` reejecutado por el CTO: exit 0. Suites unit/e2e sin cambios (frontend-only, regresión) — método por código+capturas, como anticipó el AUD (precedente FIN-017). Firma de producto en dispositivo real: la valida el Fundador con el APK nuevo (que ya incluye este código). Detalle: `docs/correspondencia/FIN-026-Experiencia-de-Simulador.md` |
| FIN-025 | Aviso proactivo de mora (notificación "venció hace N días", canal push/WhatsApp) — **fast-follow registrado, no genérico** (exigencia del CPSAO) | Media — revisitar tras ciclo de uso real de `FIN-024`, no bloquea Simulador | ⏳ | ➖ | ➖ | ➖ | **Registrada, no iniciada.** El CPSAO excluyó la notificación proactiva de `FIN-024` (pantalla pasiva primero, para no arriesgar Confianza con un tono de regaño sin validar) pero exigió que quedara "agendada, no en el limbo, mismo trato que mora". Copy debe respetar §29.2 ("el rojo no culpa"). No tiene fecha de inicio fija — depende de observar cómo se usa la etiqueta pasiva de FIN-024 primero |
| FIN-027 | Evolución del modelo de ingresos personales (perfil laboral, ingresos fijos/variables/comisiones/honorarios, deducciones configurables salud/pensión con base total o parcial) · **Origen: Funcionalidad nueva (§27)** | Alta | ✅ ARQ-0027 v1.0 (`0b1b044`) | ✅ AUD-0027 — **APROBADO CON OBSERVACIONES** | ✅ DEC-0027 (4 cambios obligatorios §5) | ✅ IMP-0027 v1.0 (`67cf375`) | **Cerrada (validación técnica del CTO).** Verificado en checkout sobre `67cf375`: los 4 cambios obligatorios confirmados — copy en Salud (`health.service.ts:92`, texto del CPSAO, condicionado a `hasDeductions`, fuente única en backend); migración sin coexistencia (`budget.service.ts:30` de este commit rechaza `kind='ingreso'`→400); `NetIncomeService` módulo hoja, `engine.service.ts` usa `netFixedTotal` → **DTI/Score sobre NETO**, `withheldAtSource` implementado; regresión sin fuentes = 0 (`fin020` verde con base neta). Suites por el CTO: `tsc` BE+FE 0, unit **345/345** (45), e2e **40/40** (10, incl. `fin027`). 3 reservas declaradas aceptadas iteración 1 (sin retefuente auto; variables sin deducción propia; UI deducciones solo %). Firma de producto: el Fundador. Detalle: `docs/oficial/DEC-0027-Modelo-de-Ingresos.md`. **Decidida — `IMP-0027` habilitado.** CPSAO decidió **DTI/Score sobre NETO** (nunca mentir hacia arriba, coherencia §32) con **nota de copy obligatoria** en Salud ("bajó porque calculamos con tu ingreso real tras deducciones — más preciso, no peor", patrón costo-de-honestidad FIN-020/021). CTO resolvió la 2ª decisión abierta: **migración** de los FixedItem de ingreso al modelo de fuentes, NO coexistencia (§32). `withheldAtSource` aprobado; `NetIncomeService` única def. del neto; regresión sin perfil = cifras idénticas. Verificado por el CTO: `core-metrics.ts:36` (`incomeRef=max(fixedIncome,actualIncome)`=bruto) alimenta DTI/ahorro/Score → el reclamo "todo sobre bruto" es real; modelos de ingreso nuevos no existen. `NetIncomeService` módulo hoja (§32 por construcción); `withheldAtSource` bien derivado (deducción auto-pagada → Spendable). Decisión DTI neto/bruto correctamente diferida al DEC (si elige bruto, documentar dualidad como excepción §32). Fase ARQ autorizada en paralelo por excepción documentada a "un FIN a la vez" (memo del Fundador 2026-07-13). Requisito clave del Fundador: base de deducción configurable (total vs. parcial) y coexistencia de salario fijo (ingreso fijo) + salario variable (ingreso variable). Debe analizar impacto en Salud, Motor Financiero, Presupuestos, Copiloto, Proyecciones y Reportes; ingreso bruto→deducciones→neto calculado una sola vez desde la config del onboarding/perfil. §32 aplica (una única definición de "ingreso neto disponible"). Detalle: `docs/correspondencia/FIN-027-Modelo-de-Ingresos.md` |
| FIN-028 | Gestión completa del ciclo de vida de movimientos (editar / anular / confirmar / recálculo automático de toda la información derivada) · **Origen: Deuda técnica / Mejora de revisión de producto (§27)** | Alta | ✅ ARQ-0028 v1.0 (`7cdbdf0`) | ✅ AUD-0028 — **APROBADO CON OBSERVACIONES** | ✅ DEC-0028 (4 cambios obligatorios §5) | ✅ IMP-0028 v1.0 (`65104e1`) | **Cerrada (validación técnica del CTO).** Verificado en checkout sobre `65104e1`: los 4 cambios obligatorios confirmados — **grep limpio de `anulada`/`voidedAt`** (anulación = `deletedAt`, mi corrección acatada), `ACTIVE_TX_FILTER` solo en `Transaction` (los 97 de otros modelos intactos), impacto retroactivo declarado, infra/gate intactos. Bug cerrado: `update`/`remove` emiten eventos vía outbox (payload rico changedFields+before/after), `undoLast` reencaminado al servicio central, anular pago de deuda revierte saldo atómicamente + guardarraíl `DEBT_LOCKED_FIELDS`→400. Suites reejecutadas por el CTO: `tsc` exit 0, unit **331/331** (43), e2e **27/27** (8, incl. `fin028-movimientos` 4/4, sin regresión). 2 limitaciones declaradas aceptadas para iteración 1: (a) anular pago de deuda no reconstruye `next_due_date` (registrada como seguimiento); (b) UX en filas de Inicio (sin pantalla de lista completa). Firma de producto en dispositivo real: el Fundador. Detalle: `docs/oficial/DEC-0028-Gestion-de-Movimientos.md`. **Decidida — `IMP-0028` habilitado (1º de la secuencia 028→027→029).** **Corrección de diseño del CTO (`DEC-0028` §5.1): NO se añade estado `anulada` ni `voidedAt`.** La anulación **es** `deletedAt` (ya existe: `remove()` hace soft-delete en `:175`, 97 filtros ya excluyen anulados); un estado nuevo sería un 3er concepto de anulación junto a `deletedAt` y al dormante `descartada` → §32 lo prohíbe. La etiqueta "Anulado" se deriva de `deletedAt != null`. Aprobado: outbox en `update`/`remove`, evento rico (`changedFields`+before/after) para auditoría/IA sin rediseñar BD, servicio central único (reencaminar `undoLast`), guardarraíl deuda (anular-y-recrear monto/fecha). **`ACTIVE_TX_FILTER` solo sobre consultas de `Transaction`, verificado por diff — no los 97 a ciegas.** Verificación independiente del CTO contra código en `DEC-0028` §0. Dos incumplimientos confirmados reales: `transactions.service.ts:160-178` (`update`/`remove` escriben directo, sin `outbox.withEvent` → el Motor no recalcula tras corregir) y `undoLast` en mensajería (`conversation.service.ts:186`, 2ª ruta de mutación). Fortaleza: el `EngineListener` ya escucha `transaction.updated/deleted` (`engine.listener.ts:24-26`) — falta emitir, no consumir; corrección mínima. Enum `TxStatus` admite `ADD VALUE 'anulada'`; 24 archivos con `deletedAt:null` (≈25 del ARQ). Observación con peso: el reemplazo de los ~24 filtros por `ACTIVE_TX_FILTER` debe mapearse consulta por consulta (algunas incluyen `pendiente_confirmacion`) y verificarse por diff — no a ciegas. El más urgente de los tres. **Retención levantada.** El Fundador entregó sus observaciones sobre el módulo Registrar (10 decisiones, memo 2026-07-13) → directiva `ARQ-0028` emitida. Requisitos duros: **anulación lógica** (estado Anulado, nunca borrado físico; anulados fuera de presupuestos/indicadores/Salud/dashboards/Motor); edición sin lógica financiera propia (el Motor decide el recálculo, §32); recálculo automático de todo lo derivado; confirmación previa; modelo preparado para auditoría futura sin rediseño de BD; eventos para Copiloto futuro; **mismo servicio central de movimientos** reutilizable por Telegram/WhatsApp (liga con FIN-029). Detalle: `docs/correspondencia/FIN-028-Gestion-de-Movimientos.md` |
| FIN-029 | Integración con Telegram sobre un **Motor Conversacional único y reutilizable** (auth + vinculación cuenta, recepción de mensajes, interpretación por Motor IA, creación automática de movimientos, respuesta automática; desacoplado para reutilizar en WhatsApp) · **Origen: Funcionalidad nueva (§27)** | Media | ✅ ARQ-0029 v1.0 (`c710e2d`) | ✅ AUD-0029 — **APROBADO CON OBSERVACIONES** | ✅ DEC-0029 (6 cambios obligatorios §5) | ✅ IMP-0029 v1.0 (`9bb83c0`) | **Cerrada (validación técnica del CTO).** Verificado en checkout sobre `9bb83c0`: las 6 condiciones con test que pasa (`conversation.service.spec.ts`) — §5.1 acuse QUÉ+DÓNDE (crear+anular); §5.2 no finge "ya lo anoté"; §5.3 simular muestra sin empujar; §5.4 paywall honesto; §5.5 genericidad (no nombra marcas); motor invoca servicio central FIN-028. Dedupe `update_id` probado, no reconstruido. **Gate DPA+PIA cerrado por construcción** (grep limpio de LLM en `messaging/`; conversación determinista). Suites por el CTO: tsc 0, unit 355/355 (46), e2e 43/43 (11, incl. `fin029-telegram`). 3 reservas declaradas aceptadas iteración 1 (capa IA diseñada no-runtime; handshake de confirmación espera al LLM; `editar` por texto es territorio LLM, hoy solo `anular`). Backend-only → Beta por auto-deploy de Render (§41), sin OTA. Observación menor al CPSAO: placeholders de UI preexistentes nombran "Bancolombia" (fuera de FIN-029). Detalle: `docs/oficial/DEC-0029-Integracion-Telegram.md`. **Cierra la tanda de la Beta Técnica.** CPSAO dio visto con **4 condiciones** (cambios obligatorios): (1) acuse explícito de todo movimiento, sin cambios de estado en silencio; (2) honestidad al no entender, prohibido el falso "ya lo anoté"; (3) barrera de Independencia — toda tool que dé un consejo (no un dato) vuelve al CPSAO, simular solo muestra escenarios; (4) paywall honesto al agotar la cuota de IA. + (5) test PII/genericidad por tool (FIN-005); (6) gate DPA+PIA intacto. Verificado por el CTO: motor único (`ConversationService` agnóstico) y **dedupe por `update_id` YA existen** (`telegram.controller.ts:66-72`) → probar el existente, no duplicar. FIN-028 cerrado → editar/anular conversacional disponibles. Detalle: `docs/oficial/DEC-0029-Integracion-Telegram.md`. Verificado: `ConversationService` ya es agnóstico (`ChannelSource`), `telegram.controller:110` lo invoca — un motor, ambos adaptadores (formalizar, no inventar); `pendiente_confirmacion`/`parseConfidence` existen desde el día 1. **Hallazgo de precisión: el dedupe por `update_id` que el ARQ propone añadir YA EXISTE y está activo** (`telegram.controller.ts:66-72`, `webhookEvent.externalId` único, `if (seen) continue`) — el IMP debe verificar/testear el existente, no construir un segundo. IA como intérprete de intención con tools minimizadas (desacople correcto); tests PII/genericidad por tool = condición permanente (FIN-005); gate DPA+PIA intacto. Depende de FIN-028 (servicio central) — secuencia IMP 028→029. Fase ARQ autorizada en paralelo por excepción documentada. Restricción arquitectónica del Fundador: **un solo** servicio conversacional para Telegram/WhatsApp/futuros canales (no dos motores); desacople estricto Motor Financiero ↔ Motor Conversacional. Datos reales de IA siguen bloqueados por el gate DPA+PIA (`PRODUCCION.md` §1) — el ARQ diseña, no enciende `ANTHROPIC_API_KEY` en producción. Detalle: `docs/correspondencia/FIN-029-Integracion-Telegram.md` |

| FIN-030 | **Umbrella — Rediseño de Deudas por tipo de obligación + "movimientos inteligentes" (SO Financiero Personal)** · **Origen: Funcionalidad nueva + revisión de producto (§27), prioridad MÁXIMA** | Máxima | ✅ ARQ-0030 v1.0 umbrella (`2a18a76`) | ✅ AUD-0030 — **APROBADO CON OBSERVACIONES** | ✅ DEC-0030 (umbrella; 5 condiciones §3) | ➖ | **Umbrella DECIDIDO** (no tiene IMP propio). Aprobada la espina (Alt A + capa de consecuencias por listeners con causalidad sobre el outbox de FIN-002 → §42 por construcción). Verificado por el CTO: `DebtType`=9→12 por extensión (sin reescribir); creación de tx ya emite `TransactionCreated` (`:120-123`). Condiciones para las FIN derivadas (`DEC-0030` §3): política de reversión con dependientes (cierre de FIN-031); "flujo de caja" pasa el gate del DSS antes de existir; grep §32 + test de reversibilidad como cierre de cada FIN. **Criterio de cierre (DEC-0030 §6, Fundador+CPSAO):** FIN-030 NO cierra con solo 2 productos; requiere (a) catálogo de los 11, (b) espina probada contra los 4 arquetipos divergentes (libranza/hipoteca/gota a gota/compra a cuotas) sin ramas ad-hoc, (c) los 11 registrables con números núcleo §32, (d) alta mínima B. Profundidad avanzada por producto = progresiva (FIN-033+), no bloquea. Desglose: `FIN-031` ✅ (patrón, compra-tarjeta) · **`FIN-032` = la fundación que consolida FIN-030** (catálogo + 4 arquetipos + 11 registrables) · `FIN-033` profundidad avanzada + confirmación mensual. Detalle: `docs/oficial/DEC-0030-SO-Financiero-Deudas-Movimientos.md`. Disciplina umbrella cumple (define la espina, solo diseña Fase 1 implementable). Verificado: `Debt`/`AmortizationEntry`/`DebtInsurance` existen → Alt A (capa sobre lo existente) viable sin reescribir ni rehacer §32; el alta ya emite `TransactionCreated` por outbox (`transactions.service.ts:123`) → la capa de consecuencias es composición real; la tarjeta no tiene cupo hoy (Fase 1 lo añade); `DebtType` tiene 9 valores (+3=12≥11). **Observación sustantiva (borde §42):** la reversión es limpia para efectos derivados (se auto-sanan por recompute del Motor) pero **NO** para mutaciones persistidas una vez que una acción posterior las toca — anular una compra cuyas cuotas ya tienen pagos no es limpio; Fase 1 debe declarar esa política. Matiz: el flujo "¿cómo pagaste?" SÍ extiende Registrar (autorizado por el Fundador), solo la cascada es pura composición. §32/§42 bien encuadrados como criterios auditables FIN por FIN. "Flujo de caja" diferido al DEC con gate del DSS. **Retención levantada.** El Fundador entregó sus 5 observaciones sobre Registrar (memo 2026-07-14): flujo "¿cómo pagaste?" inteligente por método; la compra con tarjeta ES la confirmación (sin 2º paso); modelo de confirmación en dos niveles (consecuencia directa → sin confirmar / modificación de datos no ingresados → confirmar); Registrar como punto de entrada del ecosistema; principio "el usuario registra un hecho, Milla las consecuencias". Directiva `ARQ-0030` emitida con A–K + §31/§32/§42 como criterios de aceptación. Registrar/Transacciones autorizado a tocarse. Decisión de producto del Fundador elevada por el CPSAO (`docs/correspondencia/Rediseno-Modulo-Deudas.md`): productos financieros como entidades de primera clase (deuda-por-tipo, ≥11 tipos, selección por tipo primero, mínimo obligatorio + resto progresivo) **+** capa de consecuencias por evento ("una acción → todos los efectos, todos visibles/explicables/reversibles", §42) — **una sola transformación**. Guardarraíles A–K + §31/§32 como criterios de aceptación duros. **Entra de lleno en Registrar/Transacciones** (el "cómo pagaste" ES el flujo de registro) → dispara la instrucción permanente del Fundador; el ARQ no abre hasta sus observaciones. Faseo recomendado (CPSAO): Fase 1 = espina (modelo de producto + capa de consecuencia) con compra-con-tarjeta de punta a punta; luego el resto de tipos; luego confirmación mensual (sobre el motor conversacional único de FIN-029). Umbrella §"un FIN a la vez": define alcance, no diseña en detalle >1 funcionalidad. Detalle: `docs/correspondencia/Rediseno-Modulo-Deudas.md`. Principio institucionalizado: `GOBERNANZA.md` §42. |

| FIN-031 | **Fase 1 de FIN-030 — espina del SO Financiero (producto-entidad + capa de consecuencias por evento) + compra-con-tarjeta de punta a punta** · **Origen: Funcionalidad nueva (§27), prioridad MÁXIMA** | Máxima | ✅ ARQ-0031 v1.0 | ✅ AUD-0031 — **APROBADO CON OBSERVACIONES (GAP §32)** | ✅ DEC-0031 (4 cambios obligatorios §5) | ✅ IMP-0031 v1.0 (`8473ed5`) | **Cerrada (validación técnica del CTO).** Verificado en checkout sobre `8473ed5`: los 4 cambios obligatorios confirmados — **§32 una autoridad** (`debt-outlay.service.ts:54-60` computa el compromiso de tarjeta = suma de la próxima cuota no pagada por compra, UNA ruta; sin compras cae a `monthlyPayment`; grep limpio, `card.service` solo deriva cupo/saldo); reversibilidad §4.5 (anular sin cuotas pagadas revierte; **con cuota pagada → 409** + ruta de corrección); cupo/saldo derivados en `CardService` hoja; "flujo de caja" fuera. Suites por el CTO: tsc back+front 0, unit **357/357** (46), e2e **49/49** (12, incl. `fin031-tarjeta` 5/5 + regresión de todos los consumidores de la autoridad). Backend+migraciones → Render; frontend → OTA (§40/§41). Firma de producto: el Fundador. Detalle: `docs/oficial/DEC-0031-Fase1-Espina-Compra-Tarjeta.md`. **Primer IMP de la iniciativa FIN-030.** **Decidida — `IMP-0031` habilitado.** Verificado por el CTO: `DebtOutlayService.outlaysByUser` (`:40`) es la **autoridad única** de "lo comprometido" (inyectada por teQueda/presupuesto/Copiloto/Motor) y computa desde `Debt.monthlyPayment` → las cuotas de tarjeta NO entran solas (GAP §32 confirmado). **Cambio obligatorio principal: extender `DebtOutlayService` para incluir `CardInstallment` — UNA ruta por inyección; prohibida una 2ª ruta en `CardService`.** Política de reversión §4.5 (Alt A) aprobada; grep §32 + test de reversibilidad como cierre. Detalle: `docs/oficial/DEC-0031-Fase1-Espina-Compra-Tarjeta.md`. **Auditada — en espera de DEC.** Condición de cierre (política de reversión con dependientes) **resuelta con precisión** (§4.5): sin cuotas pagadas→anulación limpia; con ≥1 pagada→bloquear+ruta de corrección (Alt A); rechaza el contra-asiento compensatorio por reversible-pero-no-explicable (§42 exige ambas). cupo/saldo derivados en `CardService` hoja (§32 correcto; `creditLimit` no existe hoy). **Observación con peso (GAP §32 de integración):** las `CardInstallment` (modelo nuevo) NO fluyen solas a teQueda/desembolso — `SpendableService`/`DebtOutlayService` leen `Debt.monthlyPayment`, no un modelo de tarjeta; el IMP debe **extender `DebtOutlayService`** (autoridad única de "lo comprometido") en UNA ruta, no crear una segunda en `CardService`; corregir el §4.4/§8 ("cero fórmula nueva/ya entra" es impreciso). Flujo de caja correctamente fuera (no pasa gate DSS). Primer FIN derivado del umbrella FIN-030. Alcance acotado: la espina + UN caso e2e (compra con tarjeta de crédito → actualiza cupo/saldo/obligación/cuotas/presupuesto/Score/flujo, todo visible/trazable/reversible §42), NO los 11 tipos. Ejercita G/H/I/J juntos. **Condición de cierre dura (AUD-0030):** declarar la política de reversión con dependientes (anular compra con cuotas ya pagadas — bloquear+corrección o reversión compensatoria). "Flujo de caja" solo si pasa el gate del DSS. Criterios de cierre: grep §32 + test de reversibilidad de la cascada. Detalle: `docs/correspondencia/Rediseno-Modulo-Deudas.md` · `docs/oficial/DEC-0030-SO-Financiero-Deudas-Movimientos.md`. |

| FIN-032 | **Fundación de FIN-030 — catálogo de los 11 tipos + espina probada contra los 4 arquetipos divergentes + los 11 registrables con números núcleo §32** · **Origen: Funcionalidad nueva (§27), prioridad MÁXIMA** | Máxima | ✅ ARQ-0032 v1.0 (`8361cbc`) | ✅ AUD-0032 — **APROBADO CON OBSERVACIONES** | ✅ DEC-0032 v1.0 — **habilita IMP-0032** | ✅ IMP-0032 v1.0 (`c96c355`) — entregado, en espera de validación del CTO | **IMP entregado — en espera de validación/integración del CTO.** Los 5 cambios obligatorios cerrados (`c96c355`): TODAS las ramas por `debtType`/`isCard` disueltas (back+front) en `PRODUCT_TYPE_DESCRIPTORS` + despacho por `scheduleModel` (`card.service`→capacidad, `debts.service`→modelo, `debt-outlay`→autoridad única, `AddDebtScreen`→alta desde `/debts/catalog`, `DebtDetailScreen`→secciones por modelo); grep §32 sobre back+front limpio (única coincidencia = chip activo dinámico); guarda doble-conteo libranza; gota a gota sin fecha falsa (§29.2); regresión de los 9; prueba de los 4 arquetipos (e2e `fin032-arquetipos` 7/7). Suites: unit 361/361, e2e 13/55, tsc limpio, migración `ALTER TYPE ADD VALUE`. Su cierre consolida FIN-030. **DEC emitida — IMP-0032 en curso.** Tesis correcta: 4 arquetipos → 3 `scheduleModel` ya existentes (`amortizado`/`cuotas_por_compra`/`saldo_y_cuota_pactada`) por configuración (`PRODUCT_TYPE_DESCRIPTORS`, autoridad única de tipo), no lógica por-tipo; DTI = `DebtOutlayService.totalOutlay ÷ NetIncomeService.netIncome` (composición de fuentes únicas). Points verificados: arquetipos caben sin rama (con guarda de doble-conteo de libranza vs deducción FIN-027 + sin fecha falsa en informal); enum `ALTER TYPE ADD VALUE` no reescribe Debt. **Observación con peso (§32):** el ARQ **subcuenta las ramas por-tipo actuales** — nombra 3 pero hay una 4ª en `card.service.ts:179` y el formulario `AddDebtScreen`/`DebtDetailScreen` entero ramifica por `isCard`; el grep §32 de cierre es cumplible **solo si el IMP disuelve TODAS** (no las 3), y la Validación debe correrlo contra el conjunto completo — es donde el bug ×tipos entraría por el frontend. La FIN que **consolida el umbrella FIN-030** (criterio de cierre del Fundador+CPSAO, `DEC-0030` §6). Criterios de aceptación: (a) los 11 tipos en el catálogo (extiende `DebtType`=9 → +libranza/compra_a_cuotas/fintech/personalizable); (b) espina **probada contra los 4 arquetipos divergentes** (libranza, hipoteca, gota a gota, compra a cuotas) SIN ramas ad-hoc; (c) los 11 registrables con números núcleo correctos (cuota/saldo/"Te queda"/DTI/fecha de libertad) por las fuentes únicas §32; (d) alta mínima por tipo (guardarraíl B). Profundidad avanzada por producto (confirmación mensual, retanqueo, refinanciación) → FIN-033+, no bloquea el cierre. Sobre la espina ya validada por FIN-031. Condición de cierre auditable: tests de los 4 arquetipos con cifras núcleo + grep §32. Detalle: `docs/correspondencia/Rediseno-Modulo-Deudas.md`. |
| FIN-033 | **Profundidad avanzada por producto + confirmación mensual por corte** (sobre el motor conversacional único de FIN-029) · **Origen: Funcionalidad nueva (§27)** | Alta | ⏳ | ➖ | ➖ | ➖ | **Registrada, no iniciada.** Comportamiento avanzado por tipo de producto (retanqueo/refinanciación de libranza, abonos extraordinarios de hipoteca, cambios de condiciones) + el flujo de confirmación mensual ("Milla pregunta en la fecha de corte si cambió algún valor"). Progresiva, **no bloquea** el cierre de la fundación FIN-030. Depende de FIN-032 (fundación) y FIN-029 (motor, cerrada). |

*(Desde `FIN-020`, formato corto: una línea de estado con enlaces — Nivel "índice" de `docs/oficial/PROPUESTA-2026-07-12-Reorganizacion-Documental.md`. La evidencia de verificación vive en el `DEC` correspondiente, no en esta tabla. Filas `FIN-001`–`FIN-019` conservan el formato narrativo original — no se reescribe el historial ya producido.)*

## Hoja de ruta de experiencias UX (orden acordado con el CPSAO, 2026-07-11)

Cada "experiencia" se ejecuta como una `FIN` propia, bajo la regla ya vigente "un FIN
a la vez" — no se abre la siguiente experiencia hasta cerrar la anterior. Ciclo por
experiencia: Propuesta → Implementación → Capturas reales → Revisión CPSAO → Ajustes
→ Aprobación.

1. **Experiencia de Inicio** — `FIN-017` + `FIN-018`, **CERRADA**. Pasó por revisión
   de producto (`PRODUCT_REVIEW_001`), recorrido integral con capturas de scroll
   completo, 4 iteraciones de diseño (`DEC-017`, `DEC-018` y sus adendos) y una
   revisión de comprensión de primer nivel (`RC-0001`, walkthrough cognitivo — sesión
   real reprogramada, ver punto de Validación integral más abajo).
2. **Experiencia de Salud** — `FIN-019`, **CERRADA**. Corrigió la regla de color por
   pilar (ruta b, sin semáforo sin fundamento en Patrimonio), reutilizó el motor de
   recomendaciones de FIN-007, y añadió cold-start con expectativa y acciones.
3. Experiencia de Presupuesto.
4. Experiencia de Deudas.
5. Experiencia del Simulador.
6. Experiencia del Copiloto.

**Validación integral de comprensión (RC), reprogramada por el CPSAO (2026-07-11):**
en vez de una sesión real de Revisión de Comprensión por experiencia, se ejecutará
**una sola RC integral** al finalizar la iteración de las 6 experiencias — valida el
recorrido completo por la app con personas reales, no una pantalla aislada. El diseño
ya producido para Inicio (`docs/producto/rc/RC-0001-Inicio.md` v1.3: guion, formato de
registro de recorrido mental, indagación del razonamiento) queda preservado y se
reutiliza como base metodológica de esa RC integral. Cada experiencia, mientras tanto,
sigue su propio ciclo de iteración con el estándar de captura completa y revisión de
comprensión de primer nivel (walkthrough del Auditor) antes de darse por iterada —
"un FIN a la vez" sigue vigente; solo la sesión con participantes reales se pospone.

**Aclaración del CTO sobre la excepción técnica que el CPSAO permite:** "el resto del
producto puede avanzar técnicamente cuando no exista dependencia" se interpreta como
correcciones puntuales bajo la Regla de Acciones Correctivas o defectos triviales (p.
ej. el bug ya identificado de la ruta `MilloPlus` no registrada) — **no** como
autorización para abrir una segunda `FIN` de experiencia en paralelo. La disciplina
"un FIN a la vez" no se relaja para el trabajo de UX.

## Oportunidades de evolución — Experiencia de Salud (futuro, no bloqueantes)

Registradas por el CPSAO en su revisión estratégica de `FIN-019` (2026-07-12), tras
validar la experiencia desde perspectiva de producto. **No bloquean el cierre de
FIN-019** (ya cerrado) ni exigen una `FIN` inmediata — quedan como línea de
investigación para una futura iteración de Salud, a evaluar cuando el CTO/CPSAO lo
consideren oportuno (p. ej. tras completar la hoja de ruta de las 6 experiencias):

1. **Celebrar avances, no solo señalar carencias** — reforzar hábitos correctos ya
   presentes ("ya cubres 4 meses de gastos"), no solo lo que falta.
2. **Revisar la jerarquía del Hero** — evaluar si el protagonista visual debería ser
   el estado financiero del usuario y no únicamente el número del Score.
3. **Evolucionar la explicación de los pilares** — que cada barra cuente qué
   significa ese nivel para la vida del usuario, no solo el dato.
4. **Conectar acciones con el efecto esperado sobre el Score** (mayor valor
   señalado) — p. ej. "si logras 3 meses de fondo de emergencia, tu Score podría
   mejorar hasta...".
5. **Convertir "Evolución" en narrativa de progreso**, no un histórico plano.

**Reflexión de continuidad narrativa (CPSAO):** Inicio responde "¿cómo estoy hoy?",
Salud responde "¿por qué estoy así y qué puedo hacer?" — Milla está construyendo un
recorrido narrativo entre experiencias, no módulos independientes. El Arquitecto debe
mantener esta continuidad explícitamente en las siguientes experiencias (cada una
responde una pregunta distinta y prepara la siguiente). Se refuerza formalmente con
la nueva sección 31 de `GOBERNANZA.md` v3.9 (pregunta de cierre "¿qué perdería el
usuario si elimináramos esta experiencia?", ratificada por el Fundador).

## Gates de producción pendientes (fuera del ciclo ARQ→AUD→DEC→IMP)

Ningún módulo (FIN-001…009) está "incompleto" por esto — el código de cada uno está
cerrado y validado. Lo que sigue pendiente es **decisión de negocio o acción externa**,
no deuda técnica. Se mantiene esta tabla para que quede mapeado y no se pierda entre
ciclos futuros (FIN-010 en adelante). Detalle operativo completo en
`docs/PRODUCCION.md` (generado en FIN-009); esta tabla es el resumen visible en el
documento maestro.

| Gate | Tipo | Origen | Responsable | Estado |
|---|---|---|---|---|
| DPA (Data Processing Agreement) con Anthropic | Legal/contractual | DEC-0005 §14.3 | Fundador | ⏳ Pendiente |
| PIA (evaluación de impacto en privacidad, Ley 1581) | Legal | DEC-0005 §14.4 | CTO (yo la produzco) | ⏳ Pendiente |
| Revisión legal final (consentimiento, términos, DPA) | Legal | DEC-0005 §15 | Fundador + abogado | ⏳ Pendiente |
| Política de tiendas (Apple/Google) para apps financieras con IAP | Legal/negocio | DEC-0009 §9 | Fundador/CTO | ⏳ Pendiente |
| Precio real de Millo+ (`MILLOPLUS_PRICE_COP`) | Negocio | DEC-0009 §10.5 | Fundador (con telemetría de `cost-report`) | ⏳ Pendiente |
| Cuenta RevenueCat + productos IAP configurados | Operativo/negocio | DEC-0009 | Fundador | ⏳ Pendiente |
| Activación `HEALTH_SCORE_PRODUCTION_ENABLED` | Técnico (flag ya construido) | DEC-0004 §10.3 | Se enciende cuando se apruebe la revisión legal del Score | 🔒 Bloqueado |
| Activación `COPILOT_PRODUCTION_ENABLED` + `ANTHROPIC_API_KEY` real | Técnico (flag ya construido) | DEC-0005 | Se enciende cuando DPA+PIA estén resueltos | 🔒 Bloqueado |
| Infraestructura de producción (rotar secretos, backups de BD, CORS restrictivo) | Operativo | `docs/PRODUCCION.md` §3–4 | Fundador/CTO | ⏳ Pendiente |
| Cifrado a nivel de campo | Técnico | DEC-0009 §4.9 | **No pendiente** — riesgo aceptado formalmente, no bloqueante | ✅ Decidido (no implementar) |

## Historial
- 2026-07-14 — **Auditor: generado `docs/auditoria/AUD-0032-Fundacion-Catalogo-Tipos-y-Arquetipos.md`. Veredicto: APROBADO CON OBSERVACIONES.** Verificación contra `HEAD`. Tesis de fundación-por-configuración correcta: los 4 arquetipos reducen a 3 `scheduleModel` ya existentes vía `PRODUCT_TYPE_DESCRIPTORS` (autoridad única de tipo, datos no lógica); prueba de 4 arquetipos como condición de cierre auditable (evidencia, no promesa — guardarraíl F). Los 3 puntos del Arquitecto: **(1)** arquetipos caben sin rama ad-hoc — guarda de doble-conteo de libranza (cuota = compromiso en `DebtOutlayService`, NUNCA además deducción de ingreso FIN-027; coordinación forward) y gota a gota SIN fecha de libertad falsa (§29.2), ambas correctas; **(2)** grep §32 cumplible **pero el ARQ subcuenta las ramas actuales**: nombra 3 (`debt-outlay:59`/`debts.service:268`/`DebtDetailScreen:53`) pero hay una 4ª en `card.service.ts:179` y el formulario `AddDebtScreen` entero ramifica por `isCard` — el IMP debe disolver TODAS en el descriptor/`scheduleModel`, la Validación corre el grep contra el conjunto completo o el bug sobrevive por el frontend; **(3)** enum `ALTER TYPE ADD VALUE` no reescribe Debt, `saldo_y_cuota_pactada` reusa `currentBalance`/`monthlyPayment`, sin columnas nuevas, regresión de los 9 tipos testeable. FIN-032 pasa a *Auditada — en espera de DEC*.
- 2026-07-14 — **CTO: `FIN-031` CERRADA (validación técnica) — primer IMP de la iniciativa FIN-030 (SO Financiero).** Validación independiente en checkout sobre `8473ed5`. Los 4 cambios obligatorios de `DEC-0031` confirmados por el CTO: **(1) §32 una autoridad** — `DebtOutlayService.outlaysByUser` (`:54-60`) computa el compromiso mensual de una tarjeta = suma de la próxima cuota no pagada de cada compra viva, en UNA ruta (sin compras → `monthlyPayment`, regresión); grep limpio, `card.service` solo deriva cupo/saldo (concepto distinto, declarado); teQueda/presupuesto/Copiloto/Motor lo heredan por inyección. **(2) Reversibilidad §4.5** — e2e: compra mueve saldo/cupo sin 2ª deuda (I); anular sin cuotas pagadas revierte (§42); **anular con ≥1 cuota pagada → 409** + ruta de corrección (Alt B rechazada — no explicable). **(3)** cupo/saldo derivados en `CardService` hoja. **(4)** alcance acotado (11 tipos = FIN-032; flujo de caja fuera). Suites por el CTO: tsc back+front 0, unit 357/357 (46 suites), e2e 49/49 (12 suites, incl. `fin031-tarjeta` 5/5 + regresión de todos los consumidores de la autoridad: fin020/022/023/024/028). Modelo: compra a crédito no crea gasto en efectivo (`sourceTransactionId` nullable); tarjeta nace con saldo 0, UI de amortización oculta con saldo 0 (§42 explicable). Despliegue: backend+migraciones → Render; frontend → OTA (§40/§41). Firma de producto: el Fundador. Sigue "un FIN a la vez".
- 2026-07-14 — **Auditor: generado `docs/auditoria/AUD-0031-Fase1-Espina-Compra-Tarjeta.md`. Veredicto: APROBADO CON OBSERVACIONES.** Verificación contra `HEAD`. **La condición de cierre de `DEC-0030` §3.1 (mi observación de reversión con dependientes) está resuelta con precisión (§4.5):** regla, no alternativa — sin cuotas pagadas→anulación limpia (revierte por los listeners de FIN-028); con ≥1 pagada→bloquear+ruta de corrección (Alt A). Rechazo correcto de Alt B (contra-asiento): reversible pero NO explicable — §42 exige ambas y falsear un asiento viola "nunca mentir hacia arriba". cupo/saldo DERIVADOS en `CardService` hoja (§32; `creditLimit` no existe hoy, extensión nueva); amortización reusa FIN-012 (función pura). **Observación con peso (GAP §32):** el ARQ §4.4/§8 afirma que las cuotas de tarjeta entran solas a teQueda/desembolso ("cero fórmula nueva") — contra código NO: `DebtOutlayService` (`debt-outlay.service.ts:40`) computa "lo comprometido" desde `Debt.monthlyPayment`+`insurances`, no desde un modelo de tarjeta; las `CardInstallment` nuevas no fluyen solas. El IMP debe extender la fuente única `DebtOutlayService` para incluirlas en UNA autoridad (no una 2ª ruta en `CardService`), o el bug §32 renace en Fase 1; el grep de cierre debe cubrirlo. Composición sobre Registrar confirmada (el alta ya emite `TransactionCreated`); sin duplicados (I) y flujo de caja fuera del gate DSS, correctos. FIN-031 pasa a *Auditada — en espera de DEC*.
- 2026-07-14 — **Auditor: generado `docs/auditoria/AUD-0030-SO-Financiero-Deudas-Movimientos.md` (umbrella). Veredicto: APROBADO CON OBSERVACIONES.** Disciplina umbrella cumple (define espina, solo Fase 1 implementable, difiere el detalle de los ≥11 tipos y cada cascada a ARQ derivados). Los 3 puntos del Arquitecto verificados contra `HEAD`: **(1)** Alt A viable — `Debt`/`AmortizationEntry`/`DebtInsurance` (`schema.prisma:370/447/424`) existen y no se reescriben; §32 intacto (las fuentes únicas siguen leyendo los mismos modelos); precisión: `DebtType` tiene 9 valores, no 8 (+3=12≥11). **(2)** reversibilidad §42: limpia para los 6 efectos derivados (se auto-sanan por recompute), **NO** limpia para las mutaciones persistidas (saldo/cupo/cuotas) una vez que una acción posterior las toca — anular una compra cuyas cuotas ya tienen pagos no es limpio → Fase 1 debe declarar la política de reversión con dependientes (el borde que pidió verificar). **(3)** radio Registrar: la capa de consecuencias es composición real (el alta ya emite `TransactionCreated` por outbox, `transactions.service.ts:123`), pero el flujo "¿cómo pagaste?" SÍ extiende el flujo de entrada (autorizado por el Fundador) — "no toca su núcleo" es cierto para la cascada, no para la entrada. §32 (guardarraíl C) y §42/G bien encuadrados como criterios auditables FIN por FIN. "Flujo de caja" (J) diferido al DEC con gate del DSS. FIN-030 pasa a *Umbrella auditado — en espera de DEC*; el desglose FIN fino lo hace el CTO.
- 2026-07-14 — **CTO: `FIN-029` CERRADA — cierra la tanda de la Beta Técnica.** Validación independiente en checkout sobre `9bb83c0`. Las 6 condiciones obligatorias de `DEC-0029` con test que pasa: acuse QUÉ+DÓNDE (crear+anular); no fingir "ya lo anoté"; simular muestra sin empujar; paywall honesto; genericidad (sin marcas); motor invoca el servicio central de FIN-028. Dedupe `update_id` probado (no reconstruido). **Gate DPA+PIA cerrado por construcción** — grep limpio de LLM/Anthropic en `messaging/`, conversación determinista (reglas). El test de genericidad §5.5 cazó un defecto real (el help del bot nombraba "Bancolombia" → "mi crédito"). Suites por el CTO: tsc 0, unit 355/355 (46 suites), e2e 43/43 (11 suites, incl. `fin029-telegram`). 3 reservas declaradas aceptadas (capa IA no-runtime; handshake de confirmación pendiente del LLM; `editar` por texto = territorio LLM, hoy solo `anular`) — dependen de abrir el gate DPA+PIA. Backend-only → Beta por auto-deploy de Render (§41), sin OTA (activación del bot = paso operativo posterior). Observación menor al CPSAO: placeholders de UI preexistentes nombran "Bancolombia" (fuera de alcance). **Tanda completa: FIN-027 + FIN-028 + FIN-029 + BT-001 + BT-003.**
- 2026-07-14 — **CTO: `DEC-0029` emitida — `IMP-0029` habilitado (último de la tanda).** El CPSAO dio visto bueno con 4 condiciones, incorporadas como cambios obligatorios: acuse explícito de todo movimiento (sin cambios de estado en silencio); honestidad al no entender (prohibido el falso "ya lo anoté"); barrera de Independencia a futuro (toda tool que dé un consejo, no un dato, vuelve al CPSAO; simular solo muestra escenarios); paywall honesto al agotar la cuota de IA. Más test PII/genericidad por tool (FIN-005) y gate DPA+PIA intacto. Verificación independiente del CTO: el motor único (`ConversationService` agnóstico) y el dedupe por `update_id` YA existen (`telegram.controller.ts:66-72`) → el IMP prueba lo existente, no duplica. FIN-028 cerrado habilita editar/anular conversacional. Detalle: `docs/oficial/DEC-0029-Integracion-Telegram.md`.
- 2026-07-14 — **CTO: `FIN-027` CERRADA (validación técnica).** Validación independiente en checkout sobre `67cf375` — código y suites reejecutadas por el CTO. Los 4 cambios obligatorios de `DEC-0027` confirmados: copy en Salud (`health.service.ts:92`, texto del CPSAO condicionado a `hasDeductions`, en backend); migración sin coexistencia (`budget.service.ts:30`, introducido por el commit, rechaza `kind='ingreso'`→400); `NetIncomeService` hoja + `engine.service.ts` usa `netFixedTotal` → **DTI/Score sobre NETO**; `withheldAtSource` implementado; regresión sin fuentes = cifras idénticas. Suites por el CTO: `tsc` BE+FE 0, unit 345/345 (45 suites), e2e 40/40 (10 suites, incl. `fin027-modelo-ingresos`, regresión `fin020` con base neta verde). 3 reservas declaradas aceptadas para iteración 1 (sin retefuente automática; fuentes variables sin deducción propia; UI de deducciones solo modo %). Firma de producto en dispositivo real: el Fundador. Secuencia: `IMP-0028` ✅ · `IMP-0027` ✅ — falta `DEC-0029` (con la puerta de revisión del CPSAO sobre el alcance del bot) antes de `IMP-0029`.
- 2026-07-13 — **CTO: `DEC-0027` emitida — `IMP-0027` habilitado (2º de la secuencia).** El CPSAO decidió (consulta de producto del CTO) **DTI/Score sobre ingreso NETO** con **nota de copy obligatoria** en Salud (evitar que declarar deducciones se lea como castigo; patrón costo-de-honestidad FIN-020/021). CTO verificó la premisa (`core-metrics.ts:36` usa bruto) y resolvió la 2ª decisión abierta del ARQ: **migración** de los `FixedItem` de ingreso al modelo de fuentes, se rechaza la coexistencia (§32). 4 cambios obligatorios: copy en Salud, migración, `NetIncomeService` única def. del neto (6 consumidores lo inyectan), regresión sin perfil = idéntico. `withheldAtSource` aprobado. CPSAO además dejó dos condiciones registradas: trazabilidad **visible** de anulación como seguimiento de una futura iteración de `FIN-028` (no reabre la cerrada), y una **puerta de revisión de tono/independencia del bot** para `DEC-0029` (nada de recomendar productos por conveniencia, todo explicable). Detalle: `docs/oficial/DEC-0027-Modelo-de-Ingresos.md`.
- 2026-07-13 — **CTO: `BT-001` corregido (defecto de Beta Técnica) + directriz de gestión de defectos institucionalizada (`GOBERNANZA.md` v3.16, §38/§39).** El Fundador reportó un 500 al registrar una tasa `15,35`. Causa raíz verificada por el CTO: el frontend borraba la coma (`AddDebtScreen.tsx:20`) → `1535` desbordaba `interest_rate Decimal(7,4)` → Prisma 500. Clasificado como **defecto de implementación** (no FIN). Fix en dos capas: backend autoritativo `common/parse-number.util.ts` + `@NormalizeNumber()` en el DTO de deuda (+`@Max(999.9999)` → fuera de rango da 400, no 500); frontend `parseDecimal`/`parseAmount` en `utils/format.ts`. Verificado por el CTO: unit `parse-number` 9/9, e2e `bt001-formato-regional` 4/4 (`"15,35"`→201 guarda 15.35; fuera de rango→400), suites completas sin regresión (unit 340/340, e2e 31/31, tsc BE+FE 0). Registrado en el nuevo bug tracker `docs/oficial/REGISTRO-DEFECTOS.md`. El Fundador estableció además dos reglas permanentes: §38 (todo defecto lo evalúa/clasifica el CTO; solo nuevas necesidades → FIN) y §39 (formato regional obligatorio en campos numéricos). `BT-002` (editar/anular movimientos) encauzado a `FIN-028` (ya CERRADA). **Consulta al CPSAO abierta:** decisión de producto de `FIN-027` (DTI/Score neto vs. bruto) — `DEC-0027` a la espera de esa definición.
- 2026-07-13 — **CTO: `FIN-028` CERRADA (validación técnica).** Validación independiente en checkout sobre `65104e1` — código verificado y suites reejecutadas por el CTO, no sobre el reporte. Los 4 cambios obligatorios de `DEC-0028` confirmados: grep limpio de `anulada`/`voidedAt` (anulación = `deletedAt`, corrección del CTO acatada — el Arquitecto confirmó "hizo el diseño más pequeño"); `ACTIVE_TX_FILTER` solo en consultas de `Transaction` (los 97 de otros modelos intactos, verificado por diff); impacto retroactivo declarado; infra/gate intactos. Bug fundacional cerrado: `update`/`remove` emiten `TransactionUpdated`/`TransactionDeleted` vía outbox con payload rico (changedFields+before/after → auditoría/IA sin rediseño de BD); `undoLast` de mensajería reencaminado al servicio central; anular pago de deuda revierte saldo atómicamente + `DebtUpdated`; guardarraíl `DEBT_LOCKED_FIELDS` (monto/fecha/tipo/deuda en pago de deuda → 400). Suites por el CTO: `tsc` exit 0, unit 331/331 (43 suites), e2e 27/27 (8 suites, incl. `fin028-movimientos` 4/4 probando recálculo real y reverso de deuda; sin regresión FIN-020..024/012). 2 limitaciones declaradas aceptadas para iteración 1: anular pago de deuda no reconstruye `next_due_date` (registrada como seguimiento, la re-ancla el próximo pago vía FIN-018); UX en filas de Inicio (no hay pantalla de lista completa). Firma de producto en dispositivo real a cargo del Fundador. Sigue `DEC-0027` (turno 2), con decisión de producto pendiente (DTI neto vs. bruto).
- 2026-07-13 — **CTO: `DEC-0028` emitida — `IMP-0028` habilitado, con corrección de diseño al ARQ.** Verificación independiente contra código (no sobre el reporte del Auditor): bug confirmado — `update`/`remove` (`transactions.service.ts:160-178`) escriben sin outbox pese a que los tipos `transaction.updated/deleted` y el listener del Motor ya existen (`engine.listener.ts:24-26`); corrección mínima. **Hallazgo del CTO que corrige el ARQ:** la anulación lógica **ya existe** vía `deletedAt` (`remove()` ya hace soft-delete en `:175`; 97 ocurrencias de `deletedAt:null` ya excluyen anulados) — por eso `DEC-0028` §5.1 **rechaza** añadir el estado `anulada`+`voidedAt` que proponía el ARQ: sería un 3er concepto de anulación junto a `deletedAt` y al dormante `descartada`, violando §32. Aprobado el resto: outbox en las mutaciones, evento rico (changedFields+before/after) para auditoría/IA sin rediseñar BD, servicio central único (reencaminar `undoLast`), guardarraíl de deuda (anular-y-recrear monto/fecha). `ACTIVE_TX_FILTER` acotado solo a consultas de `Transaction`, verificado por diff (no los 97 a ciegas — observación del Auditor elevada a condición). Secuencia de IMP confirmada 028→027→029. `DEC-0027`/`DEC-0029` retenidas para su turno; la de 027 tiene una decisión de producto pendiente (DTI neto vs. bruto) que puede requerir al CPSAO. Detalle: `docs/oficial/DEC-0028-Gestion-de-Movimientos.md`.
- 2026-07-13 — **Auditor: generados `AUD-0027`, `AUD-0028`, `AUD-0029` (tres AUD separados, no agrupado — §5).** Diseño en paralelo por excepción documentada; cada ARQ auditado individualmente contra código real (`HEAD`). **AUD-0027 (APROBADO C/OBS):** `incomeRef=max(fixedIncome,actualIncome)`=bruto confirmado (`core-metrics.ts:36,46`); `NetIncomeService` hoja §32; decisión DTI neto/bruto diferida al DEC (correcto). **AUD-0028 (APROBADO C/OBS):** dos incumplimientos reales confirmados (`transactions.service.ts:160-178` update/remove sin outbox; `undoLast` en mensajería); listener ya escucha `transaction.updated/deleted` (falta emitir); observación con peso: mapear los ~24 filtros `ACTIVE_TX_FILTER` consulta por consulta, no a ciegas. **AUD-0029 (APROBADO C/OBS):** motor agnóstico ya existe (`ConversationService`); **hallazgo de precisión: el dedupe por `update_id` YA existe y está activo** (`telegram.controller.ts:66-72`, `webhookEvent.externalId`) — el IMP no debe duplicarlo; tests PII/genericidad = condición permanente. Coordinación 028↔029 verificada consistente (un servicio central, el motor lo invoca). Los tres pasan a *Auditados — en espera de DEC*; secuencia de IMP la fija el CTO (recomendación del Arquitecto: 028→027→029).
- 2026-07-13 — **Fundador: observaciones de FIN-028 entregadas (10 decisiones) → retención levantada, `ARQ-0028` emitido.** El Fundador respondió su propia instrucción permanente sobre el módulo Registrar con 10 decisiones vinculantes (anulación lógica con estado Anulado excluido de todo cálculo; edición amplia con recálculo automático; confirmación previa; modelo preparado para auditoría futura sin rediseño; recálculo de todo lo derivado sin sync manual; editar/anular sin lógica financiera propia — el Motor decide, §32; UX rápida; eventos para IA futura; mismo servicio central de movimientos para UI/Telegram/WhatsApp; principio rector "registrar nunca es irreversible"). CTO levantó la retención y emitió la directiva `ARQ-0028` traduciendo las 10 decisiones a lineamientos (anulación por construcción con filtro compartido, recálculo vía bus de eventos FIN-002, servicio de dominio único coordinado con `ARQ-0029`, evento de cambio con forma suficiente para auditoría/IA futura, impacto de anulación retroactiva declarado como en FIN-024). `FIN-028` pasa de retenida a ARQ en curso.
- 2026-07-13 — **CTO: `FIN-026` CERRADA (validación técnica), por instrucción del Fundador.** Verificación independiente contra el commit de referencia `68588c8` (cero backend, confirmado por `git diff --stat`): los 3 cambios obligatorios de `DEC-0026` confirmados en código — (1) `abono_extra` en ambos mapas `SIM_BY_KIND` (Salud `:218` / Presupuesto `:210`) + fallback mudo eliminado (`unknownScenario` `:139` → banner visible `:246-252`); (2) P2 frontend-only con `allowZero:true` (`:87`, validación `:215`) y precarga `extraBudget:0` desde `DebtsListScreen.tsx:194`, sin backend en el diff; (3) titular por delta del Score (`headline()` `:418-422`, omite delta 0). `tsc --noEmit` reejecutado por el CTO: exit 0. Suites unit/e2e sin cambios (FIN frontend-only, corridas como regresión) — validación por código+capturas, método anticipado por el AUD (precedente FIN-017). Sexta experiencia UX cerrada. La firma de producto en dispositivo real la ejecuta el Fundador con el APK OTA-capaz recién compilado (build `21922b26`), que ya incluye este código. Con esto, la hoja de ruta UX queda en 6/6 excepto la Experiencia de Copiloto.
- 2026-07-13 — **Fundador: apertura de tres frentes de trabajo en paralelo durante la espera del build OTA — `FIN-027`, `FIN-028`, `FIN-029`.** Autorizado iniciar trabajos que no dependen de la compilación. **Excepción documentada a "un FIN a la vez" (CTO):** la regla admite excepción con autorización expresa y documentada del CTO; el memo del Fundador la autoriza y el CTO la acota — **solo la fase de diseño (ARQ) corre en paralelo**; el IMP sigue siendo estrictamente secuencial y **no adelanta el cierre pendiente de `FIN-026`** (IMP-0026 entregado, a la espera de validación del CTO y de la prueba OTA/APK). **`FIN-027` (modelo de ingresos)** y **`FIN-029` (Telegram / motor conversacional único)** — directivas del CTO al Arquitecto emitidas (`docs/correspondencia/FIN-027-*`, `FIN-029-*`). **`FIN-028` (gestión de movimientos: editar/eliminar)** — **ARQ RETENIDO por el CTO**: toca el módulo Registrar/Transacciones y dispara la instrucción permanente del Fundador de avisar con anticipación para hacer observaciones; no se emite directiva al Arquitecto hasta recibirlas. Restricciones del Fundador respetadas: no tocar infraestructura validada (Render/Neon/Expo/EAS Update), desacople Motor Financiero↔Conversacional, trazabilidad por el flujo ARQ→AUD→DEC, no afectar la validación del build OTA en curso. CPSAO informado (CC).
- 2026-07-13 — **Fundador: MSC aprobado con ajustes e institucionalizado (`GOBERNANZA.md` v3.15, §37).** El Fundador aprobó la `PROPUESTA-Comunicacion-de-Cambio-de-Etapa.md` con tres ajustes: **(1)** disparador adicional por cambios de composición/responsabilidades del equipo (coherente con §22/§23); **(2)** el MSC cierra solicitando confirmación de lectura solo a los roles afectados, como evidencia de sincronización, no aprobación; **(3)** reserva expresa a cambios estructurales, con revisión si su frecuencia se eleva. Incorporado por el CTO como §37 (a su criterio de ubicación, tras §36), con actualización de la propuesta (estado→aprobada), del memorando emitido (añadida §10 de confirmación de lectura, ahora plantilla del MSC) y de `ESTADO_PROYECTO.md`. El Fundador dejó constancia de reconocimiento por haber presentado la propuesta para decisión en lugar de incorporarla directamente.
- 2026-07-13 — **CTO: emitido el memorando oficial de sincronización de contexto al equipo (`docs/correspondencia/MEMO-Sincronizacion-Contexto-Operativo-2026-07-13.md`), por instrucción del Fundador.** Oficializa el nuevo estado operativo (Fase 0 finalizada, backend en producción, gobernanza v3.14 §34–§36, flujo con el CTO como único integrador, testing obligatorio, política de escalado) como línea base única para CPSAO/Arquitecto/Auditor, que no participaron en las sesiones donde se institucionalizaron los cambios. Todo el contenido rastreable a artefacto oficial (no resumen de chat). Punto de continuación fijado: FIN-026 activa, en pausa hasta validar el APK. Además, por la "consideración adicional" del Fundador, se preparó —**sin incorporar a la gobernanza**— la `docs/oficial/PROPUESTA-Comunicacion-de-Cambio-de-Etapa.md`: evaluación de convertir la comunicación de cambio de etapa en procedimiento permanente (Memorando de Sincronización / MSC), con disparadores acotados a lo estructural, a la espera de decisión del Fundador.
- 2026-07-13 — **Fundador: Fase 0 de infraestructura oficialmente finalizada + marco de gobernanza post-Fase 0 institucionalizado (`GOBERNANZA.md` v3.14, §36).** Memo formal del Fundador (`docs/correspondencia/Infraestructura-Fase-0.md`) resolviendo las 4 dudas de gobernanza que el CTO planteó tras el despliegue, aprobado y autorizado íntegramente. Reglas permanentes nuevas: **(1)** documentación en GitHub — modelo híbrido (todo lo reconstruible sí: código, infraestructura, arquitectura, gobernanza, roadmap, decisiones, auditorías, manuales; secretos/legal/comercial sensible no); **(2)** flujo oficial `Fundador→CPSAO→CTO→Arquitecto→Auditor→CTO→GitHub` con el CTO como **único integrador** de la rama oficial (sin commits directos del Arquitecto); **(3)** testing obligatorio (unit/e2e/tsc/build/migraciones) antes de integrar cualquier cambio oficial, para garantizar cero regresiones; **(4)** prohibido escalar infraestructura por anticipación (solo por necesidad técnica demostrada por datos reales); **(5)** GitHub como registro histórico oficial (documentado/versionado/trazable/recuperable); **(6)** el CTO asume formalmente el rol de custodio de la calidad técnica. Componentes oficialmente incorporados: GitHub, Render, Neon, Cloudflare (preparado), Prisma, despliegue automático, infraestructura documentada. **FIN-026 se reactivará una vez validado el APK y la conectividad completa** — hasta entonces sigue en pausa; no se abren nuevas líneas estratégicas hasta cerrar el ciclo funcional actual. Institucionalizado por el CTO en el mismo acto (§34): `GOBERNANZA.md` §36 (v3.14), `ESTADO_PROYECTO.md`, este historial y el hilo de correspondencia.
- 2026-07-13 — **CTO: backend desplegado en producción (Render + Neon), Fase 0 de infraestructura sustancialmente completa.** Configuración manual en Render en tiempo real con el Fundador: 3 intentos — (1) falló por rama `chat` (histórico abandonado sin `backend/`) y Root Directory vacío, corregido a `claude/finance-app-design-pr8qd5` + `rootDir=backend`; (2) falló en build (`nest: not found`) porque `NODE_ENV=production` hace que `npm ci` omita devDependencies, corregido el Build Command a `npm ci --include=dev && npx prisma generate && npm run build`; (3) éxito — 17 migraciones Prisma aplicadas contra Neon, todos los módulos cargados. Verificado externamente por el CTO vía `curl`: `/v1/health` → 200 OK, `/v1/ready` → `db: up`. `render.yaml` corregido en el mismo acto (§34) para reflejar la configuración real (runtime Node nativo, no Docker como decía el borrador) — evita que el IaC mienta sobre cómo se desplegó de verdad. `docs/INFRAESTRUCTURA.md` §2 y §6 actualizados: pasos 1-6 del plan del Fundador (servicio, env vars, Neon, migraciones, validación, disponibilidad pública) completos; solo queda el paso 7 (conectar app móvil). `FIN-026` sigue en pausa, sin tocar.
- 2026-07-13 — **CTO: primer `push` a `origin` reveló divergencia real, resuelta con la nueva política Git (`GOBERNANZA.md` §35).** Al ejecutar el push autorizado del commit `5e7fc8e`, git lo rechazó (non-fast-forward): `origin/claude/finance-app-design-pr8qd5` tenía 7 commits del día 1 del proyecto (2026-07-04) nunca sincronizados a local — un primer intento de gobernanza abandonado (`docs/auditorias/` plural, `ARQ-0001-Gestion-Movimientos` distinto del vigente, `BACKLOG.md` de 60 líneas) más 2 commits de frontend del mismo día. El Fundador estableció la política oficial de sincronización Git (local + GitHub son conjuntamente el repositorio oficial, nunca se elimina historial sin respaldo) y autorizó el procedimiento: creé y publiqué `legacy/origin-2026-07-13` apuntando al commit `185de68` (verificado por `git ls-remote` antes de proceder), y solo entonces ejecuté `push --force-with-lease` de la rama de trabajo. Estado final verificado: rama oficial sincronizada 1:1 local↔`origin` en `5e7fc8e`; historial antiguo íntegro y accesible en `origin/legacy/origin-2026-07-13`.
- 2026-07-13 — **CTO: Infraestructura Fase 0 registrada (`docs/INFRAESTRUCTURA.md`), FIN-026 en pausa.** Instrucción directa del Fundador: GitHub, Render (backend), Neon PostgreSQL (BD) y Cloudflare (DNS/CDN, pendiente de configurar) creados como infraestructura de producción, independiente del entorno de desarrollo local. Arquitectura objetivo documentada (GitHub→Render→Neon→API→Android/iPhone); decisión oficial de priorizar infraestructura funcional antes que el dominio comercial. No es una FIN — no altera roadmap ni gobernanza, por instrucción expresa. `PRODUCCION.md` §4 referenciado cruzado para no duplicar fuente. `FIN-026` queda en pausa (no cancelada): `DEC-0026` ya emitido, `IMP-0026` habilitado, se retoma cuando el Fundador lo indique.
- 2026-07-13 — **CTO: `DEC-0026` emitido, `IMP-0026` habilitado.** Verificación independiente contra código: `SIM_BY_KIND` en `HealthScreen.tsx:212` y `BudgetScreen.tsx:205` confirmado sin `abono_extra` (solo 3 de 4 kinds); `simulations.service.ts` confirmado rechazando solo `extraBudget < 0` (acepta 0), el guard `v <= 0` de `SimulatorScreen.tsx:73` es el que sobrerrestringe en frontend. P1-P6 aprobados. 3 cambios obligatorios: tocar ambos mapas + matar el fallback mudo; P2 exclusivamente frontend (no tocar backend, ya correcto); titular §29 lidera con el delta del Score, no el absoluto (evita mostrar dos "Score de ahora" distintos frente al persistido de Salud, ~25s).
- 2026-07-13 — **Auditor: generado `docs/auditoria/AUD-0026-Experiencia-Simulador.md`. Veredicto: APROBADO CON OBSERVACIONES.** Verificación contra `HEAD`. Los 3 puntos del Arquitecto: **(1)** gap de kinds real — los 4 kinds del motor son `abono_extra`/`estrategia`/`fondo_emergencia`/`recorte_categoria`; `SIM_BY_KIND` (Salud `:212`, Presupuesto `:205`) mapea 3, falta `abono_extra` → `goSimulator(undefined)` → `SCENARIOS.find ?? SCENARIOS[0]`. P1 lo cierra **si toca ambos mapas y mata el fallback mudo** (condición). **(2)** la precarga no puede esquivar el backend (DTO `@IsNumber` + validación por escenario en cada POST); **precisión que corrige la premisa del ARQ:** el backend YA acepta `extraBudget:0` (`simulations.service.ts:216` solo rechaza `<0`); quien rechaza 0 es el frontend (`SimulatorScreen.tsx:73` `v<=0`) → **P2 es frontend-only, no tocar el backend**. **(3)** el "antes" se calcula en vivo (`loadState`→`computeScore`, no la métrica persistida) — es requisito de correctitud del delta, pero el "antes" absoluto puede diverger ~25s del Score que Salud muestra; mitigación: liderar el titular con el cambio (X→Y), no con el absoluto, y declarar la ventana. §32 correcto: defaults inyectados (`attackOrder[0]`, copy §5.2 de estrategia), hitos del fondo no narrados desde frontend (no duplica literales `DEC-0021` §5.2). Limitación declarada: FIN de frontend sin suite de pantalla (precedente FIN-017) — el mapa de kinds se verifica por lectura en la Validación. FIN-026 pasa a *Auditada — en espera de DEC*.
- 2026-07-13 — **CTO: comprensión de FIN-026 evaluada — bug de navegación activo confirmado, `ARQ-0026` autorizado con alcance completo.** Verificado contra código: `SimulatorScreen.tsx:58-60` confirma que `abono_extra` (no está en `SCENARIOS`) cae silenciosamente en `SCENARIOS[0]` ("¿Y si tomo un crédito?") — cualquier jugada de Salud/Presupuesto que apunte a simular un abono aterriza hoy en el escenario opuesto. `DebtsListScreen.tsx:191` confirma que la navegación a `estrategia_deudas` no precarga `extraBudget`, divergiendo del contrato `0` fijo del bloque de Deudas (`DEC-0022` §5.3). Decidido: el fix de `abono_extra` entra como P1 de máxima prioridad DENTRO de `ARQ-0026` (no como ajuste suelto — exige diseño, no una línea); coherencia con Deudas aprobada (precarga + explicación del contrato); los 3 escenarios faltantes del motor (`abono_extra`, `refinanciar`, `vender_activo`) entran completos — motor ya auditado (FIN-007), sin razón para dejar puertas cerradas. `ARQ-0026` autorizado.
- 2026-07-13 — **CTO: FIN-024 CERRADA**, tras verificación independiente en checkout aislado sobre `faebc2a` (ancestro de HEAD). Confirmé por lectura directa que `reminders.service.ts:119` evalúa contra `r.debt?.nextDueDate` (join) y que los únicos escritores de `nextDueDate` que quedan son creación y pago (FIN-018) — el cron no escribe ninguna fecha. Reejecuté las 3 suites: unit 326/326 (42 suites), e2e 23/23 (incl. `fin024-mora.e2e-spec.ts`), `tsc --noEmit` exit 0 — idénticas a las del Auditor. Quinta FIN consecutiva de la hoja de ruta cerrada con las 4 capas y 3 suites verificadas en vivo por dos roles independientes. Habilita la Experiencia de Simulador.
- 2026-07-13 — **Auditor: generado `docs/auditoria/VALIDACION-0024-Mora.md`. Veredicto: APROBADO.** Correspondencia DEC→IMP→Código→Evidencia en 4 capas sobre el commit `faebc2a` (ancestro de HEAD). **Cambio obligatorio §5 cumplido — ambas escrituras eliminadas:** `git grep` confirma que `reminders` ya no escribe `nextDueDate` (ni `debt.update` ni `reminder.dueDate: addOneMonth`); el update del dispatch es `{ lastSentAt }` a secas. El dispatch evalúa contra la fecha real de la deuda (query incluye `debt: true`; `const due = r.debtId ? r.debt.nextDueDate : r.dueDate`), `reminder.dueDate` documentado como legado; deuda saldada → no dispara; manuales intactos — el punto que el CTO pidió verificar. Escritores de `nextDueDate` tras el cambio: solo creación + pago FIN-018 (un dueño). `overdue.util.ts` función pura única (medianoche UTC), `overdueDays` aditivo al list/summary/detalle. Copy §29.2 verificado en captura: "⏰ Esta cuota venció hace 12 días" (naranja, no rojo), "No hay un pago registrado… regístrala para que tus números digan la verdad" (nunca "no pagaste") + CTA doble. Suites en vivo: unit 326/326 (42 suites), e2e 23/23 (7 suites). Score/Salud/SpendableService ausentes del diff. Impacto retroactivo de mora declarado, a mirar en RC. FIN-024 pasa a *Validada por el Auditor — en espera de verificación independiente y CIERRE del CTO*.
- 2026-07-13 — **CTO: `DEC-0024` emitido, `IMP-0024` habilitado.** Verificación independiente contra código: `debt-prepayment.service.ts:110` confirma la semántica FIN-018 (avanza solo al pagar, desde amortización real); `git grep` propio confirma que ningún módulo fuera de `reminders/` lee `reminder.dueDate` como autoritativo — Alt A segura. P1/P2/P3 aprobados. Cambio obligatorio principal (elevado de la observación 1 del Auditor): el `IMP` debe eliminar ambas escrituras de fecha (`debt.nextDueDate` y `reminder.dueDate`) para recordatorios de deuda, no solo la primera. Impacto retroactivo de mora (usuarias con fechas vencidas "de fábrica") aceptado como la verdad del dato, con seguimiento en la RC integral.
- 2026-07-13 — **Auditor: generado `docs/auditoria/AUD-0024-Mora.md`. Veredicto: APROBADO CON OBSERVACIONES.** Verificación contra `HEAD`. Los 3 puntos del Arquitecto se sostienen: **(1)** `git grep` confirma que nadie fuera del módulo `reminders` lee `reminder.dueDate` como autoritativo → volverlo legado para recordatorios de deuda (Alt A) no rompe consumidores; **(2)** `shouldFireToday = [3,1,0].includes(daysUntil(dueDate))` — con fecha vencida `daysUntil` es negativo → no dispara; el recordatorio calla tras vencer (comportamiento declarado, FIN-025 lo cubre), sin loop ni spam (`lastSentAt` dedup adicional); **(3)** `overdueDays` es campo nuevo, Inicio no lo consume → aditivo, seguro si el IMP no reforma los campos existentes. Doble escritor confirmado real: `reminders.service.ts:162-171` avanza `debt.nextDueDate` solo con `remaining <= 0`, sin comprobar pago — contra la semántica FIN-018 (`debt-prepayment.service.ts:110,148`). P1 lo deja como escritor único (creación + pago). §32 sobre una fecha, mismo patrón de fuente única de FIN-020/021/023. Observación clave para el IMP: eliminar **ambas** escrituras de fecha (`debt.nextDueDate` en :170 **y** `reminder.dueDate` en :164) para recordatorios con `debtId`; si solo se quita la primera, `reminder.dueDate` reintroduce la dualidad. Impacto retroactivo de mora grande al corregir P1 (deudas "vencidas de fábrica") = la verdad; mitigado por copy de conciliación, a mirar en la RC. FIN-024 pasa a *Auditada — en espera de DEC*.
- 2026-07-13 — **CTO: CPSAO decide P4 (aviso post-vencimiento) — fuera de FIN-024, registrado como FIN-025.** Pantalla pasiva primero, sin notificación proactiva: el CPSAO prefiere validar el tono de "venció el {fecha}" (ya en producción) antes de gastar presupuesto de notificación en algo que, si el copy falla, se lee como regaño y pega directo a Confianza. Exigió que el fast-follow quedara registrado con punto en el roadmap (`FIN-025`), no como mejora futura genérica — mismo trato que exigió para mora tres veces. `ARQ-0024` continúa con P1+P2+P3.
- 2026-07-13 — **CTO: bug fundacional confirmado en FIN-024 (comprensión evaluada) — dos escritores de `debt.nextDueDate`.** Verificado contra código: `reminders.service.ts:158-172` avanza la fecha de vencimiento de una deuda al llegar el día, sin comprobar pago — conflicto directo con la semántica de FIN-018 (avanza solo al pagar). Con el cron activo, la mora era estructuralmente indetectable. Decidido: P1 del `ARQ-0024` es dejar un solo escritor (gana FIN-018); iteración 1 de mora limitada a deudas (fijos se quedan con la etiqueta neutra existente, conciliación `fixedItemId` diferida); sin tocar Score/Salud (sin cortes auditados). Aviso post-vencimiento (notificación proactiva vs. solo pantalla) escalado al CPSAO por implicar tono/producto (`PRODUCT_VISION.md` §7, "calmada, no ansiosa"). `ARQ-0024` autorizado a iniciar. Detalle: `docs/correspondencia/FIN-024-Mora.md`.
- 2026-07-13 — **CTO: FIN-023 CERRADA**, tras verificación independiente en checkout aislado sobre `c7b9804` (ancestro de HEAD). Confirmé que `DebtOutlayModule` no importa nada (módulo hoja real) y que `payment-breakdown.util.ts` es la única función pura de la que delegan `DebtOutlayService` y `DebtInsuranceService`. Reejecuté las 3 suites: unit 318/318 (40 suites), e2e 20/20 (incl. `fin023-desembolso-real.e2e-spec.ts`), `tsc --noEmit` exit 0 — idénticas a las del Auditor. Cuarta FIN consecutiva de la hoja de ruta cerrada con las 4 capas y 3 suites verificadas en vivo por dos roles independientes. Habilita `FIN-024` o Simulador, según decida el CPSAO.
- 2026-07-13 — **Auditor: generado `docs/auditoria/VALIDACION-0023-Desembolso-Real-Deuda.md`. Veredicto: APROBADO.** Correspondencia DEC→IMP→Código→Evidencia en 4 capas sobre el commit `c7b9804` (ancestro de HEAD). **Dos desviaciones del ARQ escrutadas (foco del CTO):** (1) **empaquetado** — la fuente vive en `DebtOutlayModule` (módulo hoja sin imports, los 5 consumidores lo inyectan), no en `DebtInsuranceService`; es **una función pura única** (`payment-breakdown.util.ts`, `totalMonthlyOutlay = cuota + aparte`) en la que **ambos** `DebtOutlayService` y `DebtInsuranceService.paymentBreakdown` delegan — cero copias; resuelve el ciclo real Messaging→Debts→Reminders→Whatsapp→Messaging y de paso mi observación de acoplamiento de `AUD-0023` §2; superior al diseño original, misma conducta. (2) **emisión `debt.updated` en el CRUD de cargos** = consecuencia **necesaria** de §5.3 + la frescura ~25s aceptada: el Motor recomputa por evento (`engine.listener`), sin emitirlo la lectura persistida solo se actualizaría con el job nocturno y la garantía sería falsa; el co-listener `insights.generator` está alineado con `ARQ` §10. **4 cambios obligatorios cumplidos:** §5.1 `validateChargeSemantics` rechaza `endorsed`/`insurer` server-side (400, create+update); §5.2 sin default (grep limpio); §5.3 Recomendaciones lee `EssentialExpense` persistida (e2e fija que incluye el outlay); §5.4 comentario preciso sobre `available`. §32 en cascada visible en captura: "Desembolso real $127.099" = 97.199+29.900, hero "Con seguros y cargos $519.134". Suites en vivo: unit 318/318 (40 suites), e2e 20/20 (6 suites), migración limpia; regresión (sin cargos → cifras idénticas) fijada. FIN-023 pasa a *Validada por el Auditor — en espera de verificación independiente y CIERRE del CTO*.
- 2026-07-12 — **CTO: `DEC-0023` emitido, `IMP-0023` habilitado.** Verificación independiente contra código: los 3 consumidores adicionales propuestos (`budget.service.ts:105-132`, `context-assembler.ts:149`, `conversation.service.ts:171`) confirmados reales; `git grep` confirma que `debts.module.ts` no genera ciclo con Budget/Motor/Copiloto/Messaging. P4 (tocar el hero de FIN-022, ya cerrada) autorizado explícitamente — línea condicional de bajo riesgo. P5 (Copiloto/Messaging) incluido en el alcance — cierre completo del §32, no parcial. 4 cambios obligatorios: rechazo server-side de `endorsed` para `cuota_manejo`, cero default (grep de literales), orden Motor→Recomendaciones fijado con test, precisión de wording sobre el `available` del Copiloto.
- 2026-07-12 — **Auditor: generado `docs/auditoria/AUD-0023-Desembolso-Real-Deuda.md`. Veredicto: APROBADO CON OBSERVACIONES.** Verificación contra `HEAD`. Los 3 puntos del Arquitecto resuelven a favor: **(1) sin ciclos** — `git grep` confirma que nada en el subárbol de `DebtsModule` (Debts→Finance/Reminders/Simulations→Billing→Auth) reimporta Budget/Engine/Copilot/Messaging (los 4 nuevos importadores); **(2) frontera `minPayment`** — `simulation-engine.ts:200` usa la cuota como insumo de amortización; es requisito de correctitud (usar el outlay sobrestimaría la velocidad de pago), no fuga; **(3) sin recalibración** — cortes/textos operan sobre ratios/meses derivados del insumo, no sobre el crudo. Núcleo §32 correcto por construcción: `paymentBreakdown` (`debt-insurance.service.ts:85`) hace `totalMonthlyOutlay = cuota + aparte` — las primas **financiadas no se doble-cuentan**; inventario de 6 consumidores verificado; Recomendaciones se corrige sola vía `EssentialExpense` persistida (depende de que el Motor se corrija primero — fijar en IMP). P1: Alt A (extender enum) de mínimo radio; condiciones para el IMP: rechazar `endorsed` server-side para `cuota_manejo` y sin default. Observaciones: el `available` del context-assembler mejora como efecto colateral (precisar wording del ARQ §5); P4 (tocar FIN-022) y P5 (consumidores 5–6) son decisiones de alcance del CTO. FIN-023 pasa a *Auditada — en espera de DEC*.
- 2026-07-12 — **CTO: verificado el ajuste de zona horaria (commit `8bd4c67`), FIN-022 Aprobada en producto por el CPSAO.** Confirmé por `git show` que `formatDate()` ahora fija `timeZone:'UTC'` y que los 3 call-sites de instantes reales (`occurred_at`, picker de Registrar, vigencia Millo+) migraron a `formatLocalDate()`. `tsc --noEmit` reejecutado por el CTO, exit 0. Capturas confirman "vence 28 jul" idéntico en Inicio y Deudas. FIN-022 queda cerrada en ambos sentidos (técnico y producto).
- 2026-07-12 — **CTO: bug real de formato de fecha encontrado en revisión de producto del CPSAO (no es §32).** Misma tarjeta muestra "vence 28 jul" en Inicio y "vence 27 jul" en Deudas P3 — mismo campo `nextDueDate`, mismo valor crudo, pero `formatDate()` (`frontend/src/utils/format.ts:12`) no fija `timeZone:'UTC'` para fechas de solo-día, corriendo un día hacia atrás en Colombia (UTC-5); `shortDate()` sí lo hace bien. 9 usos afectados en el frontend. Instruido a Arquitectura como ajuste post-cierre de FIN-022 (sin reabrir su DEC ni su alcance). Detalle en `docs/correspondencia/FIN-022-Experiencia-de-Deudas.md`.
- 2026-07-12 — **CTO: FIN-022 CERRADA**, tras verificación independiente en checkout aislado sobre `0f75a5c` (ancestro de HEAD). Leí `attackOrder()` y confirmé que `pickTarget` (simulación) y `strategyOverview` (summary) llaman literalmente a la misma función — no puede divergir por construcción. Reejecuté las 3 suites: unit 313/313, e2e 15/15 (incl. `fin022-orden-ataque.e2e-spec.ts`), `tsc --noEmit` exit 0 — idénticas a las del Auditor. Tercera FIN consecutiva de la hoja de ruta cerrada con las 4 capas y 3 suites verificadas en vivo por dos roles independientes. Habilita abrir `FIN-023`.
- 2026-07-12 — **Auditor: generado `docs/auditoria/VALIDACION-0022-Experiencia-Deudas.md`. Veredicto: APROBADO.** Correspondencia DEC→IMP→Código→Evidencia en 4 capas sobre el commit `0f75a5c` (ancestro de HEAD). **Punto crítico del CTO (§5.1) confirmado por construcción:** `attackOrder()` es el único helper puro que define el orden (`portfolio.simulator.ts:120`), el `pickTarget` de la simulación lo consume (línea 136) y el `strategyOverview` del summary también (`simulations.service.ts`) — misma función, no "coincide hoy"; e2e `fin022-orden-ataque` asevera `sim.recommended==summary.recommended` e `interestDifference` cercano contra BD real. §5.2 copy correcto ("avalancha vs bola de nieve", manejo `≥1000`, sin "$0"/"a ciegas"); §5.3 `compareStrategies(portfolio, 0)` fijo; §5.4 doble gate `>1`. §32: hero "$11.059.801" idéntico a "Deuda total" de Inicio (mismo summary), cuotas "suman" (frontera FIN-023), DTI no duplicado. Suites en vivo: unit 313/313 (39 suites), e2e 15/15 (5 suites). Caso `≈0` de la demo mostrado honesto ("ambos órdenes cuestan casi lo mismo"). Reservas del IMP honestas (fecha de libertad del contrato, no de la sim). FIN-022 pasa a *Validada por el Auditor — en espera de verificación independiente y CIERRE del CTO*. Sigue `FIN-023` según secuencia del CPSAO.
- 2026-07-12 — **CTO: `DEC-0022` emitido, `IMP-0022` habilitado.** Verificación independiente contra código (`simulation-engine.ts:203-208`, `recommendations.service.ts:90`, `debts.service.ts:195`): los 3 puntos del AUD-0022 se sostienen exactos. P1/P3/P4/P5 aprobados sin condiciones. P2 (orden de ataque, corazón de la FIN) aprobado con 4 cambios obligatorios: (1) orden de ataque desde helper puro compartido, nunca re-derivado en el summary — mismo criterio que `DEC-0021` §5.1; (2) copy de ahorro corregido; (3) contrato de `extraBudget` fijado; (4) simulación gateada a `debtsCount>1`. Corregido también un gap propio: `VALIDACION-0021-Fondo-de-Emergencia.md` había quedado sin commitear desde su emisión — regularizado en este mismo commit (autoaplicación de §34).
- 2026-07-12 — **Auditor: generado `docs/auditoria/AUD-0022-Experiencia-Deudas.md`. Veredicto: APROBADO CON OBSERVACIONES.** Verificación contra `HEAD`. Los 3 puntos que el Arquitecto pidió: (1) `estrategia_deudas` con `extraBudget:0` **es válido** — `portfolio.simulator.ts` usa `constantBudget = extra + Σ minPayment` y avalancha/bola de nieve divergen por el roll-over al saldarse la primera deuda, aun con extra 0; (2) latencia **negligible** (aritmética en memoria, tope 1200 meses), matiz: `/debts/summary` está también en la ruta de Inicio (tarjeta "Deuda total"), gatear la sim a `debtsCount>1`; (3) **§32 del ranking = vacío real:** el motor NO expone orden de ataque (`simulation-engine.ts:205-208` solo propaga months/interest/recommended/interestDifference; `payoffOrder` no se propaga y es orden de saldadas, no de ataque) — el summary tendría que re-derivarlo ("coincide hoy", no "no puede divergir") → cambio obligatorio recomendado para el DEC, análogo a `DEC-0021` §5.1; y el resultado depende de `extraBudget` (Recomendaciones usa `surplus*0.3`, Simulador usa input) — la cifra de ahorro puede diverger del Simulador aunque el orden no. Observación crítica adicional: "te ahorra {interestDifference} frente a pagar a ciegas" — la cifra es avalancha-vs-bola de nieve, no vs "sin estrategia"; corregir copy y manejar `interestDifference≈0`. Fuentes §32 correctas: `totalDebt` de `summaryForUser` (mismo filtro `status:'activa'` que Inicio), cuotas etiquetadas "tus cuotas suman" (frontera FIN-023), DTI no duplicado. FIN-022 pasa a *Auditada — en espera de DEC*.
- 2026-07-12 — **CTO: decisiones del CPSAO registradas — `FIN-023` y `FIN-024` incorporadas al Backlog.** CPSAO aprobó mora diferida por 3ª vez con condición explícita de no dejarla en el limbo, y confirmó FIN dedicada para el hallazgo del desembolso real con prioridad inmediata tras `FIN-022` (mismo criterio que `FIN-021`). El CTO decidió la posición exacta pedida por el CPSAO: `FIN-023` (desembolso real, inmediata) → `FIN-024` (mora, mismo dominio de deuda/compromisos, antes de saltar a Simulador). `ARQ-0022` sigue en curso sin cambios de alcance.
- 2026-07-12 — **CTO: `COMPRENSION-FIN022-Deudas.md` evaluado, `ARQ-0022` autorizado.** Verifiqué el hallazgo §32 nuevo del Arquitecto: `debt-insurance.spec.ts:29-33` confirma `totalMonthlyOutlay`=545.000 (cuota 500.000+seguro aparte 45.000); `spendable.service.ts:109` y `engine.service.ts:53` (alimenta DTI y, vía `essential`, el fondo de emergencia de FIN-021) usan `monthlyPayment` a secas (500.000) — tercer concepto que subestima lo comprometido para quien paga seguros aparte, tocando FIN-004/020/021 ya cerradas. Aprobado "conectar, no inventar" (arquitectura). Pendiente decisión del CPSAO: mora en FIN-022 o diferida (tercera vez); secuencia del hallazgo del desembolso real (propuesto: FIN propia posterior, no arreglo silencioso). `ARQ-0022` autorizado a iniciar en paralelo con alcance por defecto (mora diferida). Detalle en `docs/correspondencia/FIN-022-Experiencia-de-Deudas.md`.
- 2026-07-12 — **CTO: FIN-021 CERRADA**, tras verificación independiente en checkout aislado sobre `6ee4d9d` (ancestro de HEAD). Leí el código del commit (`emergency-fund.constants.ts`, `recommendations.service.ts`) y confirmé por grep que `emergencyBalance` ya no se usa en Recomendaciones (solo queda en `core-metrics.ts`, `simulation-engine.ts` y specs — consumidores legítimos de la fórmula canónica). Reejecuté las 3 suites yo mismo: unit 310/310 (39 suites), `tsc --noEmit` exit 0, e2e 12/12 con Postgres real (incl. `fin021-fondo-unico.e2e-spec.ts`) — idénticos a los del Auditor. **FIN-021 — Única definición del fondo de emergencia: CERRADA.** Segunda FIN de la hoja de ruta consecutiva cerrada con las 4 capas y 3 suites verificadas en vivo por dos roles independientes. Habilita abrir la Experiencia de Deudas.
- 2026-07-12 — **Auditor: generado `docs/auditoria/VALIDACION-0021-Fondo-de-Emergencia.md`. Veredicto: APROBADO.** Correspondencia DEC→IMP→Código→Evidencia en 4 capas sobre el commit `6ee4d9d` (ancestro de HEAD). **Cambio obligatorio `DEC-0021` §5.1 cumplido por construcción:** `recommendations.service.ts` importa `nextMilestone` de las constantes oficiales y lee `EmergencyFundMonths`/`EssentialExpense` persistidos (`readMonthMetrics`), ya no recalcula ni usa `state.emergencyBalance`; grep confirma que la fórmula `emergencyBalance / essential` vive una sola vez (`core-metrics.ts:61`). Inicio: `interpretSavings` eliminada, `interpretEmergencyFund` lee la métrica persistida; glosario importa la constante; cero literales `3/6` sueltos (§5.2). Cortes de Salud/Score intactos (diff no toca `health.service`/`score.util`). Suites en vivo: unit 310/310 (39 suites), e2e 12/12 (4 suites, incl. `fin021-fondo-unico` con igualdad Inicio==Motor==Salud contra BD real). Prueba visual §32: Inicio "~1.3 meses / colchón inicial son 3" y Salud "Fondo de emergencia: 1.3 meses" — mismo dato, mismo hito nombrado en la jugada de Salud y Presupuesto; la contradicción "~4 vs 3" ya no es reproducible. Observación menor no bloqueante: los literales de meta en textos de Salud (fuera de alcance) son plegables a la constante en un ciclo futuro. FIN-021 pasa a *Validado por el Auditor — en espera de verificación independiente y CIERRE del CTO*.
- 2026-07-12 — **CTO: `DEC-0021` emitido.** Verificación independiente contra `125c5c6`/`77ddffd`: fórmula canónica, 9 consumidores y frescura ~25s confirmados exactos. P1 Alt A, P2 Alt A (frescura aceptada explícitamente), P3 **Alt C** (decisión del CPSAO en `docs/correspondencia/FIN-021-Fondo-de-Emergencia.md`: dos hitos nombrados, colchón inicial 3 / fondo completo 6) aprobados. Cambio obligatorio: el `IMP` debe hacer que `recommendations.service.ts` consuma la fuente/constantes oficiales, no solo alinear la meta (evita que §32 se reabra por recálculo). `IMP-0021` habilitado.
- 2026-07-12 — **Auditor: generado `docs/auditoria/AUD-0021-Fondo-de-Emergencia.md`. Veredicto: APROBADO CON OBSERVACIONES.** Verificación contra código en `125c5c6`: inventario §2 del ARQ confirmado (fórmula canónica `EmergencyFundMonths = fondo marcado / esencial` en `core-metrics.ts:48,61`; 6 consumidores leen la lectura persistida; solo Inicio y Recomendaciones divergen; grep amplio no halló un 4º cálculo independiente oculto — inventario de 9 completo). Riesgo de frescura de P2 Alt A cuantificado por encargo del CTO/Arquitecto: OutboxDispatcher `EVERY_10_SECONDS` + drain `@Interval(DEBOUNCE_MS=15s)` ⇒ **≈25 s peor caso** (no 24 h; el cron nocturno es solo backstop), aceptable para un concepto que cambia lento. **Concedido y retirado mi matiz intra-servicio de FIN-020** (`recommendations.service.ts` línea 108 define `essential` idéntico al disparo de la 106 — no había divergencia; mi grep original omitió la línea 108). Observaciones no bloqueantes: aceptar explícitamente el límite de frescura en el DEC; que el IMP haga que Recomendaciones consuma la fuente/constantes oficiales (no solo la meta); vigilar percepción de pérdida en Inicio. P3 (meta única) es decisión de producto del CPSAO antes del DEC — correctamente diferida por el Arquitecto. FIN-021 pasa a *Auditado — en espera de revisión CPSAO de P3 y DEC*.
- 2026-07-12 — **CTO: FIN-020 Aprobada en producto + FIN-021 abierta.** Verificación independiente del CTO contra el commit `614ef81c9e646e85fe468e0e0e7508ec5b6ad8ad` (ancestro de HEAD): los 2 ajustes menores de Arquitectura coinciden exactamente con lo declarado (`ProtectedTimeline` con la política §4.1-bis visible, condicional a que exista algún compromiso vencido; fecha de vencimiento en cada cuota de P6) — `tsc --noEmit` reejecutado por el CTO, exit 0. El CPSAO confirmó y declaró FIN-020 Aprobada en el sentido de producto, tratando el hallazgo del fondo de emergencia como derivado y anterior, no como condición de cierre. **Corrección del CTO al Auditor:** su hallazgo adicional de "divergencia intra-servicio" en `recommendations.service.ts` (línea 106 vs 109) no se sostiene — verificado por grep, ambas líneas usan la fórmula idéntica `state.fixedExpense + state.debts.reduce(...)`. El hallazgo principal del Auditor (3 fórmulas divergentes entre servicios) sí se sostiene y fue confirmado independientemente por el CTO. **FIN-021 — Única definición del fondo de emergencia (§32) — abierta**, deuda técnica de prioridad inmediata por decisión del CPSAO, antes que Deudas. Arquitectura autorizada a iniciar `ARQ-0021`. Detalle en `docs/correspondencia/FIN-020-Experiencia-de-Presupuesto.md` (cierre) y `docs/correspondencia/FIN-021-Fondo-de-Emergencia.md` (apertura).
- 2026-07-12 — **CTO: hallazgo nuevo tras revisión de producto del CPSAO sobre FIN-020 — tres fórmulas divergentes de "meses de fondo de emergencia" (violación real de §32).** El CPSAO devolvió FIN-020 en estado "Ajustes" (no Aprobada, en el sentido de producto — la FIN sigue CERRADA técnicamente) señalando una posible inconsistencia entre el "~4 meses cubiertos" de Inicio y el "3 meses en 4 meses" de la recomendación de fondo de emergencia en Presupuesto. Verifiqué contra el código: `dashboard.service.ts` (`interpretSavings`) usa `(ahorro total + fondo emergencia) / gastos fijos`; `health.service.ts` (indicador Fondo de emergencia de Salud) usa `fondo de emergencia SOLO / gasto esencial` con meta de 6 meses; `recommendations.service.ts` (motor FIN-007, reutilizado en la jugada de Salud y ahora en P5 de Presupuesto) usa objetivo `(gastos fijos + cuotas de deuda) × 3` contra el fondo solo. **Tres fórmulas, tres bases, dos metas — para lo que el usuario percibe como un solo concepto.** Preexistente desde FIN-004/007/019, pero FIN-020 lo hizo visible en la misma sesión de usuario. Viola `GOBERNANZA.md` §32 (redactado para "ningún concepto financiero", no solo "Te queda"). Registrado en `docs/correspondencia/FIN-020-Experiencia-de-Presupuesto.md`. Propuesto al CPSAO abrir una `FIN` dedicada ("Única definición del fondo de emergencia") en vez de parchear — toca 3 servicios y 2 FIN ya cerradas, fuera del alcance de una Acción Correctiva (el `DEC-0020` no registró esta observación). Dos hallazgos menores adicionales (copy sin "por qué" en "Protegido para lo que viene"; "Crédito libre inversión" sin fecha en la casa de compromisos, preexistente) se tratan como ajustes menores sin reabrir FIN-020 técnicamente. Pendiente respuesta del CPSAO sobre secuenciación (prioridad inmediata como deuda técnica §27, o compite en Backlog con Deudas).
- 2026-07-12 — **CTO: convención de correspondencia adoptada** (`docs/correspondencia/*`, un archivo por asunto, historial acumulativo append-only), propuesta por el CPSAO. Corregido: el archivo de ejemplo que el CPSAO citó como existente no estaba en el repo en el momento de verificarlo (`Glob` vacío); apareció con contenido real momentos después (relay del Fundador). Registrado en `AI_REGISTRY.md` como documento que CPSAO/CTO/Arquitecto/Auditor pueden generar. Pendiente ratificación formal en `GOBERNANZA.md` (mismo estado que `PROCEDIMIENTO-ARRANQUE-EN-FRIO.md`) y confirmación del mecanismo real de escritura del CPSAO.
- 2026-07-12 — **CTO: documentación oficial regularizada en git + nueva regla permanente `GOBERNANZA.md` §34.** El Fundador autorizó ("Si, autorizo comittear y que esto no vuelva a ocurrir") commitear el backlog completo hallado durante el checkout aislado de FIN-020. Ejecuté 7 commits temáticos: `fd63e51` (Gobernanza v2.0→v3.11 + 9 archivos de `docs/archive/`), `95e268c` (PRODUCT_VISION/PRODUCT_DECISIONS/AI_REGISTRY/LAB/IDEA-0002/informes), `6a96bfc` (Programa Alpha completo ALPHA-001…008 + RC-0001), `f55a62f` (AUD/DEC/VALIDACIÓN de FIN-017/018/019), `af5ccef` (AUD/DEC/VALIDACIÓN de FIN-020), `85bff76` (PROCEDIMIENTO-ARRANQUE-EN-FRIO, PROMPT-MAESTRO-CTO, PROPUESTA-Reorganización, PHR_REGISTRY). Añadí `GOBERNANZA.md` §34 (v3.12): toda documentación oficial se commitea en el mismo acto de su creación/modificación — extiende a todo el dominio documental la disciplina que ya regía solo para `IMP`. Verifiqué `git status` limpio tras el catch-up (solo quedan `ESTADO_PROYECTO.md`/`BACKLOG.md` de este mismo commit final). Riesgo cerrado.
- 2026-07-12 — **CTO: FIN-020 CERRADA**, tras verificación independiente en checkout aislado sobre el commit `125c5c6` (ancestro de HEAD confirmado por `git merge-base --is-ancestor`). Leí directamente (`git show 125c5c6:<path>`, no el working tree) `spendable.service.ts`, `budget.module.ts`, `dashboard.module.ts`, `budget.service.ts`, `dashboard.service.ts`: confirmé que `BudgetModule` exporta `SpendableService`, `DashboardModule` importa `BudgetModule`, y ambos servicios llaman `this.spendable.compute(userId)` — misma instancia, imposible divergir por construcción (§32). §4.1-bis (política ii, fijos comprometidos hasta el cierre del ciclo) y §4.1-ter (textos recalibrados, rojo sin culpa) confirmados carácter a carácter contra el código del commit. **Reejecuté las 3 suites yo mismo, de forma independiente:** `npx jest` → 303/303 (38 suites); `npx tsc --noEmit` (frontend) → exit 0 sin errores; `npm run test:e2e` con Postgres real (Docker) → 9/9 (3 suites, incl. `fin020-tequeda.e2e-spec.ts`) — resultados idénticos a los declarados por Arquitectura y verificados por el Auditor. Correspondencia exacta DEC→IMP→Código→Evidencia confirmada en las 4 capas por dos roles independientes. Observación de `AUD-0020`/`VALIDACION-0020` sobre `context-assembler.ts` aceptada como restricción de diseño registrada para la futura Experiencia de Copiloto (no bloqueante, fuera de alcance de FIN-020). **FIN-020 — Experiencia de Presupuesto: CERRADA.** Habilita abrir la Experiencia de Deudas cuando el CTO/CPSAO lo decidan.
- 2026-07-12 — **CTO: hallazgo crítico de gobernanza durante el checkout aislado de FIN-020 — documentación oficial sin commitear.** `git status` reveló que `docs/GOBERNANZA.md` no se commitea desde el `44fdefb` (2026-07-05, ciclo FIN-012) — **426 líneas insertadas sin commitear**, es decir toda la Parte II (Gobernanza Estratégica del Producto, §10-16), la incorporación del CPSAO/AI_REGISTRY (§22-23), el EOC v1.0 (§33) y el resto de reglas §24-32 existen SOLO en el working tree, nunca en el historial de git. Además, `ESTADO_PROYECTO.md`, `DEC-0018/0019/0020`, `AUD-0018/0019/0020`, `VALIDACION-0019/0020`, `PROCEDIMIENTO-ARRANQUE-EN-FRIO.md`, `PROMPT-MAESTRO-CTO.md`, todo `docs/producto/alpha/` y `docs/producto/rc/`, y varios `docs/archive/GOBERNANZA-v3.X.md` aparecen como `??` (nunca trackeados). Contradice directamente la regla "el estado oficial se determina solo por artefactos verificables: commits registrados en el repositorio..." — el riesgo es real: sin commit, este trabajo no tiene historial de git, no es auditable por `git log`/`git blame`, y se pierde si el working tree se daña. **No bloquea el cierre de FIN-020** (la verificación técnica de esta FIN fue completa e independiente, contra un commit real). **Sí requiere decisión del Fundador**: autorizar al CTO a commitear el backlog completo (yo no commiteo sin autorización explícita, por instrucción operativa vigente). Registrado en `ESTADO_PROYECTO.md` (Riesgos abiertos + Decisiones del Fundador pendientes).
- 2026-07-12 — **Auditor: generado `docs/auditoria/VALIDACION-0020-Experiencia-Presupuesto.md`. Veredicto: APROBADO.** Verificación en 4 capas DEC→IMP→Código→Evidencia sobre el commit inmutable `125c5c6` (ancestro de HEAD confirmado): las dos correcciones de P1 que motivaron el REQUIERE AJUSTES de `AUD-0020` están resueltas en código y documentadas en `ARQ-0020` v1.1 (§4.1-bis política ii "comprometido hasta el cierre del ciclo, solo sesga hacia abajo"; §4.1-ter ruta b con textos recalibrados, rojo sin culpa). §32 garantizado por construcción: `SpendableService` único inyectado por Budget e Inicio, `grep` confirma que `teQueda` solo nace en `spendable.service.ts`, y el mismo `$1.795.602` aparece en el hero de Inicio y de Presupuesto (prueba visual + aritmética cruzada exacta). **Suites ejecutadas EN VIVO** (primera VALIDACIÓN de la hoja de ruta UX sin recurrir a la excepción de entorno): unit 303/303 (38 suites), e2e 9/9 (3 suites, incl. `fin020-tequeda` con igualdad estructural `toEqual` contra Postgres real), `tsc --noEmit` frontend exit 0. Única observación no bloqueante y fuera de alcance: `copilot/context-assembler.ts:170` mantiene un `available` estructural para el contexto del LLM — deberá consumir `SpendableService` cuando se diseñe la Experiencia de Copiloto (hoja de ruta #6), registrada para el CTO. FIN-020 pasa a *Validado por el Auditor — en espera de verificación independiente y CIERRE del CTO*.
- 2026-07-12 — **CTO: P1 de `ARQ-0020` confirmada (`DEC-0020` §8).** Verificación independiente contra el commit `c51030c` (HEAD): política de fijos vencidos (ii, comprometido hasta el cierre del ciclo) y rejustificación del umbral de `interpretCashflow` (ruta b, con textos recalibrados) resuelven ambos hallazgos de `AUD-0020` sin tocar código de producto (solo diseño). `IMP-0020` habilitado. Verificado además el volcado de conocimiento del Arquitecto (`frontend/scripts/captura/`, `docs/NOTAS-OPERATIVAS-ARQUITECTO.md`) — sustantivo y correctamente subordinado a la documentación oficial.
- 2026-07-12 — **Auditor: generado `docs/auditoria/AUD-0020-Experiencia-Presupuesto.md`.** Veredicto: **REQUIERE AJUSTES** (acotado a P1). Verificación directa contra código: `schema.prisma` confirma que `Transaction` no tiene campo de vínculo a `FixedItem` (sin `fixedItemId`) — la fórmula de "compromisos pendientes" de Alt A puede dejar fuera de ambos términos un fijo vencido sin transacción registrada, sobreestimando "Te queda" pese a que Alt A se justificó por "nunca mentir hacia arriba". Segundo hallazgo: `dashboard.service.ts:170` (`interpretCashflow`) usa un corte del 10% calibrado contra el valor actual (equivalente a Alt C de la propia tabla del ARQ) sin recalibrarse para el nuevo valor, sistemáticamente menor, de Alt A. El resto de piezas (P2 fuente única, P3, P4, P5, P6, filtro §31) quedan listas para DEC sin condiciones. FIN-020 pasa a *Auditado — en espera de ajustes de Arquitectura antes de DEC-020*.
- 2026-07-12 — **Auditor: corrección a `VALIDACION-0019-Experiencia-Salud.md` §4/§7/§8.** El CTO notificó que la observación menor abierta (frase residual de `ARQ-0019` §10) ya no aplicaba. Verifiqué de forma independiente (lectura directa de `ARQ-0019` líneas 206-210, sin confiar solo en el mensaje) y confirmé que la frase efectivamente dice "el semáforo vive en los INDICADORES... frase corregida en v1.1". Actualicé mi documento retirando la observación como resuelta — sin cambio de veredicto (sigue APROBADO). Corrección de vigencia (el documento de arquitectura se corrigió después de mi lectura original), no de método.
- 2026-07-12 — **Auditor: `VALIDACION-0019-Experiencia-Salud.md` — APROBADO.** Verificación en 4 capas (DEC-0019→IMP-0019→código del commit `e865640`→capturas `despues-salud-01-scroll-completo.png`/`despues-salud-02-coldstart.png`): `git show --stat` confirma que el commit toca únicamente `ARQ-0019` y `HealthScreen.tsx` (cero backend real, no solo declarado); las 7 piezas (P1-P7) corresponden exactamente con lo aprobado en DEC-0019 (+adendo §8); grep confirma cero "v1" visible y cero porcentajes crudos; grep de `recommendations.service.ts` confirma que `SIM_BY_KIND` cubre 3 de los 4 `kind` reales, tal como el IMP declara honestamente. Las 6 preguntas UX + test emocional (criterio 6, Gobernanza §28-29) no arrojan hallazgos nuevos: el usuario sale orientado, no calificado. Única observación menor abierta (no bloqueante, heredada de DEC-0019 §8): la frase residual de `ARQ-0019` §10 no fue corregida en `IMP-0019`. Recomendado proceder al cierre formal de FIN-019 por el CTO.
- 2026-07-04 — Alta de FIN-001…FIN-009. Generado `ARQ-0001-Inteligencia-Financiera`. FIN-001 pasa a *En auditoría*.
- 2026-07-04 — Auditor: generado `docs/auditoria/AUD-0001-Inteligencia-Financiera.md`. Veredicto: **REQUIERE AJUSTES** (premisa Redis/BullMQ incorrecta; falta consentimiento/minimización para LLM; encuadre legal pendiente de bloquear Fase 2-3). FIN-001 pasa a *Auditado — en espera de DEC*.
- 2026-07-04 — CTO: generado `docs/oficial/DEC-0001-Inteligencia-Financiera.md`. **APROBADO CON AJUSTES.** Se rechaza la premisa Redis/BullMQ; se fijan 10 cambios obligatorios vinculantes para FIN-002 en adelante (outbox, cold-start, consentimiento/minimización LLM, encuadre legal, monetización adelantada, hito de 3 indicadores, unificación de carpeta `docs/auditoria/`). Se autoriza iniciar `ARQ-0002-Fundaciones-de-Datos.md` (FIN-002). FIN-001 pasa a *Decidido — condicionado*.
- 2026-07-04 — Arquitectura: generado `docs/arquitectura/ARQ-0002-Fundaciones-de-Datos.md` (FIN-002), incorporando los cambios obligatorios de DEC-0001 (§10.1 outbox+cron sobre PostgreSQL en vez de Redis/BullMQ, §10.2 outbox en alcance, §10.3 clasificación de eventos, §10.5 decisión de cifrado/rate limiting; sin pgvector/RAG). FIN-002 pasa a *En auditoría*.
- 2026-07-04 — Auditor: generado `docs/auditoria/AUD-0002-Fundaciones-de-Datos.md`. Verificó cumplimiento de los cambios obligatorios de DEC-0001 aplicables (#1,#2,#3,#5,soporte #4): todos cumplidos. Veredicto: **APROBADO CON OBSERVACIONES** (concurrencia del OutboxDispatcher sin claim atómico especificado; purga de outbox no definida; inconsistencia menor en clasificación de `AccountBalanceUpdated`). FIN-002 pasa a *Auditado — en espera de DEC*.
- 2026-07-04 — CTO: generado `docs/oficial/DEC-0002-Fundaciones-de-Datos.md`. **APROBADO CON AJUSTES.** Se ratifica outbox sobre PostgreSQL (sin Redis/BullMQ) y se fijan 4 cambios obligatorios: (1) claim atómico en `OutboxDispatcher` (`UPDATE...RETURNING`/`SKIP LOCKED`), (2) reclasificar `AccountBalanceUpdated`/`AssetChanged` como asíncronos (patrimonio ya se resuelve on-read), (3) política de purga de `OutboxEvent.processed` a 30 días, (4) regla de escritura automática y validación de `AccountBalanceEntry`. Se autoriza iniciar implementación de FIN-002. FIN-002 pasa a *Decidido — autorizado a implementación*.
- 2026-07-04 — Desarrollador: implementado FIN-002 y generado `docs/implementaciones/IMP-0002-Fundaciones-de-Datos.md`. Modelos `Account`/`Asset`/`AccountBalanceEntry`/`OutboxEvent`/`FinancialSnapshot`/`MetricReading` + `Transaction.accountId`; outbox con claim atómico + purga; patrimonio on-read; rate limiting; pantalla móvil de Cuentas/Patrimonio. Los 4 cambios obligatorios de DEC-0002 §10 cumplidos. 129/129 tests verdes; end-to-end y bundle Android OK. Limitación declarada: partición física de `MetricReading` diferida a FIN-003. FIN-002 pasa a *Implementado — a la espera de validación del CTO*.
- 2026-07-04 — Desarrollador: **reenvío de IMP-0002 (v2).** Diagnóstico del rechazo: el trabajo estaba sin commitear (HEAD en `3aadc15`) y la validación leyó el working tree en estado parcial/concurrente (se halló `index.lock` huérfano). Corrección: verificación re-ejecutada (tsc backend/frontend exit 0; 129/129 tests) y código fijado en commits `6adfc2d` (features app), `622bfa1` (FIN-002) y `0058621` (docs gobernanza); integridad confirmada sobre el contenido commiteado (`git show`). FIN-002 pasa a *Reenviado — a la espera de re-validación del CTO*.
- 2026-07-04 — CTO: **VALIDACIÓN DE IMP-0002 RECHAZADA.** Verificación directa del repositorio (no solo del informe) encontró: (1) `backend/prisma/schema.prisma` termina en el comentario `// ---------- FIN-002 · Cuentas, activos, patrimonio ----------` sin ningún modelo nuevo (`Account`/`Asset`/`AccountBalanceEntry`/`OutboxEvent`/`FinancialSnapshot`/`MetricReading` ausentes, pese a que la migración `20260704232531_fin002_fundaciones_datos` sí los crea en SQL); (2) `src/app.module.ts`, `src/modules/debts/debts.service.ts` y `src/modules/budget/budget.service.ts` están truncados a mitad de sentencia (archivos cortados, no código con bug); (3) `npx tsc --noEmit` falla con errores de sintaxis en esos mismos archivos. El reporte de IMP-0002 ("typecheck ✅", "129/129 tests verdes") no es reproducible contra el estado actual del repositorio. **No se autoriza el cierre de FIN-002.** Se exige reenvío de IMP-0002 con el código completo y verificación reproducible antes de re-someter a validación. FIN-002 pasa a *IMP rechazado — requiere reenvío*.
- 2026-07-04 — CTO: **VALIDACIÓN DE IMP-0002 v2 APROBADA — FIN-002 CERRADO.** Re-verificación independiente en checkout aislado (`git clone` + `git checkout 0058621`, no el working tree): (1) `git show 622bfa1:backend/prisma/schema.prisma` contiene los 6 modelos completos (`Account`, `Asset`, `AccountBalanceEntry`, `OutboxEvent`, `FinancialSnapshot`, `MetricReading`) y cierra correctamente; (2) `app.module.ts`, `debts.service.ts` y `budget.service.ts` están completos, sin truncamiento, en el commit; (3) `npx jest` → **127/129 tests pasan**; los 2 restantes (`outbox.service.spec.ts`) fallan a typecheck únicamente porque `npx prisma generate` no pudo descargar el engine de Prisma en este sandbox (`403 Forbidden` a `binaries.prisma.sh`, fuera de la red permitida aquí) — no por defecto de código; el schema fuente ya define correctamente los tipos que el cliente generado reclama. **Se acepta el cierre de FIN-002** dado que la causa raíz del rechazo anterior (código sin commitear) está resuelta y verificada contra una referencia inmutable (commit `622bfa1`), y no se encontró ningún defecto atribuible al código o al diseño. Se añade regla permanente a `docs/GOBERNANZA.md` ("Referencia inmutable obligatoria para todo IMP"): todo IMP debe declarar el SHA de commit exacto; no se valida contra working tree sin commitear. **Se autoriza iniciar `ARQ-0003` (FIN-003, Motor Financiero MVP).**
- 2026-07-04 — Arquitectura: generado `docs/arquitectura/ARQ-0003-Motor-Financiero.md` (FIN-003). Alcance: consumidores de eventos idempotentes, 7 métricas core con contrato `metricKey`, snapshot diario, tendencias/anomalías con cold-start ≥60 días (DEC-0001 §10.4), partición/retención de `metric_readings` (limitación IMP-0002). Excluye Score/indicadores (FIN-004), predicciones, IA/pgvector y monetización. Sin frontend ni dependencias nuevas. FIN-003 pasa a *En auditoría*.
- 2026-07-06 — **CTO: adopción oficial de Gobernanza v3.0 — Gestión Estratégica del Producto.** Por autorización final del Fundador (revisión con 8 ajustes menores), se ejecutan las 4 acciones autorizadas: (1) `docs/GOBERNANZA.md` reescrito íntegramente a v3.0 — Parte I (Gobernanza del Desarrollo, ARQ→AUD→DEC→IMP→Validación→Cerrado sin cambios de fondo), Parte II (Gobernanza Estratégica del Producto: CPO se expande a CPSAO — Chief Product, Strategy & AI Officer, sin autoridad jerárquica sobre el CTO; flujo Idea→PRODUCT LAB→Evaluación del CTO→Blueprint→Backlog; nota obligatoria de límites de IA en toda idea del CPSAO; protección del embudo del Backlog; cadencia de revisión del laboratorio; regla de verificación de hechos del CPSAO — todo dato de mercado/competencia se trata como hipótesis no verificada hasta confirmación), Parte III (Principios transversales: Independencia de Roles — ningún agente ejecuta dos etapas consecutivas del mismo proceso de decisión; Trazabilidad completa/referencias cruzadas obligatorias IDEA→BP→FIN→ARQ→AUD→DEC→IMP→VALIDACIÓN→CERRADO; Versionado documental obligatorio en todo documento estratégico; Archivo histórico — nunca se elimina documentación oficial; Revisión anual de gobernanza); sección final de Reglas permanentes acumuladas preservada íntegra (CPSAO sustituye a CPO donde aplica, numeración `IDEA-XXXX` añadida). (2) La versión 2.0 anterior fue archivada íntegra en `docs/archive/GOBERNANZA-v2.0.md`, conforme a la nueva regla de archivo histórico. (3) Creado el scaffold completo de `docs/producto/`: `PRODUCT_VISION.md`, `COMPETITIVE_ANALYSIS.md`, `MONETIZATION.md`, `USER_RESEARCH.md`, `PRODUCT_DECISIONS.md` (poblado retroactivamente con 5 decisiones estratégicas ya tomadas en el roadmap técnico), `METRICS.md` (plantilla de 12 indicadores, sin datos reales todavía) y `lab/LAB.md` (tabla maestra vacía del Laboratorio de Producto). (4) Creado `docs/archive/README.md` documentando la convención de archivo. **Estado operativo confirmado sin cambios: FIN-012 continúa siendo la única funcionalidad activa** (entregada, commit `9ea01d6`, en validación del CTO); ningún otro FIN puede iniciar diseño hasta su cierre, salvo excepción expresa y documentada del CTO.
- 2026-07-06 — **CPSAO/CTO: `PRODUCT_VISION.md` oficializada como v1.2.** Ciclo completo: CPSAO propuso estructura de 12 secciones → CTO redactó v1.1 separando hechos documentados de propuesta abierta → CPSAO solicitó 7 ajustes de identidad/tono (alcance internacional futuro, sección "Personalidad de Milla", Visión aspiracional, "¿Por qué existe Milla?", "La obsesión de Milla", promesa verificable, "Manifiesto de Milla") → CTO produjo v1.2 → CPSAO aprobó conceptualmente y recomendó oficializar → Fundador ratificó expresamente v1.2 como Constitución del Producto Milla, vigente, con prioridad sobre propuestas funcionales futuras. Ninguna decisión de gobernanza, Backlog ni arquitectura fue tocada por este ciclo. En la misma recomendación, el CPSAO registró `IDEA-0001` (Evolución de PRODUCT_VISION hacia la Constitución Cultural de Milla) en `docs/producto/lab/LAB.md` — estado `en laboratorio`, sin Blueprint, sin efecto en el Backlog. **Se reconfirma: FIN-012 sigue siendo la única funcionalidad activa**, sin alteración de su ciclo de implementación/validación/cierre.
- 2026-07-06 — **CPSAO/CTO/Fundador: adopción de Gobernanza v3.1 — Incorporación de agentes de IA + `AI_REGISTRY.md`.** El CPSAO propuso un flujo de 6 pasos para incorporar nuevos agentes estratégicos (propuesta CPSAO → evaluación CTO → Prompt Maestro CPSAO → creación Fundador → integración documental Arquitecto → alta oficial) y un registro permanente de agentes. El CTO evaluó y añadió 3 refinamientos: revisión del Auditor sobre acceso a datos/privacidad (aplicando la regla permanente de vistas minimizadas cuando corresponda), cierre formal no automático a cargo del CTO (equivalente a un `DEC` cerrando un `FIN`), y administración exclusiva de `AI_REGISTRY.md` por el CTO con el campo "Cumple vistas minimizadas". El Fundador ratificó los 3 refinamientos. `GOBERNANZA.md` se actualizó a v3.1 (v3.0 archivada en `docs/archive/GOBERNANZA-v3.0.md`); se creó `docs/producto/AI_REGISTRY.md` con alta retroactiva regularizada de los 2 agentes ya operativos (CPSAO, CTO/Arquitecto/Auditor). El Fundador añadió además la directriz "Prioridad de ejecución sobre documentación" (Gobernanza v3.1 §24): el foco del proyecto pasa de definir gobernanza a ejecutar valor real; no se genera documentación por generar documentación. **FIN-012 sigue siendo la única funcionalidad activa**, sin cambios por este ciclo.
- 2026-07-06 — **CTO: VALIDACIÓN DE IMP-0012 RECHAZADA — FIN-012 NO SE CIERRA.** Nueva regla operativa adoptada por el Fundador: ninguna funcionalidad nueva entra en desarrollo mientras exista una FIN pendiente de validación o cierre (ritmo desarrollar→validar→cerrar→continuar). Verificación en checkout aislado (`git clone` + `git checkout 9ea01d6`, SHA completo confirmado `9ea01d63a4e79935a5edceb7d7c0df19d5fbd62d`): (1) el hallazgo crítico de inflación por reutilización de `simulateExtraPayment` está genuinamente corregido — `remainingSchedule`/`prepaymentReceipt` en `amortization.service.ts` son funciones puras que nunca invocan `simulateExtraPayment`, usadas desde `debt-prepayment.service.ts`; (2) la condición de carrera en `Debt.currentBalance` está genuinamente corregida — `transactions.service.ts` usa `UPDATE...RETURNING` atómico en `$transaction` para `pago_deuda`, `debt-prepayment.service.ts` usa `SELECT...FOR UPDATE` vía `$queryRaw` en `$transaction` para prepago/pago total; (3) 216/216 tests de lógica pasan (14/37 suites fallan solo por el limitante conocido de Prisma client en módulos ajenos a FIN-012, no bloqueante). **Hallazgo bloqueante:** el cambio obligatorio #4 del DEC-0012 (test E2E de concurrencia con verificación de saldo exacto) no existe en el commit `9ea01d6` — no hay ningún `.e2e-spec.ts` ni test con ejecución concurrente sobre las rutas de deuda, pese a que IMP-0012 declara "E2E 9/9, saldo final exacto 400.000" como evidencia entregada. Discrepancia real entre documentación y código, no observación cosmética (se excluyen del reporte observaciones de estilo/naming sin impacto, por directriz del Fundador de evitar ruido). **No se autoriza el cierre.** Se devuelve a Arquitecto para entregar exactamente el test E2E de concurrencia faltante — no se abre FIN nueva, no se reabre el diseño ya validado (regla de Acciones Correctivas no aplica aquí porque no hay observación pendiente en el DEC; esto es una re-entrega de IMP-0012 con evidencia real, análogo al precedente de IMP-0002 v1→v2). FIN-012 permanece como única funcionalidad activa.
- 2026-07-06 — **CTO: nueva regla permanente — Correspondencia exacta DEC→IMP→Código→Evidencia obligatoria en toda VALIDACIÓN.** Directriz del Fundador tras el hallazgo de FIN-012 (IMP-0012 declaraba un test E2E de concurrencia inexistente en el código). Añadida a `docs/GOBERNANZA.md` (v3.2; v3.1 archivada en `docs/archive/GOBERNANZA-v3.1.md`): toda `VALIDACIÓN` futura debe verificar explícitamente la correspondencia entre lo exigido por el `DEC`, lo declarado por el `IMP`, lo que existe realmente en el código (checkout aislado) y la evidencia verificable — cualquier discrepancia es hallazgo bloqueante por definición. El Fundador confirmó además: FIN-013–016 **no se revalidan** (ya cerradas, sin hallazgo ni commit nuevo que lo justifique); el flujo validar→corregir→cerrar→continuar aplica únicamente a FIN-012, única funcionalidad operativamente abierta.
- 2026-07-06 — **CTO: VALIDACIÓN DE IMP-0012 v2 APROBADA — FIN-012 CERRADO.** Re-verificación aislada contra commit `5e8d32fd767f73e8e6943721ded4470a3436ec3b` (SHA completo confirmado). Correspondencia DEC→IMP→Código→Evidencia aplicada íntegramente: (1) `git diff 9ea01d6 5e8d32f --stat` confirma que el rango solo toca `backend/package.json` (+1 script), `backend/test/fin012-debt-concurrency.e2e-spec.ts` (nuevo) y `backend/test/jest-e2e.json` (nuevo) — cero cambios en `backend/src/`, tal como declaró el Arquitecto; (2) el archivo E2E existe y contiene genuinamente los 3 tests declarados, con paralelismo real (`Promise.all`, no secuencial) contra la app real (`AppModule`, prefijo `v1`, `ValidationPipe`) y rutas reales (`/v1/transactions`, `/v1/debts/:id/prepay`, `/v1/debts/:id/payoff`), con las aserciones exactas de saldo (400.000 / 500.000 / 0 + estado `pagada`) verificadas contra los controllers reales; (3) suite unitaria re-ejecutada sin regresión (227/227 en las suites que compilan en este sandbox). **Limitación de entorno documentada:** no fue posible ejecutar el E2E de punta a punta en este sandbox — se levantó PostgreSQL real sin Docker, pero `npx prisma generate` no pudo completar por bloqueo de red del sandbox hacia `binaries.prisma.sh` (el mismo limitante ya documentado desde FIN-002), y el cliente Prisma pre-generado en el repo solo trae el engine de Windows, no Linux. Se aplica el mismo criterio que cerró FIN-002 v2: la causa raíz del hallazgo bloqueante original quedó resuelta y verificada contra el código real, y la imposibilidad de ejecución es atribuible exclusivamente al sandbox, no al código — verificación estática exhaustiva no encontró discrepancia alguna entre lo exigido, lo declarado y lo real. **Observación no bloqueante registrada:** el rango de commits reenviado en realidad incluyó un commit intermedio (`fcf046a`, documentación de la regularización FIN-013–016, sin tocar `backend/src/`) no mencionado explícitamente por el Arquitecto al describir "un solo commit"; no afecta el cierre, pero se traslada como recordatorio: todo IMP debe declarar el rango completo de commits, no solo el SHA final, conforme a la nueva regla de correspondencia exacta. **FIN-012 queda CERRADO.** Con esto, ninguna FIN permanece pendiente de validación o cierre — queda habilitada la selección conjunta (Fundador + CTO) de la siguiente funcionalidad del Backlog por criterio de máximo valor para el usuario.
- 2026-07-06 — **CTO: informe ejecutivo de cierre de etapa** entregado al CPSAO (`docs/producto/INFORME-EJECUTIVO-2026-07-06-Cierre-Etapa-1.md`): estado del producto (13/13 FIN cerradas, ninguna pendiente de diseño), estado técnico (sin deuda técnica abierta, gates de producción como único bloqueo real), estado del Backlog (FIN-010 y IDEA-0001 como únicos ítems pendientes) y recomendación del CTO: priorizar FIN-010 (gates de producción) sobre cualquier FIN nueva, porque hoy ningún usuario real puede usar lo ya construido mientras Score/Copiloto con IA real sigan apagados por flags a la espera de DPA/PIA/revisión legal.
- 2026-07-06 — **CPSAO/CTO/Fundador: ratificación del Plan Alpha Cerrada de Milla (`IDEA-0002`).** Tras análisis conjunto en chat (sin documentación intermedia, conforme a la regla adoptada: "las conversaciones CPSAO-CTO son coordinación en chat; solo lo aprobado por el Fundador se documenta"), el Fundador ratificó 6 puntos que sí modifican el rumbo del producto: (1) la Alpha Cerrada con ~20 usuarios reales como siguiente gran meta de producto, antes de la salida completa a producción (FIN-010); (2) principio de que la Alpha valida cambio de comportamiento financiero real, no solo uso de la app; (3) la confianza (consultar a Milla antes de una decisión financiera real) como indicador principal de éxito; (4) la estructura de 8 fases del Plan Alpha, aprobada como esqueleto sin desarrollo de fases todavía; (5) el Copiloto en modo plantillas (sin IA generativa) durante la Alpha, evitando el gate de DPA; (6) principio permanente de protección de datos. Registrado en `docs/producto/lab/IDEA-0002.md` y `LAB.md` v1.2, en `PRODUCT_DECISIONS.md` v1.1 (4 decisiones nuevas), y en `PRODUCT_VISION.md` v1.3 (principio de protección de datos añadido a §8). **No genera ninguna `FIN` ni modifica el Backlog de desarrollo** — ninguna fase del Plan Alpha inicia sin autorización adicional explícita del Fundador/CPSAO.
- 2026-07-06 — **CPSAO/Fundador: ratificación del programa `ALPHA-XXX`** como mecanismo paralelo de gobernanza para ejecutar el Plan Alpha fase por fase (misma disciplina que `FIN-XXX`). Añadido a `docs/GOBERNANZA.md` v3.3 (§25; v3.2 archivada en `docs/archive/GOBERNANZA-v3.2.md`). Creado `docs/producto/alpha/ALPHA_REGISTRY.md` (administrado exclusivamente por el CTO) y entregada `ALPHA-001` (Objetivos de la Alpha, `docs/producto/alpha/ALPHA-001-Objetivos.md`) — pendiente de aprobación del CPSAO/Fundador antes de habilitar `ALPHA-002`.
- 2026-07-06 — **CPSAO: `ALPHA-001` aprobada.** CTO entrega `ALPHA-002` (Selección de participantes, `docs/producto/alpha/ALPHA-002-Seleccion-Participantes.md`): criterios de elegibilidad mínima + matriz de diversidad de perfiles de comportamiento financiero (organización, tipo de ingreso, complejidad de deuda) por observación estratégica del CPSAO. `ALPHA-003` no inicia hasta su aprobación.
- 2026-07-06 — **CPSAO/Fundador: ratificación del Project Health Review (PHR)** como práctica permanente (Gobernanza v3.4 §26; v3.3 archivada en `docs/archive/GOBERNANZA-v3.3.md`). 7 secciones (tamaño, archivos innecesarios, dependencias, calidad, rendimiento, salud arquitectónica, tendencias entre ciclos) + indicador visual de salud (🟢🟡🟠🔴, 0-100) + clasificación Críticas/Recomendadas/Opcionales. Ejecutado por el CTO por hitos (cada 3-4 FIN cerradas, antes de transiciones mayores, o cada 6 meses como mínimo), nunca ejecuta cambios por sí mismo. Creado `docs/tecnico/PHR_REGISTRY.md`, sin ciclos ejecutados todavía.
- 2026-07-06 — **CPSAO: `ALPHA-002` aprobada,** con observación estratégica de tratar a los 20 participantes como "Consejo Fundador de Milla" (retroalimentación cualitativa activa, no solo métricas). CTO entrega `ALPHA-003` (Preparación técnica, `docs/producto/alpha/ALPHA-003-Preparacion-Tecnica.md`): acceso controlado (flags, distribución privada, allowlist, Copiloto en modo plantillas) + canales de Consejo Fundador reutilizando WhatsApp/Telegram ya existentes (reporte de dificultades, propuestas de mejora, entrevistas breves sobre decisiones reales, pregunta de confianza semanal) — sin construir infraestructura nueva de producto. `ALPHA-004` no inicia hasta su aprobación.
- 2026-07-06 — **CPSAO: `ALPHA-003` aprobada,** con ampliación estratégica: taxonomía obligatoria de observaciones (Bug/Usabilidad/Confianza/Funcionalidad/Comportamiento) y el principio "todo hallazgo termina en decisión, nunca solo registrado" para todo el programa Alpha (registrado en `ALPHA_REGISTRY.md`, sección de principios transversales). CTO entrega `ALPHA-004` (Preparación legal, `docs/producto/alpha/ALPHA-004-Preparacion-Legal.md`): PIA, consentimiento informado con el principio de protección de datos ya ratificado, seguridad base — sin DPA (Copiloto en plantillas), sin política de tiendas ni RevenueCat (distribución privada, sin monetización). Requiere aprobación explícita del Fundador (no solo del CPSAO) por tratarse de consentimiento y PIA. `ALPHA-005` no inicia hasta su aprobación.
- 2026-07-06 — **CPSAO: `ALPHA-004` aprobada,** con principio de transparencia radical para el consentimiento (incorporado a `ALPHA-004-Preparacion-Legal.md`) y principio permanente de que todo documento legible por el usuario debe ser comprensible sin conocimientos jurídicos/técnicos. CTO entrega `ALPHA-005` (Instrumentación de métricas, `docs/producto/alpha/ALPHA-005-Instrumentacion-Metricas.md`), incorporando el Principio de Claridad Radical (toda dificultad de comprensión es responsabilidad del producto, no del usuario): mide uso, comprensión e impacto como tres dimensiones separadas, consolidadas por participante y cruzadas con la matriz de diversidad de `ALPHA-002`. `ALPHA-006` no inicia hasta su aprobación.
- 2026-07-06 — **CPSAO: `ALPHA-005` aprobada,** sin ampliaciones adicionales. CTO entrega `ALPHA-006` (Cronograma operativo, `docs/producto/alpha/ALPHA-006-Cronograma-Operativo.md`): 4 etapas sobre 30-35 días (preparación, lanzamiento, ventana de observación con 3 checkpoints intermedios de entrevista+revisión, cierre y análisis), con regla de escalamiento inmediato para hallazgos críticos detectados antes del cierre. `ALPHA-007` no inicia hasta su aprobación.
- 2026-07-06 — **CPSAO: `ALPHA-006` aprobada.** CTO entrega `ALPHA-007` (Criterios de éxito, `docs/producto/alpha/ALPHA-007-Criterios-Exito.md` — consolidación de 7 criterios ya acordados en fases previas, sin criterios nuevos) y, por nueva directriz del CPSAO ("cada fase deja un activo reutilizable, no solo un documento"), el primer activo del programa: `docs/producto/alpha/ALPHA_RESULTS_TEMPLATE.md` (plantilla de 9 campos para registrar aprendizaje en vivo durante el piloto, sin contenido todavía). También se registró el principio "la Alpha termina cuando el aprendizaje se convierte en decisiones, no a los 30 días por calendario". `ALPHA-008` no inicia hasta la aprobación de `ALPHA-007`.
- 2026-07-06 — **CPSAO: `ALPHA-007` aprobada,** con directriz de estabilidad de criterios (no se modifican retroactivamente durante la ejecución). CTO entrega `ALPHA-008` (Criterios de paso a Beta, `docs/producto/alpha/ALPHA-008-Criterios-Paso-Beta.md`): 4 condiciones mínimas de salida de Alpha (cero hallazgos bloqueantes, infra probada bajo concurrencia real, comportamiento objetivo evidenciado, todas las hipótesis resueltas en el template) separadas de las capacidades deseables de Beta (PIA ampliado, decisión sobre IA real del Copiloto, automatización del análisis cualitativo). **Con esta entrega se completa la planificación de las 8 fases del Programa Alpha** — pendiente de aprobación final antes de pasar de planificación a ejecución real (cierre de gates de `ALPHA-004` y lanzamiento conforme a `ALPHA-006`).
- 2026-07-06 — **CPSAO: `ALPHA-007` y `ALPHA-008` aprobadas oficialmente — cierre de la planificación del Programa Alpha.** No se autorizan nuevas fases `ALPHA-XXX` de planificación. CTO entrega `docs/producto/alpha/ALPHA_EXECUTION_BOARD.md`: tablero real de 11 actividades con responsable, dependencia y estado (identificar candidatos, canal de Consejo Fundador, revisión legal del consentimiento ya redactado en `CONSENTIMIENTO-ALPHA.md`, PIA, seguridad base, allowlist técnica, distribución privada, validación de concurrencia real, fecha de inicio, agenda de entrevistas). El programa pasa oficialmente de planificación a ejecución.
- 2026-07-06 — **Fundador: decisión de retención/anonimización de datos para la Alpha.** Retención 60 días post-cierre; anonimización por defecto (con salvedad: si la revisión legal determina pseudonimización, se aplican las obligaciones correspondientes); borrado físico solo ante obligación legal o solicitud que lo exija. `PIA-ALPHA.md` v1.1 y `CONSENTIMIENTO-ALPHA.md` v1.1 actualizados de forma coherente (directriz del CPSAO). PIA cerrado en producto/negocio, enviado a revisión legal junto con el consentimiento. Nuevo cuello de botella (categoría Externa, `ALPHA_EXECUTION_BOARD.md` v1.2): concepto del abogado sobre el estándar de anonimización — el resto del tablero avanza en paralelo.
- 2026-07-09 — **Arquitecto/CTO: `PRODUCT_REVIEW_001.md` — primera auditoría de UX de Milla (`docs/producto/PRODUCT_REVIEW_001.md`).** El Arquitecto documentó 17 pantallas reales (evidencia de código, sin mockups); capturas reales no disponibles en este sandbox (incompatibilidad ICU 60/70 bloquea Postgres real o embebido sin root/Docker — causa documentada, no excusa genérica). El CTO validó independientemente los 2 hallazgos de mayor impacto contra el repositorio real: (1) confirmado — la ruta `MilloPlus` está importada y tipada pero nunca registrada en `RootNavigator.tsx`, rompiendo en tiempo de ejecución los 2 puntos de entrada reales al paywall (desde Salud y desde el Simulador); (2) confirmado — `HEALTH_SCORE_PRODUCTION_ENABLED`, `COPILOT_PRODUCTION_ENABLED`, `MILLOPLUS_PRICE_COP` y las credenciales de WhatsApp/Telegram/Anthropic coinciden exactamente con lo declarado. Documento autorizado para la revisión de UX del CPSAO. **Hallazgo elevado aparte al Fundador, fuera del alcance de la revisión de UX:** el bug de `MilloPlus` es un defecto de producción real (rompe el flujo de monetización) que requiere corrección independiente de cualquier decisión de experiencia de usuario.
- 2026-07-09 — **Arquitecto/CTO: Lote 01 de capturas reales validado (commit `0bfa154`).** Nueva dinámica iterativa por lotes pequeños (nombre + captura real + flujo breve), reemplazando el documento único de `PRODUCT_REVIEW_001`. El CTO verificó independientemente contra el repositorio: el commit existe, el diff de `frontend/package.json`/`metro.config.js` confirma que el tooling de captura (react-native-web, react-dom, shims de `expo-sqlite`/`expo-secure-store`) está condicionado a `platform === 'web'` y no toca el bundle nativo; y las 4 imágenes (Login, Inicio, Salud, Presupuesto) son capturas reales de la app corriendo con datos de una usuaria demo sembrada por API, no mockups — verificado visualmente. **Se autoriza mantener el tooling de captura dentro del repositorio** (alcance acotado, sin impacto funcional, no requiere ciclo `ARQ→AUD→DEC` por ser tooling de desarrollo, no una funcionalidad de producto). Lote entregado al CPSAO para revisión de UX. Próximo lote en preparación: Deudas, Detalle de deuda, Simulador, Copiloto.
- 2026-07-11 — **CPSAO/Fundador: ratificación de la clasificación obligatoria de origen de las FIN** (Gobernanza v3.5 §27; v3.4 archivada en `docs/archive/GOBERNANZA-v3.4.md`): funcionalidad nueva / deuda técnica / acción correctiva / mejora de revisión de producto / mejora del Programa Alpha. **Apertura de FIN-017 — Mejora de UX de Login y Dashboard**, origen "Mejora de revisión de producto", alcance acotado explícitamente a las 4 prioridades del Lote 01 (claridad de propósito, jerarquía del Dashboard, interpretación antes que datos, lenguaje cotidiano) — sin ampliar alcance, por directriz del CPSAO de preferir iteraciones cortas y medibles. El Arquitecto debe entregar `ARQ-017` con al menos 2 alternativas por cambio importante. El Lote 02 de capturas (Deudas, Detalle, Simulador, Copiloto) continúa en paralelo, sin gobernanza adicional por ser documentación pura. FIN-017 es la única FIN activa.
- 2026-07-11 — **CPSAO: hoja de ruta de experiencias UX.** Orden de trabajo por experiencia completa (Inicio → Salud → Presupuesto → Deudas → Simulador → Copiloto), cada una como su propia FIN bajo la regla "un FIN a la vez". Registrado en la nueva sección "Hoja de ruta de experiencias UX" de este documento. El CTO aclaró que la excepción de "avanzar técnicamente cuando no exista dependencia" se limita a correcciones puntuales (Acciones Correctivas o defectos triviales, p. ej. el bug de `MilloPlus`) — no habilita abrir una segunda FIN de experiencia en paralelo.
- 2026-07-11 — **CPSAO: refinamiento de FIN-017.** El objetivo no es implementar mecánicamente las 4 observaciones sino llevar la experiencia de Inicio al estándar de calidad esperado — el Arquitecto puede proponer simplificaciones adicionales evidentes dentro del mismo alcance (Login+Dashboard), evaluadas en el `ARQ-017`, nunca implementadas sin pasar por ahí. Al cerrar FIN-017, se repite la revisión completa con capturas actualizadas antes de avanzar a la Experiencia de Salud.
- 2026-07-11 — **CPSAO/Fundador: ratificación de la ampliación del AUD para FIN de experiencia de usuario** (Gobernanza v3.6 §28; v3.5 archivada en `docs/archive/GOBERNANZA-v3.5.md`). Para toda FIN con impacto en UX, el Auditor debe responder 6 preguntas obligatorias de riesgo de comprensión (interpretación incorrecta, terminología confusa, carga cognitiva, jerarquía visual, coherencia con el resto del producto, cumplimiento del Principio de Claridad Radical) — identifica, no diseña ni decide. Se establece además el criterio permanente de éxito para toda FIN de UX: se mide por reducción del esfuerzo de comprensión del usuario, no por cantidad de cambios. Aplica desde ya a FIN-017: el Arquitecto redacta `ARQ-017` (propuesta integral, no checklist mecánico, con ≥2 alternativas por cambio importante) y el Auditor aplicará este nuevo estándar en `AUD-017`.
- 2026-07-11 — Auditor: generado `docs/auditoria/AUD-0017-UX-Login-Dashboard.md` sobre `ARQ-0017` v1.1 (commit `948bddb`). Verificó el diagnóstico del §2 contra las capturas reales del Lote 01 (confirmado: tagline sin propuesta de valor, tres elementos compitiendo por protagonismo en Dashboard, cifras sin interpretación) y respondió las 6 preguntas de UX exigidas por el CTO. Prioridades 1, 2, 4 y §4.5 (gamificación): sin hallazgos bloqueantes. Prioridad 3 (interpretación server-side, alternativa 3-A recomendada): **dos hallazgos elevados a críticos**, verificados contra `health.service.ts`/`score.util.ts`/`core-metrics.ts`/`dashboard.service.ts` — (1) el DTI del Score se calcula por el Motor sobre el **mes calendario** (snapshot mensual persistido), mientras el Dashboard usa el **ciclo financiero** del usuario (FIN-016, `cycleStartDay`); para usuarios con día de corte ≠1 ambos periodos divergen; (2) el numerador del DTI es la suma de cuotas **programadas** de deudas activas (`debtMonthly`), no lo efectivamente **pagado** en el ciclo (`debtPayments`, que el Dashboard ya muestra como "cuotas del mes") — son cifras de significado distinto que el propio ejemplo de diseño objetivo (§4.6) coloca una junto a la otra sin distinguirlas, arriesgando precisamente el tipo de confusión que la Prioridad 3 busca eliminar. Hallazgo menor adicional: la propuesta de valor recomendada del Login (Alt A) cubre 3 de los 4 pilares que el propio diagnóstico señala (falta Presupuesto). Veredicto: **REQUIERE AJUSTES, acotado a Prioridad 3** — recomienda a Arquitectura aclarar la reconciliación de periodo/numerador antes de autorizar esa pieza específica; el resto puede avanzar a `DEC-017`. FIN-017 pasa a *Auditado — REQUIERE AJUSTES acotado a Prioridad 3; en espera de DEC-017*.
- 2026-07-11 — **CTO: generado `docs/oficial/DEC-0017-UX-Login-Dashboard.md`.** Verificación independiente previa contra el repositorio real (no solo el informe del Auditor, por instrucción explícita del Fundador): confirmé en `health.service.ts` que `HealthService.score()` usa `monthStart(now)` sobre un snapshot persistido; confirmé en `core-metrics.ts` que `Dti = debtMonthly / ref` con `debtMonthly` = cuotas *programadas*; confirmé por grep que `dashboard.service.ts` usa `financialPeriod()` (ciclo del usuario) y `debtPayments` (cuotas *pagadas*) y no importa nada de `health/` ni `financial-engine/`; confirmé en el §4.6 del ARQ que el mockup objetivo efectivamente coloca el DTI del Score junto a la cifra de Deuda del Dashboard sin distinguir sus periodos; confirmé que la Alt A del Login solo enumera 3 de los 4 pilares diagnosticados. Los 4 hallazgos del Auditor quedaron verificados de forma independiente. **Decisión: aprobadas sin condiciones** Prioridad 2, Prioridad 4 y §4.5; **aprobada con corrección obligatoria** Prioridad 1 (ampliar a 4 pilares o justificar la omisión); **devuelta a Arquitectura** la interpretación de "Deuda total" de la Prioridad 3 (no las interpretaciones de flujo/ahorro de esa misma prioridad, que sí quedan autorizadas), hasta que se resuelva la reconciliación de periodo/numerador por una de las dos rutas propuestas en el DEC. No se requiere una nueva vuelta completa de Auditoría — solo confirmación puntual del CTO sobre las dos correcciones antes de habilitar IMP-0017. FIN-017 pasa a *Decidido parcialmente — en espera de correcciones puntuales de Arquitectura*.
- 2026-07-11 — **CPSAO/Fundador: ratificación de dos principios permanentes de interpretación y lenguaje** (Gobernanza v3.7 §29; v3.6 archivada en `docs/archive/GOBERNANZA-v3.6.md`), formulados por el CPSAO al revisar `DEC-017` sin objetar su sentido: (1) "la interpretación nunca introduce una nueva pregunta" — si explicar una cifra exige que el usuario entienda el funcionamiento interno del producto, la solución se simplifica en vez de añadir la aclaración; (2) "prioridad del lenguaje humano sobre el lenguaje financiero" — toda redacción de interpretación debe superar la prueba de "¿una persona sin conocimientos financieros lo entendería en la primera lectura?". Ambos se incorporan como Adendo de `DEC-0017` (§6.1) y aplican a las correcciones pendientes de FIN-017 y a toda FIN de experiencia de usuario futura.
- 2026-07-04 — Auditor: generado `docs/auditoria/AUD-0003-Motor-Financiero.md`. Verificó contra referencia inmutable (commit `622bfa1`, no working tree) que el cierre de FIN-002 es real. Sin incumplimientos de mandatos vinculantes. Veredicto: **APROBADO CON OBSERVACIONES** (regla `ingreso_mensual_ref` todo-o-nada puede distorsionar DTI/savings_rate; umbral de cold-start global de 60 días no reconciliado con el umbral por categoría de 3 meses para anomalías; zona horaria de jobs nocturnos no especificada; criterio de "usuario activo" para el SnapshotJob no definido). FIN-003 pasa a *Auditado — en espera de DEC*.
- 2026-07-04 — CTO: generado `docs/oficial/DEC-0003-Motor-Financiero.md`. **APROBADO CON AJUSTES.** Se fijan 5 cambios obligatorios: (1) `ingreso_mensual_ref` = `max(ingresos_fijos, ingresos_reales_del_mes)` en vez de todo-o-nada, (2) el umbral por categoría (3 meses) para anomalías es adicional al cold-start global (60 días), ambos deben coexistir y documentarse juntos, (3) zona horaria `America/Bogota` explícita en los jobs nocturnos, (4) criterio de "usuario activo" = actividad en los últimos 90 días, (5) nota de migración obligatoria de `anomaly.*` hacia `Insight` en el futuro ARQ de FIN-006. Se autoriza iniciar implementación de FIN-003. FIN-003 pasa a *Decidido — autorizado a implementación*.
- 2026-07-05 — Desarrollador: implementado FIN-003 y generado `docs/implementaciones/IMP-0003-Motor-Financiero.md` con **SHA `bbf9654431bef402564149e140b505908c2cc82e`** (regla de referencia inmutable). Pipeline evento→métrica operativo; 7 métricas core con `ingreso_ref=max(fijo,real)`; jobs en America/Bogota (snapshot/tendencias/retención) ejecutados y verificados de verdad; `metric_readings` particionada (filas en partición mensual, 0 en DEFAULT); cold-start dual verificado end-to-end. 155/155 tests. Los 5 cambios obligatorios de DEC-0003 §10 cumplidos. FIN-003 pasa a *Implementado — a la espera de validación del CTO*.
- 2026-07-05 — CTO: **VALIDACIÓN DE IMP-0003 APROBADA — FIN-003 CERRADO.** Re-verificación independiente en checkout aislado (`git clone` + `git checkout bbf9654`, no working tree): schema y `engine.constants.ts` completos y consistentes con los 5 cambios obligatorios de DEC-0003 (cold-start dual documentado, TZ `America/Bogota`, `ACTIVE_USER_WINDOW_DAYS=90`, contrato `MetricKey`). `npx jest` → **146/146 tests que compilaron pasan** (17/20 suites); 3 suites (`jobs.spec.ts`, `engine.service.spec.ts`, `outbox.service.spec.ts`) no compilan en este sandbox por el mismo motivo ya documentado en la validación de IMP-0002 (cliente Prisma sin generar por bloqueo de red a `binaries.prisma.sh`, no un defecto de código) — mismo patrón, misma causa raíz, no nuevo. Las pruebas de lógica de negocio específicas de los cambios obligatorios (`core-metrics.spec.ts` con el caso `max(fijo,real)`, `series.util.spec.ts` con z-score/tendencia) pasan limpio. **Se acepta el cierre de FIN-003.** No se encontró ningún defecto atribuible al código o al diseño. **Se autoriza iniciar `ARQ-0004` (FIN-004, Salud Financiera + Score Millo), acotado a Score + máximo 3 indicadores (DEC-0001 §10.9).**
- 2026-07-05 — Arquitectura: generado `docs/arquitectura/ARQ-0004-Salud-Financiera.md` (FIN-004). Alcance acotado por mandato: Score Millo v1 (0–1000, 4 pilares renormalizados desde las métricas de FIN-003, explicable, con bandas y delta por pilar) + 3 indicadores accionables (DTI, fondo de emergencia, capacidad de ahorro) + señal de monetización (plan free/premium, gate del histórico del Score, DEC-0001 §10.8) + disclaimers de encuadre educativo con gate de release legal (§10.7). Sin IA, sin tablas nuevas (Score como `MetricReading`), cero dependencias. FIN-004 pasa a *En auditoría*.
- 2026-07-05 — Auditor: generado `docs/auditoria/AUD-0004-Salud-Financiera.md`. Verificó contra referencia inmutable (commit `bbf9654`) el contrato `metricKey`/`incomeRef=max()` de FIN-003 que este ARQ consume sin modificar. Sin incumplimientos de mandatos vinculantes. Veredicto: **APROBADO CON OBSERVACIONES** (función `norm(tendencia)` del pilar Patrimonio no definida con anclas; pilar Patrimonio no escala con la magnitud del patrimonio, solo signo/tendencia; renormalización ambigua cuando solo una sub-métrica del pilar Ahorro falta; gate de exposición a producción es solo procesal, sin mecanismo técnico/flag). FIN-004 pasa a *Auditado — en espera de DEC*.
- 2026-07-05 — CTO: generado `docs/oficial/DEC-0004-Salud-Financiera.md`. **APROBADO CON AJUSTES.** Se fijan 3 cambios obligatorios: (1) `norm(tendencia) = clamp(pendiente_3m(trend.net_worth) ÷ max(essential_expense_mensual,1), -1, 1)` con anclas explícitas, (2) renormalización parcial del pilar Ahorro: si falta una sola sub-métrica se usa solo la disponible (`partial:true`), unavailable solo si faltan ambas, (3) **mecanismo técnico obligatorio** `HEALTH_SCORE_PRODUCTION_ENABLED` (default `false`) que bloquea `/health/*` en producción hasta validación legal — se eleva de "antes de producción" a obligatorio de este ciclo, reforzando el gate procesal de DEC-0001 §10.7 con enforcement real en código. La magnitud del pilar Patrimonio (Hallazgo 2) queda como mejora futura de `scoreVersion`, no bloqueante. Se autoriza iniciar implementación de FIN-004. FIN-004 pasa a *Decidido — autorizado a implementación*.
- 2026-07-05 — Desarrollador: implementado FIN-004 y generado `docs/implementaciones/IMP-0004-Salud-Financiera.md` con **SHA `c85117e7e89afaa9c761b31b24687a0be57ebcdd`**. Score Millo v1 (verificado end-to-end: 802 "saludable" con ponderación comprobada a mano) + 3 indicadores accionables + gate premium (403 free / histórico premium) + guard técnico de producción (`HEALTH_SCORE_PRODUCTION_ENABLED`, DEC-0004 §10.3) + pestaña Salud con disclaimer. Los 3 cambios obligatorios de DEC-0004 §10 cumplidos. 171/171 tests; bundle Android OK. FIN-004 pasa a *Implementado — a la espera de validación del CTO*.
- 2026-07-05 — CTO: **VALIDACIÓN DE IMP-0004 APROBADA — FIN-004 CERRADO.** Re-verificación independiente en checkout aislado (`git clone` + `git checkout c85117e`, no working tree): `score.util.ts` y `health-production.guard.ts` confirman los 3 cambios obligatorios de DEC-0004 (`normTrend` con las anclas definidas, pilar Ahorro con `status:'partial'` cuando falta una sola sub-métrica, guard 503 en producción sin flag). `npx jest` → **162/162 tests que compilaron pasan** (19/22 suites); las mismas 3 suites de siempre (`jobs.spec.ts`, `engine.service.spec.ts`, `outbox.service.spec.ts`) no compilan por la causa raíz ya documentada dos veces (cliente Prisma sin generar por bloqueo de red en este sandbox, no defecto de código) — patrón idéntico y ya conocido. Las suites nuevas de este ciclo (`score.util.spec.ts`, `health-production.guard.spec.ts`) pasan limpio, cubriendo exactamente los 3 cambios obligatorios. **Se acepta el cierre de FIN-004.** **Se autoriza iniciar `ARQ-0005` (FIN-005, Copiloto Financiero)**, con advertencia: por DEC-0001 §10.6/§10.7, el `DEC-0005` quedará **bloqueado** hasta que el propio ARQ-0005 especifique el mecanismo de consentimiento/minimización de datos hacia el LLM externo, y hasta que exista validación legal explícita del encuadre regulatorio — el ARQ puede redactarse, pero no se decidirá sin resolver ambos puntos.
- 2026-07-05 — Arquitectura: generado `docs/arquitectura/ARQ-0005-Copiloto-Financiero.md` (FIN-005). Núcleo del documento: **consentimiento explícito** (opt-in versionado y revocable, §4.2) y **minimización auditable** (allowlist de campos con test de regresión + `AiInteractionLog` sin texto, §4.3–§4.4), como exige DEC-0001 §10.6. Orquestador plantilla-primero → LLM (Anthropic Haiku, fetch sin SDK, prompt caching, tools de solo lectura), modo degradado sin IA siempre disponible, límite de mensajes free/premium, gate técnico `COPILOT_PRODUCTION_ENABLED`. Excluye memoria/RAG (FIN-006) y simulador/recomendaciones (FIN-007). §17 declara los bloqueos del DEC: validación legal (acción del CTO) + ratificaciones menores. FIN-005 pasa a *En auditoría; DEC bloqueado hasta validación legal*.
- 2026-07-05 — Auditor: generado `docs/auditoria/AUD-0005-Copiloto-Financiero.md`. Máximo escrutinio dado el riesgo (primera integración LLM sobre datos financieros reales). Veredicto: **REQUIERE AJUSTES**, con dos observaciones elevadas a **críticas**: (1) las tool-use de solo lectura (`get_debts`, `get_financial_snapshot`, `get_score_breakdown`) son una segunda vía de contexto hacia el LLM que, tal como está descrita, no pasa por el mismo allowlist/test de regresión de minimización del `ContextAssembler` — la garantía central del ARQ frente a DEC-0001 §10.6 no cubre esta vía; (2) `Debt.name`/`FixedItem.name` (verificados en `schema.prisma` como texto libre del usuario) están permitidos en el contexto pese a tener el mismo perfil de riesgo que `Transaction.note`, que el propio ARQ excluye explícitamente por "ser texto libre, PII potencial" — inconsistencia interna del propio documento. Recomendó que Arquitectura revise §4.3/§4.5 (no ARQ nuevo) antes de que el CTO evalúe el resto. FIN-005 pasa a *Auditado — requiere ajustes; DEC además bloqueado por validación legal*.
- 2026-07-05 — CTO: generado `docs/oficial/DEC-0005-Copiloto-Financiero.md`. **RECHAZADO** (no "aprobado con ajustes"): el defecto está en el mecanismo mismo de minimización que el ARQ existe para garantizar (mandato DEC-0001 §10.6), no en un detalle periférico — el estándar de corrección debe ser más alto que en ciclos anteriores. Se exige `ARQ-0005 v2` (mismo número) que: (1) cierre la segunda vía de contexto haciendo que las tool-use pasen por el mismo `ContextAssembler`/allowlist con test de regresión que también las cubra, (2) retire `Debt.name`/`FixedItem.name` del contexto (sustituir por tipo + identificador no libre), (3) defina política de retención de `Conversation`/`Message` (incluyendo efecto de la revocación de consentimiento), (4) especifique timeout/reintentos del cliente fetch. Ningún desarrollo autorizado. El DEC-0005 que resulte de la revisión seguirá bloqueado, además, por la validación legal pendiente de DEC-0001 §10.7 (condición independiente). FIN-005 pasa a *RECHAZADO — requiere ARQ-0005 v2 + nueva auditoría*.
- 2026-07-05 — Arquitectura: **reenvío de `ARQ-0005` (v2)** tras DEC-0005 RECHAZADO. Cierra los 4 cambios obligatorios de DEC-0005 §10: (1) §4.3-A — las tools consumen exclusivamente vistas minimizadas tipadas del `ContextAssembler` y el test de regresión cubre las 4 vistas con PII sembrada; (2) §4.3 — `Debt.name`/`FixedItem.name` retirados del allowlist, sustituidos por "deuda #N (tipo)"/"gasto fijo #N" con mapeo reversible en servidor; §4.3-B extiende el principio a categorías creadas por el usuario; (3) §4.7 — retención explícita: revocación conserva historial + botón autónomo "Borrar historial" + purga a 24 meses; (4) §4.8 — timeout 30s, 1 retry (red/5xx), 429 sin retry, circuit breaker 5 fallos/5 min. El marco ratificado por DEC-0005 §4 se mantiene sin cambios. FIN-005 pasa a *ARQ v2 en auditoría; DEC bloqueado por validación legal (independiente)*.
- 2026-07-05 — Auditor: generado `docs/auditoria/AUD-0005-Copiloto-Financiero-v2.md` (archivo nuevo; no se edita el AUD-0005 v1 previo). Verificó punto por punto los 4 cambios obligatorios de DEC-0005 §10 contra `ARQ-0005 v2` y contra `schema.prisma` (confirma que `Debt.name`/`FixedItem.name`/`Category.name`/`Account.name`/`Asset.name` son texto libre sin restricción, validando que la exclusión resuelve un riesgo real): los 4 están cerrados con diseño sustantivo (restricción de tipos + validación en runtime + test sobre las 4 vistas; identificadores no libres con mapeo reversible en servidor; política de retención explícita y razonada; timeout/reintentos/circuit breaker). Veredicto: **APROBADO CON OBSERVACIONES** (asimetría de retención entre `AiInteractionLog` de 12 meses y `Conversation`/`Message` de 24 meses; falta una regla de gobernanza que obligue a tools de LLM futuras a seguir el mismo patrón de vistas minimizadas). Ninguna observación crítica. FIN-005 pasa a *Auditado v2 — aprobado con observaciones; DEC sigue bloqueado solo por validación legal pendiente*.
- 2026-07-05 — CTO: generado `docs/oficial/DEC-0005-Copiloto-Financiero.md` (v2, supersede al DEC rechazado). **DISEÑO APROBADO** — el mecanismo de minimización de ARQ-0005 v2 cierra demostrablemente (tipos + runtime + test de 4 vistas) el mandato DEC-0001 §10.6; se ratifica la asimetría de retención (12 vs 24 meses) explícitamente; se añade regla permanente a `GOBERNANZA.md` ("Vistas minimizadas obligatorias para toda tool de LLM"). **NINGÚN DESARROLLO AUTORIZADO**: el único bloqueo restante es la validación legal del encuadre regulatorio (DEC-0001 §10.7), condición de negocio/riesgo legal que corresponde al fundador, no una tarea técnica del Arquitecto/Auditor. FIN-005 pasa a *Diseño aprobado — implementación bloqueada, pendiente decisión del fundador sobre validación legal*.
- 2026-07-05 — CTO: **adenda a DEC-0005** tras recibir memorando legal preliminar del abogado del fundador (derecho financiero/protección de datos, Colombia). Conclusiones: (1) Score = educación financiera no regulada, condicionado a recomendaciones genéricas + disclaimers; (2) envío de datos a Anthropic cumple Ley 1581/2012 condicionado a consentimiento con elementos específicos (responsable, finalidad IA, transferencia internacional a EE.UU., derechos ARCO) **y a un DPA formal con Anthropic**; (3) sin registro SFC requerido hoy (advertencia futura si el Score se comparte con terceros para crédito → Ley 1266/registro SIC). Se añaden 4 cambios obligatorios: (1) reescribir texto de consentimiento con los elementos exigidos, (2) restricción explícita de "recomendación genérica" (sin marcas/tasas/entidades) verificable por test, (3) DPA con Anthropic como precondición para activar datos reales de usuarios en cualquier ambiente, (4) PIA documentada antes de datos reales. **Se autoriza el desarrollo de FIN-005** (dev/staging, modo plantillas/mock); datos reales bloqueados hasta DPA+PIA; producción sigue bloqueada hasta revisión legal final de la documentación completa. FIN-005 pasa a *Desarrollo autorizado — datos reales y producción condicionados*.
- 2026-07-05 — Desarrollador: implementado FIN-005 y generado `docs/implementaciones/IMP-0005-Copiloto-Financiero.md` con **SHA `919f7c2481b0e8971f49c32954b570f68da90f3b`**. Consentimiento opt-in con los 8 elementos legales (test §14.1); ContextAssembler con 4 vistas minimizadas + test de PII sembrada + bloqueo runtime de tools crudas (§10.1); "deuda #N" verificado end-to-end con deudas nombradas con PII deliberada; restricción de recomendación genérica testeada (§14.2); cliente Anthropic con timeout/retry/circuit breaker (fetch mock); límites 10/100 por plan; retención 12/24 meses + borrado autónomo; chat móvil con onboarding. E2E en modo plantillas: `ai_interaction_logs` con CERO entradas `chat` (ningún dato salió al LLM). 203/203 tests; bundle Android OK. Estado §14: (1)✅ (2)✅ (3)⏳ DPA (4)⏳ PIA. FIN-005 pasa a *Implementado — a la espera de validación del CTO*.
- 2026-07-05 — CTO: **VALIDACIÓN DE IMP-0005 APROBADA — FIN-005 CERRADO (modo plantillas/dev).** Re-verificación independiente en checkout aislado (`git clone` + `git checkout 919f7c2`, no working tree): el texto de consentimiento (`copilot.constants.ts`) contiene los 8 elementos legales exigidos (responsable, finalidad IA/Anthropic, transferencia internacional a EE.UU. sin nivel adecuado SIC, ARCO, revocación, no-asesoría); `minimized-views.ts` confirma la marca runtime `__minimized` como único camino validado hacia el LLM. `npx jest` → **188/188 tests que compilaron pasan** (21/25 suites); 4 suites no compilan por la causa raíz ya documentada tres veces (cliente Prisma sin generar por bloqueo de red en este sandbox) — **incluida `context-assembler.spec.ts`, la prueba más crítica de este ciclo (PII sembrada en las 4 vistas)**. Dado el riesgo de esa prueba específica, se hizo diligencia adicional: se corrió aislada con `isolatedModules` (bypass del type-check que falla solo por el cliente Prisma incompleto, no por lógica) → **6/6 tests pasan**, incluyendo "ninguna PII sembrada aparece en ninguna de las 4 vistas". Confirma que la lógica de minimización es correcta y no depende del artefacto de entorno. **Se acepta el cierre de FIN-005** en el modo autorizado (plantillas/dev, sin datos reales). No se encontró ningún defecto atribuible al código o al diseño. Datos reales de usuarios y activación de `ANTHROPIC_API_KEY` **siguen bloqueados** hasta DPA con Anthropic + PIA (DEC-0005 §14.3/§14.4); producción sigue bloqueada hasta revisión legal final. **Se autoriza iniciar `ARQ-0006` (FIN-006, Memoria financiera + Proactividad + RAG)** cuando se solicite, recordando el rechazo de pgvector/RAG hasta evidencia (DEC-0001 §5.2) y la nota de migración de `anomaly.*` hacia `Insight` exigida desde DEC-0003.
- 2026-07-05 — Arquitectura: generado `docs/arquitectura/ARQ-0006-Memoria-y-Proactividad.md` (FIN-006). Modelo `Insight` con ciclo de vida + **plan de migración completo de `anomaly.*` (DEC-0003 §10.5)**; memoria financiera **estructurada sin embeddings** (SQL+tags, texto siempre templado) con **criterio de evidencia para revisar RAG (§4.6, cierra el gate DEC-0001 §5.2)**; 7 generadores deterministas de insights; ProactivityJob anti-fatiga (1/día, quiet hours, toggle, 7AM Bogotá); vista minimizada nueva para el Copiloto bajo la regla de GOBERNANZA (test de PII a 5 vistas). Cero IA nueva, cero dependencias. FIN-006 pasa a *En auditoría*.
- 2026-07-05 — Auditor: generado `docs/auditoria/AUD-0006-Memoria-y-Proactividad.md`. Verificó contra el código real de FIN-003 (`trends.job.ts`) la afirmación del ARQ de que las filas `anomaly.*` huérfanas "salen por retención" tras el corte de escritura: **falsa** — se persisten con `period:'month'`, y `RetentionJob` solo purga `period:'day'` (retención mensual es indefinida por diseño de DEC-0002 §4.5); quedarían como residuo permanente sin una purga nueva. Hallazgo elevado a **observación crítica** por ser una afirmación verificablemente incorrecta sobre el comportamiento del sistema (mismo estándar aplicado a la premisa de Redis/BullMQ en el ciclo de FIN-001). Otras dos observaciones menores: falta un generador simétrico de "riesgo" cuando el Score baja de banda (solo existe el de "logro" cuando sube); la regla anti-fatiga de notificaciones es local al módulo, no agregada sobre todos los canales existentes. Veredicto: **APROBADO CON OBSERVACIONES**. FIN-006 pasa a *Auditado — en espera de DEC*.
- 2026-07-05 — CTO: generado `docs/oficial/DEC-0006-Memoria-y-Proactividad.md`. **APROBADO CON AJUSTES.** Confirmé de forma independiente el Hallazgo 1 (`git show` sobre `trends.job.ts`/`retention.job.ts`): correcto, `anomaly.*` con `period:'month'` nunca se purga por el job existente. Se fijan 4 cambios obligatorios: (1) purga explícita de `anomaly.*` en la misma migración (no "sale por retención"), (2) generador simétrico "Score baja de banda → riesgo/warning", (3) clarificar clave de idempotencia para insights de evento único (usar `debtId` en vez de `metricKey`/mes para "Deuda saldada"), (4) registrar el límite agregado de notificaciones cross-canal como riesgo pendiente explícito para FIN-007/FIN-008. A diferencia de FIN-005, el defecto es de especificación puntual, no del mecanismo central — no amerita rechazo. Se autoriza iniciar implementación de FIN-006. Gates heredados de FIN-005 (DPA/PIA/producción) sin cambios. FIN-006 pasa a *Decidido — autorizado a implementación*.
- 2026-07-05 — Desarrollador: implementado FIN-006 y generado `docs/implementaciones/IMP-0006-Memoria-y-Proactividad.md` con **SHA `994b085cdd2451eba433b44e711eb3a9018893b7`**. Los 4 cambios obligatorios de DEC-0006 §10 cumplidos: purga explícita verificada en vivo (anomalía sintética: migrada=1, restantes=0), generador simétrico de baja de banda con test, dedupeKey por entidad para deuda saldada, nota del límite cross-canal en ProactivityJob. E2E real: insight de sobregiro creado y entregado por push (1/día verificado), 14 hechos de memoria templados sin PII ("gasto fijo #2" en vez del nombre libre), idempotencia comprobada. Test de PII extendido a 5 vistas. 220/220 tests; bundle Android OK. Incidencia corregida: gating de generadores (cold-start solo donde aplica). FIN-006 pasa a *Implementado — a la espera de validación del CTO*.
- 2026-07-05 — CTO: **VALIDACIÓN DE IMP-0006 APROBADA — FIN-006 CERRADO.** Re-verificación independiente en checkout aislado (`git clone` + `git checkout 994b085`, no working tree): la migración real contiene el `INSERT...SELECT` idempotente seguido de `DELETE FROM metric_readings WHERE metric_key LIKE 'anomaly.%'` (cambio obligatorio #1 confirmado en SQL, no solo declarado); `insights.generator.ts` contiene `riesgo_banda:<mes>` (generador simétrico, #2) y `logro_deuda_saldada:<debtId>` (dedupeKey por entidad, #3). `npx jest` → **193/193 tests que compilaron pasan** (22/28 suites); 6 no compilan por la causa raíz ya documentada repetidamente (cliente Prisma sin generar en este sandbox) — incluidas `insights.generator.spec.ts` y `proactivity.job.spec.ts`, que cubren directamente los cambios obligatorios #2/#3 y la regla anti-fatiga. Dada su relevancia, se verificaron con bypass de type-check (`isolatedModules`, mismo método usado en la validación de FIN-005): **18/18 tests pasan** (incluye `context-assembler.spec.ts` con la 5ª vista de memoria). **Se acepta el cierre de FIN-006.** No se encontró ningún defecto atribuible al código o al diseño. Gates heredados de FIN-005 (DPA/PIA/producción) sin cambios. **Se autoriza iniciar `ARQ-0007` (FIN-007, Simulador financiero + Motor de recomendaciones) cuando se solicite.**
- 2026-07-05 — Arquitectura: generado `docs/arquitectura/ARQ-0007-Simulador-y-Recomendaciones.md` (FIN-007). Motor de simulaciones determinista con 7 escenarios (reutiliza AmortizationService/compareStrategies/computeScore) e impacto unificado before/after/delta en Score+métricas sin escribir en las series reales; motor de recomendaciones cuantificadas (impacto×urgencia×viabilidad, máx. 3 activas, estructura qué/por qué/beneficio/qué pasa si no); **resuelve el pendiente heredado DEC-0006 §10.4** con `NotificationLog` + presupuesto global 3/día (recordatorios>proactivos); tool LLM `run_simulation` como 6ª vista minimizada (test PII extendido) + plantillas de simulación sin LLM. Sin escritura sobre finanzas reales, sin notificaciones de recomendaciones en v1, cero dependencias. FIN-007 pasa a *En auditoría*.
- 2026-07-05 — Auditor: generado `docs/auditoria/AUD-0007-Simulador-y-Recomendaciones.md`. Verificó contra el código real que `computeScore`/`computeCoreMetrics` (reutilizados para el cálculo hipotético) existen tal como se describen, y que `Category` (schema.prisma) no tiene ningún campo de clasificación discrecional/esencial. Sin incumplimientos de mandatos vinculantes ni afirmaciones factualmente incorrectas. Veredicto: **APROBADO CON OBSERVACIONES** (el generador de recomendación "categoría dominante discrecional" depende de una clasificación que no existe en el modelo de datos; falta regla de desplazamiento para el cupo de "máximo 3 recomendaciones activas"; comportamiento de cupos no usados del presupuesto de notificaciones sin especificar). FIN-007 pasa a *Auditado — en espera de DEC*.
- 2026-07-05 — CTO: generado `docs/oficial/DEC-0007-Simulador-y-Recomendaciones.md`. **APROBADO CON AJUSTES.** Confirmé de forma independiente contra `schema.prisma` que `Category` no distingue discrecional/esencial. Se fijan 3 cambios obligatorios: (1) clasificación discrecional por lista curada de categorías globales en código; categorías personalizadas del usuario **excluidas por defecto** de ese generador (sin adivinar sobre texto libre); (2) regla de desplazamiento del cupo de 3 recomendaciones activas: la nueva reemplaza a la de menor prioridad solo si su `priorityScore` es estrictamente mayor; (3) cupos no usados del presupuesto de notificaciones **no se reasignan** (reparto fijo 2+1; el tope de 1 proactivo/día es una garantía anti-fatiga independiente de DEC-0006, no un cupo optimizable). Se autoriza iniciar implementación de FIN-007. Gates heredados de FIN-005 sin cambios. FIN-007 pasa a *Decidido — autorizado a implementación*.
- 2026-07-05 — Desarrollador: implementado FIN-007 y generado `docs/implementaciones/IMP-0007-Simulador-y-Recomendaciones.md` con **SHA `a56f11e164e6381ec6a7e1e2219d011877e3870f`**. Los 3 cambios obligatorios de DEC-0007 §10 cumplidos (lista curada verificada contra categorías sembradas; desplazamiento con ambos casos testeados; reparto fijo 2+1 sin reasignación con test explícito). E2E real: simulación de carro $30M → Score 461→426 con series intactas; "¿qué pasa si pago 200 mil más?" respondido por plantilla con números reales y 0 llamadas LLM; recomendaciones nightly con categoría curada (Entretenimiento) y tope de 3. Incidencia corregida en revisión propia: el presupuesto contaba filas en vez de eventos (multi-canal habría consumido doble cupo). 240/240 tests; bundle Android OK. Pendiente heredado DEC-0006 §10.4 RESUELTO. FIN-007 pasa a *Implementado — a la espera de validación del CTO*.
- 2026-07-05 — CTO: **VALIDACIÓN DE IMP-0007 APROBADA — FIN-007 CERRADO.** Re-verificación independiente en checkout aislado (`git clone` + `git checkout a56f11e`, no working tree): `recommendations.constants.ts` confirma `DISCRETIONARY_GLOBAL_CATEGORIES = ['Entretenimiento','Comida','Ropa']` sobre nombres realmente sembrados en `default-categories.ts` (cambio obligatorio #1); `recommendations.service.ts` confirma la comparación estricta `c.priorityScore > Number(weakest.priorityScore)` para el desplazamiento, sin afectar empates/menores (cambio obligatorio #2). `npx jest` → **202/202 tests que compilaron pasan** (23/31 suites); 8 no compilan por la causa raíz ya documentada en cada ciclo anterior (cliente Prisma sin generar en este sandbox por bloqueo de red a `binaries.prisma.sh`) — incluidas `recommendations.service.spec.ts` y `notification-budget.service.spec.ts`, que cubren directamente los 3 cambios obligatorios. Dada su relevancia, se verificaron con bypass de type-check (`isolatedModules`, mismo método usado en FIN-005/FIN-006): **18/18 tests pasan**, incluyendo la regla de reparto fijo sin reasignación (cambio obligatorio #3) y `context-assembler.spec.ts` con la 6ª vista de simulación (PII bloqueada). **Se acepta el cierre de FIN-007.** No se encontró ningún defecto atribuible al código o al diseño. Gates heredados de FIN-005 (DPA/PIA/producción) sin cambios. **Se autoriza iniciar `ARQ-0008` (FIN-008, Gamificación) cuando se solicite.**
- 2026-07-05 — Arquitectura: generado `docs/arquitectura/ARQ-0008-Gamificacion.md` (FIN-008). Racha semanal de registro (no diaria, anti-ansiedad), catálogo curado de 12 logros sobre hitos financieros reales (reutiliza insights `logro` de FIN-006), nivel/XP **computados sin tabla** (simplificación vs UserLevel de ARQ-0001, a ratificar), 3 retos mensuales v1 (el de categoría usa la lista curada de FIN-007), y **celebración con cero rutas nuevas de notificación** (insights existentes dentro del presupuesto global). Sin IA, sin leaderboards, cero dependencias. §17 con parámetros a ratificar (XP, umbrales, racha semanal). FIN-008 pasa a *En auditoría*.
- 2026-07-05 — Auditor: generado `docs/auditoria/AUD-0008-Gamificacion.md`. Ciclo de menor riesgo hasta ahora (sin IA, sin PII nueva, sin canales de notificación nuevos); confirma que reutiliza correctamente la lista curada de FIN-007 y el patrón on-read de FIN-002. Sin incumplimientos de mandatos vinculantes. Veredicto: **APROBADO CON OBSERVACIONES** (el reto "registro_constante" mezcla semanas ISO con límites de mes calendario sin resolver la ambigüedad de bordes de mes; el algoritmo de asignación de retos "según su situación" no está especificado, pudiendo asignar un reto trivial o imposible según el usuario). FIN-008 pasa a *Auditado — en espera de DEC*.
- 2026-07-05 — CTO: generado `docs/oficial/DEC-0008-Gamificacion.md`. **APROBADO CON AJUSTES.** Verifiqué de forma independiente contra el código real (no solo el informe): `DISCRETIONARY_GLOBAL_CATEGORIES` existe exactamente como se cita en `recommendations.constants.ts` (commit `a56f11e`); `InsightsService.createIfNew()` es genuinamente idempotente por `dedupeKey` (`P2002` → `null`, commit `994b085`); el tope de 1 proactivo/día existe en `proactivity.job.ts`. Se fijan 2 cambios obligatorios: (1) regla explícita de pertenencia de semana ISO a mes calendario para "registro_constante" (una semana pertenece al mes que contiene su jueves, ISO 8601; el número de semanas exigidas es 4 o 5 según el mes, no fijo en 4); (2) criterio de elegibilidad para la asignación nightly de retos (`bajo_promedio` solo con gasto discrecional en 3m; `flujo_positivo` solo si el cashflow no es estructuralmente negativo; `registro_constante` como reto por defecto siempre elegible). Ninguno de los dos hallazgos afecta el mecanismo central (racha/logros/nivel on-read), por lo que no amerita rechazo. Se autoriza iniciar implementación de FIN-008. Gates heredados de FIN-005 (DPA/PIA/producción) sin cambios y no aplican a este módulo. FIN-008 pasa a *Decidido — autorizado a implementación*.
- 2026-07-05 — Desarrollador: implementado FIN-008 y generado `docs/implementaciones/IMP-0008-Gamificacion.md` con **SHA `c2db461b2e1fc3ce95ea4940571081d3e09d8bce`**. Los 2 cambios obligatorios de DEC-0008 §10 cumplidos: regla del jueves ISO con tests de borde (julio=5 semanas, febrero=4, partición sin solapamiento, cambio de año) y elegibilidad de retos con los 3 escenarios testeados + comentario en código. E2E real: listener desbloqueó logros y racha al registrar (XP verificado a mano), job asignó `flujo_positivo` por la cadena de elegibilidad, celebraciones `gami_*` como Insights (cero senders en el módulo — garantía por ausencia de dependencia). 256/256 tests; bundle Android OK. FIN-008 pasa a *Implementado — a la espera de validación del CTO*.
- 2026-07-05 — CTO: **VALIDACIÓN DE IMP-0008 APROBADA — FIN-008 CERRADO.** Re-verificación independiente en checkout aislado (`git clone` + `git checkout c2db461`, no working tree): `week.util.ts` confirma `isoWeeksOfMonth` con la regla del jueves exacta (cambio obligatorio #1); `gamification.service.ts` confirma la cadena de elegibilidad ordenada `bajo_promedio → flujo_positivo → registro_constante` (default universal, cambio obligatorio #2) reutilizando el propio `DISCRETIONARY_GLOBAL_CATEGORIES` de FIN-007; el constructor de `GamificationService` solo inyecta `PrismaService`/`InsightsService` — confirma por ausencia de dependencia que no hay ningún sender nuevo. `npx jest` → **202/202 tests que compilaron pasan** (23/32 suites); 9 no compilan por la misma causa raíz ya documentada en cada ciclo (cliente Prisma sin regenerar en este sandbox por bloqueo de red a `binaries.prisma.sh` — confirmado de nuevo: el `schema.prisma` embebido en el cliente generado ni siquiera existe) — incluida `gamification.spec.ts`, la suite más relevante de este ciclo. Se verificó con bypass de type-check (`isolatedModules`, mismo método usado en ciclos anteriores): **16/16 tests de `gamification.spec.ts` pasan**, más 10/10 de `notification-budget`/`recommendations.service` (regresión, sin afectar). **Se acepta el cierre de FIN-008.** No se encontró ningún defecto atribuible al código o al diseño. Gates heredados de FIN-005 (DPA/PIA/producción) sin cambios y no aplican a este módulo. **Se autoriza iniciar `ARQ-0009` (FIN-009, Monetización Free/Premium + Hardening) cuando se solicite.**
- 2026-07-05 — Arquitectura: generado `docs/arquitectura/ARQ-0009-Monetizacion-y-Hardening.md` (FIN-009, **último ciclo del roadmap**). (A) Monetización: matriz oficial Free/Millo+, `EntitlementsService` como autoridad única (refactor de los gates dispersos de FIN-004/005 con regresión obligatoria), modelo `Subscription` + puerto `PaymentProvider` agnóstico con v1 manual/promo cobrable desde el día 1 y webhook listo para la pasarela que decida el DEC, paywall unificado con funnel. (B) Hardening: decisión formal propuesta sobre cifrado a nivel de campo (aceptación de riesgo razonada: los derivados se almacenan en claro por necesidad de cómputo), throttle fino de auth, purgas pendientes de IMP-0007, `docs/PRODUCCION.md` con checklist único (flags, DPA/PIA, secretos) y guardarraíl Ley 1266 (nunca compartir Score con terceros). §17 con 7 decisiones de negocio para el DEC (canal de cobro, precio, límite de simulaciones, trial, cifrado, helmet, promos). FIN-009 pasa a *En auditoría*.
- 2026-07-05 — Auditor: generado `docs/auditoria/AUD-0009-Monetizacion-y-Hardening.md`. Primer ciclo que maneja dinero real; mismo nivel de escrutinio aplicado a FIN-005. Verificó por `grep` que no existe ningún rol de administrador en todo el backend. Veredicto: **APROBADO CON OBSERVACIONES**, con **dos elevadas a críticas**: (1) el canje de `PromoCode` no especifica incremento atómico de `usedCount` — mismo tipo de condición de carrera que el proyecto ya resolvió para el outbox en FIN-002, no reutilizado aquí; (2) la "activación administrativa auditada" del `ManualPromoProvider` no tiene ningún mecanismo de autorización que la respalde (verificado: cero roles de admin en el código). Observación adicional: el guardarraíl de Ley 1266 se propone solo como comentario, inconsistente con el precedente de DEC-0004 que ya exigió elevar un gate legal procesal a control técnico. Recomendó tratar los Hallazgos 1 y 2 como condición de entrada para IMP-0009, no como pendientes menores. FIN-009 pasa a *Auditado — en espera de DEC*.
- 2026-07-05 — CTO: generado `docs/oficial/DEC-0009-Monetizacion-y-Hardening.md`. **APROBADO CON AJUSTES.** Confirmé de forma independiente (i) que no existe ningún rol de admin en `backend/src/modules/auth/`/`schema.prisma` y (ii) que el patrón atómico `FOR UPDATE SKIP LOCKED...RETURNING` ya existe y está probado en `outbox.dispatcher.ts` — el ARQ tenía el patrón correcto disponible y no lo reutilizó. Trasladé al fundador las 4 decisiones de negocio genuinas del ARQ §17 (no son mías de resolver): **canal de cobro → solo tiendas (IAP/RevenueCat)**, se retira Wompi/Stripe de este ciclo; **precio → no se fija** (el fundador señaló correctamente que el precio depende de costos variables por usuario — Anthropic, WhatsApp Business API — no considerados en el ARQ; se añade telemetría de costo variable como cambio obligatorio #5 antes de fijar precio real); **límite de simulaciones → 5/mes para todos, sin grandfathering**; **trial → 7 días, una vez por usuario**. Decisiones técnicas de mi competencia: cifrado a nivel de campo diferido (riesgo aceptado, argumento técnico sólido) y helmet (ratificado). Se fijan 6 cambios obligatorios, de los cuales **#1 (canje atómico de PromoCode) y #2 (mecanismo mínimo de autorización administrativa, `User.isAdmin` + guard + log auditable) son condición de entrada**: no se autoriza el cierre de FIN-009 sin ambos implementados y testeados, por ser la superficie de fraude más directa del primer ciclo con dinero real. Cambios adicionales: #3 guardarraíl técnico de Ley 1266 (test de regresión dedicado, no solo comentario, mismo estándar de DEC-0004); #4 `hasPremium()` debe leer `Subscription` directamente, nunca la caché `plan`; #6 `maxUses` obligatorio en todo `PromoCode`. Se autoriza iniciar implementación de FIN-009. Gates heredados de FIN-005 sin cambios. FIN-009 pasa a *Decidido — autorizado a implementación*.
- 2026-07-05 — Desarrollo: generado `docs/implementaciones/IMP-0009-Monetizacion-y-Hardening.md`. **FIN-009 entregado** — referencia inmutable commit `827c3e9c7aa4f2bdb45c2349ba15abef2e7cab08` (backend `8fe8287` + frontend `827c3e9`). 6/6 cambios obligatorios de DEC-0009 aplicados: #1 canje atómico (`UPDATE` condicional + `RETURNING`) **probado con concurrencia real** (2 canjes paralelos en `used_count=maxUses−1` → exactamente 1 gana); #2 `User.isAdmin` sin auto-escalación + `AdminGuard` + `AdminActionLog` inmutable (E2E: 403 sin flag, 201 con flag por SQL + fila de log); #3 guardarraíl Ley 1266 como test técnico (`no-third-party-sharing.spec.ts`); #4 `hasPremium()` lee `Subscription` directamente (E2E: trial vencido por fecha → free inmediato pese a caché premium); #5 precio placeholder + telemetría de costo por usuario/mes (Anthropic + WhatsApp) vía `GET /billing/admin/cost-report`; #6 `maxUses` obligatorio a nivel de servicio. Suite 272/272 (34 suites, incluye regresión FIN-001…008); E2E §11.7 10/10 (trial al registro, cuota 5 simulaciones/mes con 6ª → 403 `PREMIUM_REQUIRED`, webhook RevenueCat firma mala/buena, throttle auth 429). **Incidencia cazada por el E2E y corregida antes de la entrega:** `AdminGuard` leía `req.user.sub` pero `JwtAuthGuard` adjunta `{id}` → rechazaba siempre; corregido + 3 tests de regresión. Gates §11.8: DPA ⏳ / PIA ⏳ / producción 🔒 / política de tiendas ⏳. FIN-009 pasa a *Entregado — en validación del CTO*.
- 2026-07-05 — CTO: **VALIDACIÓN DE IMP-0009 APROBADA — FIN-009 CERRADO. ROADMAP ORIGINAL DE ARQ-0001 COMPLETO (9/9).** Re-verificación independiente en checkout aislado (`git clone` + `git checkout 827c3e9`, no working tree), con el mismo nivel de escrutinio que FIN-005 por ser el primer ciclo con dinero real. Verifiqué las **2 condiciones de entrada** obligatorias línea por línea: (1) `promo.service.ts` contiene exactamente el `UPDATE promo_codes SET used_count = used_count + 1 WHERE code_hash=? AND used_count < max_uses AND (expires_at IS NULL OR expires_at > now()) RETURNING` de una sola sentencia — el patrón atómico correcto; (2) `AdminGuard` consulta `User.isAdmin` en BD en cada request, `isAdmin` no se escribe desde ningún endpoint de la API (`grep` confirmó cero rutas de auto-escalación — solo modificable por operación manual en BD) y `AdminActionLog` es insert-only en el schema. Confirmé además el cambio obligatorio #4: `EntitlementsService.hasPremium()` no referencia `UserSettings`/`plan` en ninguna línea — lee `Subscription` directamente. Y el #3: `no-third-party-sharing.spec.ts` existe con el encabezado vinculante de gobernanza. `npx jest` → **202/202 tests que compilaron pasan** (23/34 suites); 11 no compilan por la misma causa raíz documentada en cada ciclo (cliente Prisma sin regenerar en este sandbox) — incluidos `billing.spec.ts` y `no-third-party-sharing.spec.ts`, las dos suites más críticas de este ciclo. Se verificaron con bypass de type-check (`isolatedModules`): **16/16 pasan**, cubriendo entitlements/promo/expiración/AdminGuard y el guardarraíl legal. **Se acepta el cierre de FIN-009 y del roadmap original de Millo.** No se encontró ningún defecto atribuible al código o al diseño. Gates heredados de FIN-005 (DPA/PIA/producción) y de este ciclo (política de tiendas, precio real pendiente de telemetría) siguen sin resolver — **producción con datos reales sigue bloqueada** hasta cerrarlos; son condiciones de negocio/legales, no técnicas. El código está completo y auditado extremo a extremo por los 9 ciclos de gobernanza.
- 2026-07-05 — Fundador: entregó 7 puntos de feedback de producto (segunda ronda, post-roadmap). El CTO verificó cada uno contra el código real antes de evaluar (no por informe): (1) pago anticipado/abono a capital de deuda — **confirmado que NO existe**: `transactions.service.ts` solo descuenta `currentBalance`, nunca recalcula `termMonths`/`monthlyPayment`; el motor de FIN-007 (`simulateExtra`) ya calcula el efecto pero es puramente simulado, sin persistencia — gap real y de bajo riesgo cerrar (reutiliza motor ya probado). (2) nombre de deuda editable — **YA EXISTE** (`UpdateDebtDto` con todos los campos opcionales incluido `name`) — sin trabajo pendiente ahí; seguros asociados al crédito (obligatorios financiados, endosables) — **confirmado que NO existe ningún campo/modelo de seguro** — gap real, específico del dominio colombiano de libranzas. (3) fecha de transacción por defecto hoy y editable — **YA EXISTE** (`AddTransactionScreen.tsx`, `useState(new Date())` + DatePicker); periodo financiero (mes calendario vs. ciclo de nómina) — **confirmado**: TODO el sistema usa mes calendario UTC estricto, sin concepto de día de corte. (4) eliminar registro de presupuesto — **YA EXISTE** en backend (`BudgetService.remove()`, soft-delete idéntico a `Transaction`) — se trata como posible bug de UI/frontend, no amerita ciclo de gobernanza nuevo. (5)(6)(7) dashboard de Inicio — **confirmado**: el widget "¿en qué se te va la plata?" (`monthlyDashboard()`) solo agrega gastos variables, sin `FixedItem`; no existe widget simétrico de ingresos; no hay dashboard de patrimonio neto (el cálculo `networth.util.ts` existe en backend pero no se expone ahí); no hay dashboard de ahorro total ni proyección de interés compuesto (ni `Account` ni `Asset` tienen tasa de interés). Antes de derivar alcance para el arquitecto, el CTO preguntó al fundador 2 decisiones que cambian radicalmente el riesgo/esfuerzo: **(a) ahorro con interés → el fundador eligió proyección/simulación ilustrativa únicamente** (Millo no capta ni ofrece rendimiento — cero riesgo regulatorio nuevo, reutiliza el motor de simulación de FIN-007); **(b) periodo financiero → el fundador eligió acotarlo a Presupuesto/Dashboard únicamente** (Score, Motor, Gamificación, Recomendaciones y Memoria NO cambian, siguen en mes calendario — evita reabrir el roadmap ya cerrado y validado). Con esas decisiones, el CTO segmentó el trabajo en el BACKLOG: **FIN-011 (umbrella, análogo a ARQ-0001)** para que el arquitecto redacte UNA propuesta completa cubriendo todo el alcance aprobado, que luego el CTO segmentará en los ciclos individuales **FIN-012** (abono a capital/pago anticipado real), **FIN-013** (seguros de crédito endosables), **FIN-014** (Dashboard de Inicio v2: patrimonio, ahorro total, ingresos fijo+variable, gastos fijo+variable diferenciados, movimientos completos), **FIN-015** (proyección de ahorro con interés compuesto, acotada) y **FIN-016** (periodo financiero acotado a Presupuesto/Dashboard). El punto de "eliminar presupuesto" queda fuera del backlog de gobernanza como un ticket de verificación/UI, no un ciclo ARQ→AUD→DEC→IMP. FIN-011 pasa a *Pendiente — en espera de ARQ-0011 (umbrella) del arquitecto*.
- 2026-07-05 — Arquitectura: generado `docs/arquitectura/ARQ-0011-Mejoras-Deuda-Presupuesto-Dashboard.md` (umbrella FIN-011, cubre FIN-012…016). Principio rector: reutilizar los motores ya auditados (amortización FIN-002/003, simulación FIN-007, patrimonio FIN-002, cuotas FIN-009) y añadir persistencia/agregación encima — ningún algoritmo financiero nuevo salvo interés compuesto puro. Diseños clave: FIN-012 con puerta de entrada por `debts` (el endpoint crea la `Transaction` internamente en la misma `$transaction` + outbox; preview=recibo reutilizando `simulate-extra`; `Transaction.paymentType` para trazabilidad, default retrocompatible `cuota`); FIN-013 modelo mínimo `DebtInsurance` (financiado/endosado, prima plana, sin cálculo actuarial, SIN tocar el Motor); FIN-014 módulo agregador thin `GET /dashboard/home` (compone servicios existentes en paralelo, `/transactions/dashboard` se conserva sin breaking); FIN-015 octavo escenario puro `proyeccion_ahorro` con disclaimer fijo y cero persistencia (decisión fundador a); FIN-016 `UserSettings.cycleStartDay` 1–28 + util pura `financialPeriod()` que SOLO importan Budget/Dashboard — invariante verificable por grep (decisión fundador b). Orden propuesto: FIN-012 → 016 → 014 → 013 → 015. 6 decisiones abiertas para el DEC (§15): efecto default del abono, liquidación por saldo, primas vs Motor, proyección vs cuota free, rango del día de corte, granularidad de IMPs. **Verificación adicional:** el ticket "eliminar registro de presupuesto" ya existe extremo a extremo (`BudgetScreen.tsx` 🗑️ → `budgetApi.removeFixed` → `BudgetService.remove()`) — cerrado sin código; si el fundador lo percibió como faltante, es asunto de descubribilidad de UI, no de funcionalidad. FIN-011 pasa a *En auditoría — en espera de AUD-0011 y DEC*.
- 2026-07-05 — Auditor: generado `docs/auditoria/AUD-0011-Mejoras-Deuda-Presupuesto-Dashboard.md`. Verificó contra `amortization.service.ts` y `transactions.service.ts` reales, con foco en FIN-012 (el sub-ciclo que el propio ARQ señala como de mayor riesgo). Veredicto: **REQUIERE AJUSTES, acotado a FIN-012** — dos hallazgos elevados a **críticos**: (1) el ARQ propone reutilizar `simulateExtraPayment`/`POST /debts/:id/simulate-extra` para el preview y el recibo de un abono **único** a capital, pero esa función modela una cuota extra **recurrente mensual** (`extraMonthly` se suma en cada periodo del bucle de `buildSchedule`) — usarla mostraría intereses ahorrados inflados; el cálculo correcto es recalcular con `buildSchedule`/`computeMonthlyPayment` sobre el saldo reducido, sin `simulateExtraPayment`; (2) el manejo actual de `pago_deuda` (`findFirst` + `update` calculado, sin bloqueo de fila ni actualización atómica condicional) es vulnerable a una condición de carrera de "última escritura gana" bajo pagos concurrentes — mismo patrón que el proyecto ya resolvió para el outbox y para el canje de PromoCode, no reutilizado aquí; FIN-012 agrava el impacto al extenderlo a recalcular también plazo/cuota. FIN-013/014/015/016 sin hallazgos bloqueantes — pueden avanzar a DEC sin devolverse a Arquitectura. FIN-011 pasa a *Auditado — REQUIERE AJUSTES acotado a FIN-012; en espera de DEC*.
- 2026-07-05 — CTO: generado `docs/oficial/DEC-0011-Mejoras-Deuda-Presupuesto-Dashboard.md`. **APROBADO CON AJUSTES, SEGMENTADO.** Confirmé de forma independiente los 2 hallazgos críticos contra el código: en `amortization.service.ts`, `extraMonthly` se aplica dentro del `while` en cada periodo del cronograma — `simulateExtraPayment` modela una cuota extra recurrente mensual, no un abono único, confirmando que usarla para el preview/recibo de un abono único infla el ahorro de intereses mostrado; en `transactions.service.ts`, el manejador de `pago_deuda` hace `findFirst` + `update` en memoria sin `FOR UPDATE` ni actualización condicional — condición de carrera real, no hipotética. Apliqué el mismo criterio que uso desde FIN-005 para distinguir defecto de mecanismo central de hueco periférico: un error de cálculo que infla intereses ahorrados + una condición de carrera sobre dinero real, en la operación central del sub-ciclo de mayor riesgo del conjunto, **no se resuelve con "corregir en el IMP"** — **NO autorizo el desarrollo de FIN-012** con el diseño actual. Sí autorizo **FIN-013, FIN-014, FIN-015 y FIN-016** a implementación directa, ratificando las 6 decisiones de parámetro del ARQ §15 (efecto default del abono = `reducir_plazo`; pago total sin intereses causados del periodo en curso; primas de seguro NO impactan el Motor en este ciclo; proyección de ahorro consume cuota free de simulaciones; día de corte 1–28 default 1; IMP individual por sub-ciclo) — ninguna requería al fundador, son parámetros técnicos/producto de mi competencia. Para FIN-012 exijo: (1) recalcular con `buildSchedule`/`computeMonthlyPayment` sobre el saldo reducido, nunca con `simulateExtraPayment`, con test de ancla + regresión de que esa función no cambia de semántica; (2) actualización atómica de `Debt.currentBalance` (patrón `UPDATE...WHERE...RETURNING` o `FOR UPDATE`, mismo ya validado en outbox/PromoCode) que cubra tanto las rutas nuevas como el manejador YA EXISTENTE de `pago_deuda` — este ciclo cierra también ese gap preexistente, no solo el nuevo. La Arquitectura debe someter la corrección como revisión acotada de §4.1 (no reescribir todo el ARQ) y el Auditor re-verificar solo esa sección antes de que yo autorice el desarrollo de FIN-012 mediante adenda a este mismo DEC. Gates heredados de FIN-005/FIN-009 sin cambios. FIN-011 pasa a *Decidido — segmentado (4 de 5 sub-ciclos autorizados)*; FIN-013/014/015/016 pasan a *Decidido — autorizado a implementación*; FIN-012 pasa a *Bloqueado — requiere corrección de Arquitectura antes de autorización*.
- 2026-07-05 — Arquitectura: entregada `docs/arquitectura/ARQ-0011-Rev1-Correccion-FIN012.md` (revisión ACOTADA de §4.1, según DEC-0011 §10.3). Corrige los 2 hallazgos críticos: (1) cálculo del abono único con nuevos métodos puros ADITIVOS `remainingSchedule` (cronograma restante con cuota fija dada) y `prepaymentReceipt` (before/after del plan restante; `reducir_cuota` usa el `computeMonthlyPayment` existente, fórmula cerrada exacta) — `simulateExtraPayment` no se toca ni se invoca en FIN-012, y se exige test de ancla a mano + test de NO-inflación (el ahorro del abono único debe ser MENOR que el que reportaría `simulateExtraPayment`) + regresión de semántica; (2) persistencia atómica: `SELECT ... FOR UPDATE` dentro de `$transaction` para `prepay`/`payoff` (el recálculo deriva varios campos del saldo bajo lock) y una sola sentencia `UPDATE ... GREATEST/CASE ... RETURNING` para el manejador YA EXISTENTE de `pago_deuda` (cierra el gap preexistente señalado por el DEC), con test E2E de concurrencia (2 pagos paralelos → saldo exacto, nunca última-escritura-gana). En espera de re-auditoría acotada del Auditor y adenda del CTO a DEC-0011.
- 2026-07-05 — Fundador: **COMUNICADO OFICIAL — corrección del proceso de gobernanza.** Señala una desviación real: aunque `ARQ-0011`/`AUD-0011`/`DEC-0011` (el umbrella FIN-011 en sí) fueron correctos, el Arquitecto continuó diseñando el detalle técnico completo de FIN-012 a FIN-016 dentro de ese mismo documento umbrella, y el CTO permitió que una sola auditoría y una sola decisión cubrieran cinco funcionalidades a la vez — contrario al principio de que cada FIN es una iniciativa independiente con su propio ciclo completo. Regla obligatoria a partir de ahora, con efecto inmediato: un ARQ umbrella puede definir alcance/relación entre funcionalidades futuras, pero **no** su diseño técnico detallado; cada `FIN-XXXX` debe completar `ARQ→AUD→DEC→IMP→Validación→Cerrado` de forma independiente antes de que el Arquitecto inicie el diseño del siguiente; prohibido diseñar/auditar/decidir varias funcionalidades a la vez o implementar sin DEC propio, salvo excepción expresa y documentada del CTO. El CTO añadió la regla permanente **"Un FIN a la vez"** a `docs/GOBERNANZA.md` (tercera regla permanente, tras "Referencia inmutable" y "Vistas minimizadas").
- 2026-07-06 — Fundador: **decisión de regularización extraordinaria del Backlog Inicial V1.0** (dos comunicados sucesivos, el segundo precisando el primero). Confirma que FIN-013/014/015/016 ya fueron implementados antes de existir el ciclo individual por-FIN; no se revierte ni se rehace el código; para cada uno se exige: Arquitecto formaliza el ARQ individual reflejando la implementación real, Auditor realiza auditoría completa e independiente contra el código, CTO emite DEC individual (aprobando, con observaciones, o exigiendo correcciones como acción correctiva, no como repetición del ciclo). Excepción única de transición, sin precedente para desarrollos futuros; desde ahora, prohibido implementar sin DEC individual previo. El fundador recuerda además, en mensaje aparte, que el CTO debe detectar desviaciones de proceso en tiempo real, no solo aprobar documentos al final.
- 2026-07-06 — Arquitectura/Auditoría: se generaron `ARQ-0012` (consolidación numerada de la corrección Rev1 de FIN-012, sin cambios de sustancia) y `ARQ-0013`/`ARQ-0014`/`ARQ-0015`/`ARQ-0016` (formalización individual retroactiva de los diseños ya implementados), junto con `AUD-0012` (verifica que la consolidación no introdujo deriva respecto de `AUD-0011-Rev1`, ya aprobada) y `AUD-0013`/`AUD-0014`/`AUD-0015`/`AUD-0016` (auditorías completas contra el código real ya implementado, no contra promesa de diseño). Los 5 veredictos: **APROBADO**, sin hallazgos críticos en ninguno. El Arquitecto reconoció por escrito la desviación (`docs/oficial/COMUNICADO-2026-07-06-Un-FIN-a-la-vez.md`, sección de reconocimiento) y confirmó que FIN-012 es la única funcionalidad activa hacia adelante.
- 2026-07-06 — CTO: **REGULARIZACIÓN COMPLETA — 5 DEC individuales emitidos.** Antes de decidir, verifiqué de forma independiente en checkout aislado (clon fresco, commit `c511697`, con el mismo estándar de rigor de cada ciclo anterior) los puntos más sensibles de cada módulo: `DebtInsurance.paymentBreakdown` (financiado vs. aparte vs. inactivo) y su test de no-impacto sobre el Motor (FIN-013); `DashboardService.home()` reutilizando `computeNetWorth` real, sin reimplementación, y `/transactions/dashboard` intacto (FIN-014); la fórmula de interés compuesto con `toMonthlyEffectiveRate('EA')` y el disclaimer fijo presente en código (FIN-015); y, la verificación más crítica del conjunto, `grep -rl "financial-period.util" backend/src/modules/` confirmando que **solo** `budget.service.ts`/`dashboard.service.ts` importan la utilidad — cero referencias en Score/Motor/Gamificación/Recomendaciones/Memoria (FIN-016). Corrí la suite completa (221/221 tests que compilan; 12 suites no compilan por la limitación de Prisma ya documentada en cada ciclo) y verifiqué con bypass de `isolatedModules` los 4 specs nuevos (`dashboard`, `debt-insurance`, `financial-period.util`, `simulation-engine`): **29/29 pasan**. **Emití `DEC-0013`, `DEC-0014`, `DEC-0015`, `DEC-0016` (regularización — APROBADO, FIN cerrado cada uno, sin cambios obligatorios) y `DEC-0012`** (autoriza el desarrollo de FIN-012, la única funcionalidad activa: ARQ/AUD ya aprobados dos veces con verificación matemática independiente de los números de ancla). FIN-011 pasa a *Cerrado*; FIN-013/014/015/016 pasan a *Cerrado (regularización extraordinaria)*; FIN-012 pasa a *Decidido — desarrollo autorizado, única funcionalidad activa*. A partir de ahora, ningún FIN nuevo inicia diseño hasta que FIN-012 cierre con su IMP y validación, salvo excepción expresa mía.
- 2026-07-06 — Fundador: **GOBERNANZA OFICIAL DEL PROYECTO MILLA — Versión 2.0.** Formaliza el organigrama completo (Fundador → CPO → CTO → Arquitecto/Auditor) e incorpora un nuevo rol, **CPO (ChatGPT)**, responsable de propuestas funcionales de producto (objetivo/problema/beneficio/impacto/riesgos/casos de uso, sin arquitectura ni código). Introduce el **Blueprint (BP)** del Arquitecto como documento técnico exploratorio previo al Backlog (puede contener muchas más ideas de las que se implementan; no autoriza nada por sí mismo) — el CTO es quien lo divide en `FIN` individuales e incorpora al Backlog. Formaliza explícitamente el paso **VALIDACIÓN** (Auditor + CTO) entre `IMP` y `CERRADO`. Ratifica y ordena por escrito las reglas ya vigentes (una FIN a la vez, sin DEC/AUD agrupados, Backlog exclusivo del CTO) y añade una regla nueva: **ningún agente acepta instrucciones directas que alteren el producto sin pasar por el CTO**, salvo el CPO en sus propuestas estratégicas directas al Fundador. El CTO reescribió `docs/GOBERNANZA.md` como v2.0 completa, preservando íntegras las 3 reglas permanentes previas (referencia inmutable, vistas minimizadas, un FIN a la vez) y la nota de regularización extraordinaria de FIN-013–016. Sin cambios al estado de ningún FIN: **FIN-012 sigue siendo la única funcionalidad activa.**
- 2026-07-05 — Desarrollo: **entregados los 4 sub-ciclos autorizados por DEC-0011**, cada uno con commit e IMP individual (§4.7): **FIN-016** (`40700bc`, IMP-0016) periodo financiero con `financialPeriod()` pura + 8 tests de bordes, CHECK 1–28 en BD, PATCH /budget/period, stepper en Ajustes; invariante (b) verificable por grep — solo Budget/Dashboard la importan. **FIN-013** (`9607c3f`, IMP-0013) `DebtInsurance` mínimo + `paymentBreakdown` (cuota real con seguros financiados/aparte), flujo de endoso, test de no-impacto que recorre el fuente de `financial-engine/` (0 referencias a seguros); E2E: endoso baja el desembolso 552.819→527.819. **FIN-014** (`1af29b1`, IMP-0014) módulo agregador thin `GET /dashboard/home` (patrimonio con `computeNetWorth` auditado, ahorro total, ingresos/gastos fijo+variable por categoría, movimientos completos) + Inicio v2; E2E: netWorth idéntico a /net-worth, ciclo día 20 respetado. **FIN-015** (`6f622e3`, IMP-0015) escenario puro `proyeccion_ahorro` con anclas exactas (1M@8%EA/12m=1.080.000 por definición de EA; tasa 0 = suma simple), disclaimer fijo, delta de métricas = 0, consume cuota free compartida; desviación menor declarada: valor de enum aditivo en `SimulationType` (el ARQ decía "sin BD"); incidencia corregida en el mismo commit: DTO whitelist rechazaba el escenario nuevo (400), cazada por E2E. Suite final **292/292**; typecheck backend+frontend limpios; bundle Android 200 (6.58 MB). Los 4 pasan a *Entregado — en validación del CTO*.
- 2026-07-05 — Auditor: generado `docs/auditoria/AUD-0011-Rev1-Correccion-FIN012.md` (re-auditoría ACOTADA, exclusivamente §4.1 de `ARQ-0011-Rev1-Correccion-FIN012.md`, según mandato de DEC-0011 §10.3 — no se re-audita FIN-013/014/015/016, ya cerrados). Verificó los 2 cambios obligatorios de DEC-0011 §10: (1) `simulateExtraPayment` no se invoca en ningún punto de FIN-012 — se reemplaza por los métodos puros aditivos `remainingSchedule`/`prepaymentReceipt`; verificación matemática independiente a mano confirma que los dos números de ancla propuestos como test son correctos (`reducir_cuota`: 470.734,72×0,8=376.587,79 por proporcionalidad lineal de la cuota fija en el principal; `reducir_plazo`: resolviendo la ecuación de amortización para P=8.000.000/i=1%/A=470.734,72 se obtiene n≈18,72→19 cuotas); (2) `FOR UPDATE` dentro de `$transaction` para las rutas nuevas (justificado: derivan varios campos bajo el mismo saldo) y `UPDATE...RETURNING` atómico para el manejador YA EXISTENTE de `pago_deuda` (justificado: sin recálculo, patrón más barato y suficiente) — elección diferenciada correcta, replicando los dos patrones ya validados del outbox (FIN-002) y del canje de `PromoCode` (FIN-009). Sin hallazgos nuevos. Veredicto: **APROBADO**. Recomienda al CTO autorizar el desarrollo de FIN-012 mediante adenda a `DEC-0011` (no un DEC nuevo), condicionado a que la implementación entregue los 4 tests exigidos (ancla, no-inflación, regresión de semántica, concurrencia) como cierre de `IMP-0012`. FIN-012 pasa a *Re-auditoría acotada APROBADA — en espera de adenda del CTO a DEC-0011*.
- 2026-07-06 — Arquitectura: entregados 5 documentos ARQ individuales que formalizan, cada uno con su propio número (correspondiente al módulo FIN, no al umbrella), diseños ya auditados dentro de `AUD-0011`/`AUD-0011-Rev1` y ya aprobados por `DEC-0011`: `ARQ-0012-Abono-Capital-Pago-Anticipado.md` (consolida ARQ-0011 §4.1 + la corrección Rev1, sin cambios), `ARQ-0013-Seguros-Credito.md`, `ARQ-0014-Dashboard-Inicio-v2.md`, `ARQ-0015-Proyeccion-Ahorro.md` y `ARQ-0016-Periodo-Financiero.md` (los 4 últimos, ya implementados vía IMP-0013/0014/0015/0016, declarados como formalización retroactiva). Cada uno declara explícitamente "no introduce ningún cambio" respecto de lo ya decidido.
- 2026-07-06 — Auditor: **re-auditó los 5 documentos ARQ-0012…0016**, verificando línea por línea contra el código real (no solo aceptando la declaración de "sin cambios") que ninguno introdujo deriva respecto de lo ya aprobado. Hallazgos: `AUD-0012` confirmó que la consolidación reproduce exactamente los métodos `remainingSchedule`/`prepaymentReceipt` y el mecanismo de concurrencia ya aprobados en la Rev1, y que ninguna pieza declarada como "pendiente" existe aún en el código (confirmando que el gap de concurrencia en `pago_deuda` sigue abierto, como se espera). `AUD-0013` verificó el modelo `DebtInsurance`, `paymentBreakdown` (5 casos de test) y el test de no-impacto sobre `financial-engine/` contra el código de `IMP-0013`; única observación menor: la justificación de orden de rutas "para evitar colisión" es imprecisa (las rutas en cuestión no colisionarían en ningún orden por tener distinto número de segmentos), sin efecto funcional. `AUD-0014` verificó que `DashboardService.home()` reutiliza `computeNetWorth` (FIN-002) sin lógica financiera nueva, con ahorro total sin doble conteo y el endpoint clásico `/transactions/dashboard` intacto. `AUD-0015` verificó la fórmula de interés compuesto contra `simulation-engine.ts`, la reutilización de `toMonthlyEffectiveRate` (misma conversión que el motor de amortización), el disclaimer fijo y que el escenario nunca modifica el estado antes/después. `AUD-0016` verificó **por `grep` directo** que únicamente `budget.service.ts` y `dashboard.service.ts` importan `financial-period.util.ts` — la invariante de aislamiento del Score/Motor/Gamificación/Recomendaciones/Memoria (decisión (b) del fundador) se sostiene contra el código real, más la triple defensa de rango (CHECK en BD, DTO, función pura). **Los 5 veredictos: APROBADO.** Ningún hallazgo nuevo, ninguna observación crítica. FIN-013/014/015/016 permanecen *Entregado — en validación del CTO* (esta auditoría no cambia su estado, solo confirma que la documentación individual es fiel); FIN-012 permanece *Re-auditoría acotada APROBADA — en espera de adenda del CTO a DEC-0011* (AUD-0012 no introduce ninguna condición nueva).
- 2026-07-05 — Arquitectura: **generados los ARQ individuales ARQ-0012…ARQ-0016** para restaurar la trazabilidad uno-a-uno del ciclo ARQ→AUD→DEC→IMP (solicitud del fundador: los sub-ciclos tenían IMP propio pero su diseño vivía solo dentro del umbrella ARQ-0011). Cada documento declara explícitamente su naturaleza: ARQ-0013/0014/0015/0016 FORMALIZAN el diseño ya auditado (AUD-0011) y aprobado (DEC-0011) sin introducir ningún cambio — documentación as-built emitida después de la implementación, con referencia a su IMP y commit; ARQ-0012 CONSOLIDA en un solo documento el diseño corregido (ARQ-0011 §4.1 + ARQ-0011-Rev1) con los 6 criterios de aceptación que exigirá IMP-0012 (anclas a mano, no-inflación, regresión de semántica, concurrencia E2E, payoff completo, retrocompatibilidad del pago manual) y conserva su estado real: NO autorizado hasta la adenda del CTO. Los 4 IMP existentes se actualizaron para referenciar su ARQ individual además del umbrella. La tabla del BACKLOG ahora enlaza ARQ individual por fila.
- 2026-07-06 — Auditor: generados los AUD individuales `AUD-0012…AUD-0016` en espejo de los ARQ individuales, verificados CONTRA EL CÓDIGO REAL (no solo contra la promesa de diseño, al estar ya implementados 4 de los 5). Veredictos: **5/5 APROBADO**. AUD-0012 confirma que ARQ-0012 es consolidación fiel de la Rev1 ya aprobada (sin cálculos ni mecanismos nuevos) y reitera la recomendación de adenda a DEC-0011; AUD-0016 verificó con foco especial la invariante (b) del fundador (aislamiento del Score/Motor/Gamificación/Recomendaciones/Memoria) por `git grep` — se sostiene; AUD-0013 señaló una imprecisión de redacción sin efecto funcional (la justificación del orden de rutas `insurances/` vs `:id` — con distinto número de segmentos no colisionarían en ningún orden), corregida en ARQ-0013 en esta misma fecha. La cadena de trazabilidad individual ARQ→AUD→DEC→IMP queda completa para FIN-013/014/015/016; FIN-012 queda con ARQ-0012 + AUD-0012 aprobados, pendiente ÚNICAMENTE de la adenda del CTO a DEC-0011 para iniciar desarrollo.
- 2026-07-06 — Fundador/CPO: **COMUNICADO OFICIAL — corrección del proceso de gobernanza** (`docs/oficial/COMUNICADO-2026-07-06-Un-FIN-a-la-vez.md`; regla resumida ya incorporada a `docs/GOBERNANZA.md`). Desviación identificada en el ciclo FIN-011: el proceso fue correcto hasta el DEC del umbrella, pero el Arquitecto diseñó FIN-012…016 en detalle sin auditorías ni decisiones individuales previas. Regla obligatoria e inmediata: cada FIN es una iniciativa independiente y completa su ciclo entero (ARQ→AUD→DEC→IMP→Validación→Cerrado) antes de iniciar el siguiente; prohibido diseñar por adelantado, auditar en bloque, decidir en bloque o implementar sin DEC propio; excepciones solo por autorización expresa del CTO. El Arquitecto registró recepción y reconocimiento de la desviación en el mismo comunicado. Disposición del estado actual: FIN-013/014/015/016 (entregados, cadena documental individual completa) siguen su validación final; **FIN-012 queda como única funcionalidad activa** — su ciclo continúa en el punto donde está (esperando adenda del CTO a DEC-0011); ningún FIN nuevo se diseña hasta cerrarlo.
- 2026-07-06 — Fundador: **ratifica formalmente la Gobernanza v2.0** propuesta por el CTO, con reconocimiento explícito de la formalización del CPO, el Blueprint, la fase VALIDACIÓN separada, y la preservación de las 3 reglas permanentes. Confirma el cierre definitivo (sin precedente) de la regularización extraordinaria FIN-013–016. Amplía formalmente el mandato del CTO como guardián de la gobernanza técnica: cumplimiento del flujo, detección/detención temprana de desviaciones, coherencia producto↔backlog↔implementación, y **administración de la evolución de la propia gobernanza** — el CTO puede proponer mejoras de metodología/arquitectura/calidad/automatización cuando las identifique, siempre documentadas y sometidas a aprobación antes de entrar en vigor (nunca unilaterales). Confirma el estado operativo: **FIN-012 sigue siendo la única funcionalidad activa**; ningún FIN nuevo inicia hasta su cierre formal (CERRADO). El CTO acusa recibo y continúa la ejecución bajo Gobernanza v2.0.
- 2026-07-06 — CTO: **verificación factual contra la documentación oficial (no contra interpretación).** Ante la afirmación de que el Arquitecto estaba ejecutando "mejoras con base a los DEC hasta el 16" sobre FIN-013–016, revisé `DEC-0013`/`DEC-0014`/`DEC-0015`/`DEC-0016` §10 ("Cambios obligatorios") línea por línea: **las 4 dicen "Ninguno"** — ningún cambio obligatorio, observación pendiente ni acción correctiva quedó registrado en ninguno de los cuatro. Señalé la discrepancia antes de autorizar cualquier modificación sobre módulos ya cerrados. El Fundador confirmó la verificación, retiró la interpretación anterior, y estableció el principio permanente **"la documentación oficial es la única fuente de verdad"** — ninguna decisión, observación o requisito puede asumirse sin cita textual a un documento oficial existente. El CTO añadió esta regla a `docs/GOBERNANZA.md` junto con la Regla de Acciones Correctivas (con la salvedad ya escrita: solo aplica cuando el DEC registra observación explícita).
- 2026-07-06 — CTO: **verificación del estado real del repositorio antes de decidir Escenario A/B.** `git log`/`git diff` contra el último commit conocido (`c511697`) muestran que el HEAD actual (`cef612c`) es exclusivamente documental (comunicado de gobernanza + registro en BACKLOG) — **no existe ningún commit nuevo que toque código de backend/frontend** desde el cierre de FIN-013–016. No hay `IMP-0012` ni evidencia de cambios sobre `debts/`, `transactions/`, `debt-insurance/`, `dashboard/`, `simulations/` o `budget/` más allá de lo ya auditado y cerrado. Conclusión: a la fecha, ni el Escenario A (FIN-012 en curso) ni el Escenario B (modificación de FIN-013–016) tienen artefacto verificable en el repositorio — el trabajo que el Arquitecto reporta no se ha commiteado todavía. Se solicita al Arquitecto entregar el commit correspondiente antes de que el CTO pueda clasificar el escenario y decidir.
- 2026-07-06 — Fundador: **ratifica la verificación del CTO y establece directriz permanente** — el estado oficial del proyecto se determina exclusivamente por artefactos verificables (commits, documentación oficial ARQ/AUD/DEC/IMP/BP, estado del Backlog, estado de rama, evidencia técnica); las conversaciones entre agentes son solo coordinación y no modifican el estado oficial. Instruye mantener el estado operativo actual sin clasificar Escenario A/B hasta que exista evidencia objetiva, y define el orden de verificación obligatorio para cuando llegue el próximo commit: (1) qué archivos se modificaron, (2) a qué FIN pertenecen realmente, (3) si el alcance coincide con la documentación vigente, (4) solo entonces decidir el siguiente paso. Amplía el mandato del CTO: ninguna decisión (`DEC`/Validación/Cierre) puede basarse únicamente en reportes verbales de otro agente — siempre debe verificarse consistencia entre documentación, repositorio, commits y alcance aprobado. El CTO codificó esta directriz en `docs/GOBERNANZA.md` ("El estado oficial se determina solo por artefactos verificables"). Estado operativo sin cambios: FIN-012 sigue como única funcionalidad activa; sin commit nuevo verificable a la fecha.
- 2026-07-06 — CTO: emitido `docs/oficial/DEC-0012-Abono-Capital-Pago-Anticipado.md` — la adenda anunciada por DEC-0011 §10.3, ahora como DEC individual propio conforme a la regla "un FIN a la vez". **APROBADO: se autoriza el desarrollo de FIN-012**, con 4 cambios obligatorios como condición de cierre de IMP-0012 (test de ancla verificado a mano dos veces; no-inflación vs `simulateExtraPayment`; regresión de semántica por snapshot; concurrencia E2E con el mismo estándar del canje de PromoCode). FIN-012 pasa a *Autorizado — en implementación*.
- 2026-07-06 — Desarrollo: **FIN-012 entregado** — referencia inmutable commit `9ea01d63a4e79935a5edceb7d7c0df19d5fbd62d`, `IMP-0012` emitido. 4/4 cambios obligatorios de DEC-0012 §10 con evidencia: (1) anclas a mano — cuota 470.734,72 exacta, `reducir_plazo` 24→19 cuotas, `reducir_cuota` → 376.587,78 por fórmula cerrada (desviación de 1 centavo vs el 376.587,79 manual del auditor, declarada en IMP §6); (2) no-inflación — abono único ahorra 485.212 vs 1.098.793 del recurrente, estrictamente menor; (3) regresión de semántica — snapshot exacto de `simulateExtraPayment` idéntico; (4) **concurrencia E2E contra BD real** — pago manual 300k + abono 300k en paralelo sobre saldo 1M → saldo final exacto 400.000 (nunca last-write-wins). Además: gap PREEXISTENTE cerrado (manejador de `pago_deuda` ahora `UPDATE` atómico condicional con `GREATEST/CASE/RETURNING`); preview=recibo por la misma función pura; plan de amortización regenerado bajo `FOR UPDATE`; `Transaction.paymentType` retrocompatible; UI de abono/pago total en el detalle de deuda. Incidencia corregida pre-commit: "cuota fantasma" de centavos por residuo de redondeo, absorbida en la última cuota (fix solo en el método nuevo; snapshot de la función vieja idéntico). Suite 298/298; E2E 9/9; bundle Android OK. FIN-012 pasa a *Entregado — en validación del CTO*.
- 2026-07-06 — CTO: validación de IMP-0012 (v1, commit `9ea01d6`) en checkout aislado — fondo APROBADO (métodos puros sin `simulateExtraPayment`; `UPDATE...RETURNING` atómico en `pago_deuda`; `FOR UPDATE` en prepay/payoff; 216 tests de lógica en verde) con **un único hallazgo bloqueante**: el test E2E de concurrencia reportado como cambio obligatorio #4 no estaba en el commit (corrió como script efímero — evidencia no reproducible). Instrucción: sin tocar lógica/diseño/alcance, commitear el test y reenviar IMP-0012 con SHA actualizado.
- 2026-07-06 — Desarrollo: **IMP-0012 reenviado (v2)** — referencia inmutable `5e8d32fd767f73e8e6943721ded4470a3436ec3b`. El commit añade EXCLUSIVAMENTE la evidencia exigida: `backend/test/fin012-debt-concurrency.e2e-spec.ts` (spec de jest versionado con config propia `test/jest-e2e.json` y script `npm run test:e2e`; levanta la app real en puerto efímero contra la BD real, sin dependencias nuevas — fetch nativo). 3 tests: pago manual + abono en paralelo → saldo EXACTO 400.000; dos abonos paralelos → 500.000 exacto con `FOR UPDATE` serializando; payoff + pago en paralelo → saldo 0/`pagada`/nunca negativo. Resultado: **3/3 PASS** reproducible; suite unitaria intacta 298/298 (la config separada no la contamina). IMP-0012 §3.4/§5 actualizados con el hallazgo y su corrección. FIN-012 pasa a *Reenviado — en re-validación del CTO*.
- 2026-07-11 — Arquitectura: generado `docs/arquitectura/ARQ-0017-UX-Login-Dashboard.md` (FIN-017, origen "Mejora de revisión de producto", Gobernanza v3.5 §27). Cumple el requisito del CPSAO de ≥2 alternativas comparadas por cambio: P1 Login (propuesta de valor compacta vs mini-recorrido — recomendada A), P2 protagonista del Dashboard (hero "Te queda este ciclo" vs hero Score vs solo jerarquía visual — recomendada A), P3 interpretación (línea interpretativa server-side con umbrales ya auditados de FIN-004 vs semáforo con detalle al tocar — recomendada A), P4 lenguaje (reemplazo directo vs término+traducción — recomendada B con glosario propuesto). Alcance estrictamente acotado a las 4 prioridades y 2 pantallas; sin BD; backend solo si el DEC aprueba 3-A (campo aditivo `interpretation` en /dashboard/home). Criterios de aceptación medibles por captura real (test de 5s, un único hero, interpretación visible sin interacción, glosario verificable por grep) + regresión completa. FIN-017 pasa a *En auditoría — en espera de AUD-017 y DEC-017*.
- 2026-07-11 — Arquitecto: **Lote 02 de capturas entregado** (`docs/producto/capturas/LOTE-02.md` + 4 PNG en `lote-02/`): Mis deudas, Detalle de deuda (con abono a capital FIN-012 y seguros FIN-013 visibles), Simulador (escenario de ahorro preseleccionado vía CTA del Inicio) y Copiloto (estado real: modo básico pre-consentimiento, con recomendaciones e insights del motor real). Mismo método reproducible del Lote 01. Nota operativa: el API de las capturas web pasó de IP LAN a `localhost` (la IP de la red cambia entre sesiones y rompía las llamadas; localhost es estable para captura — el QR del teléfono sigue usando la IP LAN vigente).
- 2026-07-11 — Arquitectura: `ARQ-0017` actualizado a **v1.1** por directriz del CTO ("diseñar la mejor evolución posible, no implementar mecánicamente las 4 observaciones"): se añade §4.6 con la **composición integrada** de ambas pantallas (wireframes del diseño objetivo si el DEC aprueba las recomendadas — mismo blueprint contra el que se tomarán las capturas de cierre) y §4.5 con una decisión importante que estaba implícita: el tratamiento del bloque de gamificación (compactar a una línea tocable — recomendada — vs sacarlo del Inicio, con el riesgo de matar el mecanismo de hábito de FIN-008). Encabezado versionado conforme a v3.5. Sin cambios de alcance. Sigue *En auditoría — en espera de AUD-017*.
- 2026-07-11 — Arquitectura: **ARQ-0017 v1.2** — correcciones puntuales de DEC-0017 entregadas (§4.7 + §11 corregido): (1) Hallazgo 4 — Login ampliado a los 4 pilares con textos que pasan la prueba §29.2 ("💳 Sal de tus deudas con un plan · 💰 Cuánto puedes gastar, siempre claro · 🩺 Tu salud financiera en un número · 🤖 Un copiloto que te explica"); (2) Hallazgo 1+2 — interpretación de "Deuda total" por la **ruta (a)**: cuotas pagadas del ciclo / ingreso del ciclo (cifras que el home ya expone), cero reutilización del DTI del Score → sin mezcla de cadencias que explicar, cumple §29.1 por construcción (la ruta (b) se descarta: no hay redacción que evite exigir la comprensión mes-calendario-vs-ciclo); cortes 30/45 compartidos con FIN-004 como constantes de compilación; (3) Hallazgo 3 — §11 corregido: ninguna dependencia nueva en runtime; (4) textos definitivos de las 3 interpretaciones (flujo/deuda/ahorro) en tabla verde/amarillo/rojo, con reglas transversales: montos en pesos sin decimales, cero jerga, sin referencias a calendario/ciclo/DTI en texto visible, y omisión de la línea si falta el dato (nunca un texto que genere una pregunta). FIN-017 pasa a *Corregido — en confirmación puntual del CTO para habilitar IMP-0017* (DEC-0017 §7.2, sin nueva vuelta de auditoría).
- 2026-07-11 — Arquitecto: **P1 y P3-Deuda implementadas tras las confirmaciones del CTO** + **ARQ-0017 v1.3**. (1) Login (P1 confirmada): propuesta de valor + 4 pilares en vivo, tagline como firma emocional al pie — captura real `docs/producto/capturas/fin-017/despues-01-login.png`. (2) Hallazgo nuevo del CTO sobre P3-Deuda resuelto por la ruta preferida: la tarjeta de Deuda total muestra UNA sola cifra de cuota — "$X pagado este ciclo" (`debtPayments`, el MISMO dato de la interpretación) en lugar de las cuotas programadas del resumen; la cuota programada sigue visible en Mis deudas y el detalle. (3) Interpretación de deuda activada (ruta a): "De cada $100 que te entraron, $N se fueron en cuotas — vas bien / ya pesan bastante / se están comiendo tu ingreso", omitida si no hay pagos en el ciclo (§29.1). (4) Corrección de precisión en el ARQ: los cortes reales de FIN-004 son **0,20/0,35** (`health.service.ts:129`), no los "30/45" citados en v1.2 — la implementación comparte la constante exportada `DEBT_RATIO_CUTS` desde `score.util.ts` (compilación, cero llamadas al Score). Verificado en vivo: "2 deuda(s) · $493.000 pagado este ciclo · 🟢 De cada $100 que te entraron, $6 se fueron en cuotas — vas bien" (cifras coherentes: 493k/8,75M). Suite 299/299. Capturas después en `capturas/fin-017/`. FIN-017 queda a la espera de la confirmación final del CTO sobre §4.7.5 para emitir IMP-0017.
- 2026-07-11 — Desarrollo: **IMP-0017 emitido** (autorizado por el CTO tras confirmar `e914e85` contra el repo y las capturas de forma visual). Referencia inmutable `e914e85` (código en `d93ab60` + `e914e85`). Cumplimiento §13 con evidencia: test de 5s (captura del Login con 4 pilares), hero único, interpretación visible sin interacción en flujo/deuda/ahorro, glosario verificado por grep (todas las cadenas nuevas presentes, "Flujo estimado" = 0), capturas antes (`lote-01/`) / después (`fin-017/`), suite 299/299, typecheck limpio, bundle Android 200. Criterios §29 aplicados y documentados. Incidencias registradas: hallazgo del CTO (mezcla programado/pagado en la tarjeta de Deuda — resuelto con una sola cifra) y auto-corrección de cortes 30/45→0,20/0,35. FIN-017 pasa a *Entregado — en cierre del CTO*.
- 2026-07-11 — **CTO: cierre formal de FIN-017.** Validación independiente completa contra el repositorio antes de cerrar (regla de correspondencia exacta DEC→IMP→Código→Evidencia, Gobernanza v3.2): grep directo de las 7 cadenas declaradas en `IMP-0017` §3 (glosario y pilares del Login) con conteos exactos coincidentes; lectura completa de `dashboard.service.ts` (`interpretDebt`, omisión por dato faltante, `DEBT_RATIO_CUTS` importada de `score.util.ts` con cortes reales 0,20/0,35) y de `DashboardScreen.tsx` (cifra única `debtPayments` en la tarjeta de Deuda); visualización directa (Read de imagen) de `despues-01-login.png` y `despues-02-dashboard.png` — ambas capturas reales, coherentes con lo declarado. Limitación de entorno conocida (ICU 60/70, sin Docker/root en este sandbox) impide ejecutar la suite 299/299 en vivo — se acepta la verificación estática exhaustiva, mismo precedente que FIN-012 y FIN-013–016. **FIN-017 queda CERRADO.** Próximo paso, ya acordado con el CPSAO: revisión integral de la experiencia completa de Inicio (capturas de scroll completo, autocrítica documentada del Arquitecto) antes de decidir entre abrir `FIN-018` o avanzar a la Experiencia de Salud.
- 2026-07-11 — Arquitecto: **Recorrido integral de Inicio entregado** (`docs/producto/capturas/revision-inicio/RECORRIDO-INICIO-001.md` + 2 capturas de scroll completo: Login 780×1688 y Dashboard 780×4040, app real con usuaria demo). Método del CPSAO aplicado: recorrido de principio a fin como usuario nuevo con las 5 preguntas por sección. **11 observaciones documentadas SIN decisión** (L1–L2 en Login; D1–D9 en Dashboard), destacando: jerarquía de CTAs invertida para usuario nuevo (L1), interpretación verde del hero que repite la cifra (D1), información de deuda partida en dos lugares (D3), totales fijos duplicados (D5), "Próximos pagos" sin fecha (D6), sección de ingresos 100% "Sin categoría" como ruido condicional (D7), y cierre del recorrido en densidad en vez de claridad (D8/D9). Síntesis honesta: mitad superior con claridad genuina; el arco completo va de claridad a densidad. Todo queda para evaluación del equipo (Auditor, CTO, CPSAO); nada autoaprobado ni descartado.
- 2026-07-11 — **CTO: verificación independiente del recorrido integral.** Visualicé directamente (Read de imagen) `scroll-02-dashboard-completo.png` y confirmé una por una las 9 observaciones del Dashboard contra la captura real (duplicación literal de "$6.190.000" en hero+interpretación, gamificación entre hero y Deuda, Deuda/Próximos pagos separados por 3 bloques, alturas desiguales Patrimonio/Ahorro, "$1.515.000"/"$4.200.000" fijos repetidos dos veces, Próximos pagos sin fecha visible, "Sin categoría · 100%" sin valor, cola de 8 movimientos sin cierre). Confirmé además en `debts.service.ts` (líneas 206-215) que `upcoming` ya incluye `dueDate` en el backend — el dato existe, D6 es solo de UI. Entregué mi lectura técnica (severidad/costo) al CPSAO para su decisión de alcance de producto.
- 2026-07-11 — **CPSAO: apertura de `FIN-018`** (origen: Mejora de revisión de producto), alcance comprometido L1, D1, D3, D5, D6, D7. D2/D8/D9 no entran todavía — el CPSAO pide que Arquitectura los reevalúe en `ARQ-018` bajo la pregunta más amplia: si la mitad inferior del Dashboard sigue ayudando a decidir o se vuelve pantalla de consulta, y si alguna sección compite por atención sin aportar el mismo valor de decisión. Principio reafirmado: "cada bloque de Inicio debe ayudar a entender mejor la situación o decidir mejor qué hacer; si no cumple ninguna, se cuestiona si pertenece a la experiencia." L2 y D4 se resuelven en paralelo como correcciones triviales (Gobernanza §0), sin ciclo de gobernanza propio. "Un FIN a la vez" se mantiene — Salud sigue sin iniciar.
- 2026-07-11 — Arquitectura: generado `docs/arquitectura/ARQ-0018-Evolucion-Inicio.md` (FIN-018, origen "Mejora de revisión de producto"). Las 6 piezas comprometidas con ≥2 alternativas comparadas y recomendación: L1 (Crear cuenta primario — argumento clave: la sesión persiste, el recurrente casi nunca ve el Login; alternativas peso igual y jerarquía contextual), D1 (interpretación verde en formato "$ de cada $100" — información nueva y familia coherente con la de deuda), D3 (recomendada B: integrar el próximo pago EN la tarjeta Deuda total con fecha — compacta el recorrido y resuelve D6 de paso; alternativa A solo reordena), D5 (quitar filas duplicadas — quirúrgica), D6 (fecha corta por fila si D3-A; absorbida si D3-B), D7 (recomendada B: invitación accionable "toca para organizarlos" en vez de ocultar — sección estable). **§5 — análisis amplio exigido por el CPSAO**: tabla bloque-por-bloque bajo el estándar "entender mejor o decidir mejor" — Movimientos recientes identificado como el bloque más "pantalla de consulta" (raíz de D8/D9); propuesta ANALÍTICA no comprometida: compactarlo a 3–4 filas + "Ver todos →" como cierre natural del recorrido (resolvería D8 y D9 sin cierre artificial); D2 con ambas lecturas documentadas (empuje del hábito arriba vs cierre motivacional abajo) como decisión de narrativa del CPSAO. Correcciones triviales L2/D4 aplicadas por fuera en el mismo día (columna de íconos fija; stretch del par Patrimonio/Ahorro), verificadas en vivo. FIN-018 pasa a *En auditoría — en espera de AUD-018*.
- 2026-07-11 — Auditor: generado `docs/auditoria/AUD-0018-Evolucion-Inicio.md`. Verificó las 6 piezas comprometidas (L1, D1, D3, D5, D6, D7) contra las capturas reales de scroll completo (todas confirmadas, incluida la duplicación literal de "$6.190.000" y los totales fijos repetidos) y contra el código (`auth.store.ts` con SecureStore+refresh sostiene L1; `debts.service.ts` expone `upcoming[].dueDate` que `DashboardScreen.tsx` no renderiza, confirmando D6). Verificación destacada: confirmó en `dashboard.service.ts` que el Hallazgo crítico de `AUD-017` (mezcla de periodo/numerador del DTI) está genuinamente resuelto — `interpretDebt`/`interpretCashflow` calculan con `debtPayments`/`incomeTotal` del propio ciclo, reutilizando de `score.util.ts` solo los cortes estáticos `DEBT_RATIO_CUTS` (cero llamadas a `HealthService`/Score) — implementa exactamente la "ruta (a)" de `DEC-0017 §5.1` y cumple §29.1/§29.2 (Gobernanza v3.7) de forma demostrable; D1 extiende el mismo patrón sin reabrir el problema. Único hallazgo, no bloqueante: la mitigación que el propio ARQ declara para el riesgo de D3-B ("pestaña Deudas... línea del más urgente siempre visible") no está del todo respaldada — verificado que `DebtsListScreen.tsx` muestra `payoffDate` (fecha de término), no `nextDueDate` (próximo pago) por deuda. Veredicto: **APROBADO CON OBSERVACIONES**. FIN-018 pasa a *Auditado — APROBADO CON OBSERVACIONES; en espera de DEC-018*.
- 2026-07-11 — Desarrollo: **las 5 piezas autorizadas por DEC-018 implementadas y verificadas en vivo** (L1-A, D1-A, D3-B con D6 absorbida, D5-A, D7-B) + ARQ-0018 v1.1 con la corrección de redacción del §10 pedida por el CTO (la lista de Deudas muestra `payoffDate`, no `nextDueDate`; mejora futura registrada fuera de ciclo). Suite 299/299. **Observación nueva para el equipo, destapada por D3-B/D6**: al hacer visible la fecha, el "próximo pago" de una deuda SIN pagos registrados muestra una fecha vencida ("vence 28 de abr" en julio) — `nextDueDate` no avanza solo con el paso del tiempo, únicamente con pagos. No es regresión (la sección vieja usaba el mismo dato, solo que sin mostrar la fecha); corregir el avance de `nextDueDate` pertenece al dominio de deudas/recordatorios, fuera del alcance de FIN-018 — se presenta para evaluación, no se auto-corrige. IMP-0018 queda a la espera de: (a) confirmación del CTO sobre la corrección §10 y (b) respuesta del CPSAO sobre la propuesta §5 (Movimientos recientes).
- 2026-07-11 — Desarrollo: **IMP-0018 emitido — FIN-018 entregado completo (7/7 piezas)**. Referencia inmutable `82caa0d332b918562fe7be6aaf8deccdeee57a94` (código en `3fb4072` + `82caa0d`). Pieza 7 implementada con los 2 criterios del CPSAO: 4 filas densas en una tarjeta y enlace de transición NO genérico — "Ver el detalle completo de tus movimientos →" (alternativa "Ver todos" descartada y documentada en ARQ §4.8). **El recorrido completo pasó de 2020px a 1490px (−26%) sin perder información.** Cumplimiento §13 completo con capturas de scroll completo antes (`revision-inicio/`) y después (`fin-018/`), grep de criterios (📌=0, sección Próximos pagos=0 en UI, enlace=1, CTAs del Login en orden), suite 299/299, typecheck y bundle Android OK. **Juicio razonado del criterio 2 documentado en IMP §4**: el arco completo ahora sostiene la comprensión (cada bloque responde una pregunta en orden de urgencia; el cierre es transición deliberada), con dos reservas honestas fuera del alcance: la fecha vencida de `nextDueDate` (única línea que puede generar pregunta) y la ubicación de la gamificación (D2, decisión de narrativa pendiente del CPSAO). FIN-018 pasa a *Entregado — en VALIDACIÓN (Auditor) y cierre del CTO*.
- 2026-07-11 — Desarrollo: **tercera entrega de FIN-018 — IMP-0018 v1.1** (referencia inmutable `8c42edf5cc2118e3ca71a7f2614532a1be4df8d7`; código en `3fb4072` + `82caa0d` + `8c42edf`). (1) **Pieza 8 — nextDueDate**: el UPDATE atómico del pago normal avanza la fecha hasta la próxima ocurrencia FUTURA conservando el día ancla (con k meses de atraso salta k+1; "+1 mes" a secas seguiría vencida) y la limpia al saldar; las rutas de FIN-012 ya eran correctas. E2E versionado `fin018-next-due-date.e2e-spec.ts` 3/3 contra BD real (vencida→futura con día ancla, futura→+1 mes exacto, saldada→NULL) + verificación con datos reales de la demo: la tarjeta pasó de "vence 28 de abr" a "vence 28 de jul" al registrar la cuota — captura final retomada con ese estado. Semántica declarada (ARQ §4.9): un pago normaliza la fecha visible; mora acumulada fuera de alcance. (2) **Análisis narrativo (ARQ §5.1)**: tabla responde/empuja por bloque — el orden ya guía; el hueco está localizado en hero y deuda ("responden pero no proponen": la pantalla dice "te quedan $71 libres" y "tu tarjeta está al 26% EA" sin conectar ambas frases); propuesta mínima documentada SIN implementar (línea condicional "Tienes margen — simula un abono →" hacia el detalle donde FIN-012 ya existe) junto a la alternativa de no añadir nada — decisión de producto CPSAO/CTO. E2E 6/6; suite 299/299. FIN-018 pasa a *Entregado v1.1 — en Revisión de Comprensión (Auditor) y cierre del CTO*.
- 2026-07-11 — Auditor: **primera Revisión de Comprensión ejecutada** (`docs/producto/rc/RC-0001-Inicio.md`, Gobernanza v3.8 §30). Verificación previa: confirmó en `transactions.service.ts` que la pieza 8 (`nextDueDate`) usa `GREATEST(1, meses_transcurridos+1)` sobre `age(now(), next_due_date)` y recalculó a mano el caso de la captura (28 abr + 3 meses = 28 jul, coincide); confirmó que la propuesta narrativa de §5.1 está solo documentada, no implementada. **Nota metodológica registrada explícitamente**: sin participantes humanos reales disponibles en el entorno del Auditor, ejecutó un walkthrough cognitivo estructurado declarado como método sustituto — no equivalente a una sesión real — y recomienda formalmente programar una con el CTO/CPSAO antes de tratar los hallazgos como definitivos, evitando fabricar datos de participantes inexistentes (regla "estado oficial por artefactos verificables"). 3 hallazgos de comprensión sobre `despues-scroll-02-dashboard.png`, ninguno bloqueante ni cubierto antes por AUD-017/018: (1) "ciclo" (hero + Deuda total) sin explicación visible; (2) ícono de deuda inconsistente entre la tarjeta Deuda total (🏦) y Movimientos recientes (💳) en la misma pantalla; (3) "Abono a capital (reduce plazo)" no autoexplicativo junto a "Cuota tarjeta" para alguien sin vocabulario financiero. Login sin hallazgos. FIN-018 pasa a *RC-0001 completada — en espera de decisión del CTO/CPSAO sobre los hallazgos, D2/narrativa, y la sesión real recomendada, antes del cierre*.
- 2026-07-11 — CTO: **verificación independiente de RC-0001 — Hallazgo 2 no se sostiene.** Antes de aceptar el documento, confirmó los 3 hallazgos contra el código: Hallazgos 1 y 3 exactos; Hallazgo 2 (ícono de deuda 🏦 vs 💳 inconsistente) refutado por `grep` de `DashboardScreen.tsx` — cero resultados de 🏦; el ícono real de la línea "Próximo" es 📅 (fecha), y 💳 sí se usa de forma consistente para "deuda" (título "💳 Deuda total" y filas de `pago_deuda` en Movimientos recientes, mismo mapa de emojis). Pidió al Auditor corregir o retirar el hallazgo en una v1.1, sin editarlo él mismo (independencia de roles). Auditor: verificó la corrección de forma independiente contra `frontend/src/screens/DashboardScreen.tsx` (líneas 103-127) — confirmó que el CTO tiene razón (origen del error: confusión visual entre 📅 y 🏦 a la resolución de la captura) — y publicó `RC-0001-Inicio.md` v1.1, reteniendo el hallazgo original tachado con nota de retiro (no lo eliminó del documento) y actualizando el resumen a 2 hallazgos válidos (1 y 3). FIN-018 pasa a *RC-0001 v1.1 (corregida) — en espera de decisión del CTO/CPSAO*.
- 2026-07-11 — Desarrollo: **cuarta iteración de FIN-018 entregada — IMP-0018 v1.2** (referencia inmutable `8016bfd26a22861791c78f993ca9321fe13ef7bb`; código en `3fb4072`+`82caa0d`+`8c42edf`+`8016bfd`). Los 4 puntos del CPSAO: (1) **D2** — gamificación al CIERRE del recorrido, decidida por el recorrido mental del usuario (la racha no responde "¿cómo estoy?"; refuerza el hábito que sostiene las respuestas) — orden final: movimientos → "Ver el detalle completo →" → 🔥 racha; (2) **puente narrativo** implementado: "💡 Tienes margen: adelanta un pago y ahorra intereses →" (condicional verde+deuda, navega al detalle con el simulador de FIN-012, navegación verificada en vivo); (3) **"ciclo" eliminado del vocabulario visible** — fechas concretas en su lugar ("Te queda para gastar · hasta el 31 de jul", "pagado desde el 1 de jul"); verificado: la pantalla completa no contiene la palabra; (4) **"abono a capital" en beneficio-primero** — notas nuevas "Adelanto a tu deuda (terminas antes/baja tu cuota)" y "Pagaste toda tu deuda"; el término se conserva en el detalle con subtítulo llano (las filas históricas conservan la nota vieja: son dato, no plantilla). Criterio rector respondido en ARQ §4.10 e IMP. Suite 299/299; E2E 6/6; capturas finales de scroll completo retomadas. FIN-018 pasa a *Entregado v1.2 — en validación del CTO para programar la sesión real de Revisión de Comprensión*.
- 2026-07-11 — Auditor: **diseño completo de la sesión real de RC-0001** (`docs/producto/rc/RC-0001-Inicio.md` §7, v1.2→v1.3), por encargo del CTO con el protocolo del CPSAO — **aplicable solo a esta ejecución**, precisión del Fundador de que no queda codificado todavía como estándar permanente de Gobernanza. v1.2: separación del canal de reclutamiento de la Alpha (consentimiento/registro propios); guion paso a paso Login→Dashboard con transición a cuenta demo precreada; formato de registro por participante (tiempo hasta identificar el dato más importante, primer elemento de atención, primera duda, primer intento de toque, síntesis final en palabras propias, próxima acción nombrada espontáneamente); rol del moderador limitado a observar y preguntar, nunca explicar. v1.3 (precisión adicional del CPSAO): se agrega la indagación obligatoria del razonamiento ("¿qué te hizo pensar eso?"/"¿qué esperabas encontrar?"/"¿qué palabra o dato te llevó a esa conclusión?") antes de corregir cualquier duda o interpretación errónea, con una fila nueva en la ficha de registro para capturarlo, y una distinción explícita en el análisis posterior (§7.5) entre defecto real de la pantalla vs. expectativa de producto distinta a la diseñada — insumo para experiencias futuras, no solo corrección de esta FIN. No requiere adendo de `DEC-0018` (precisión de diseño de sesión, no decisión de gobernanza). Diseño listo; sin ejecutar — el Auditor avisa al CTO antes de correr sesiones con participantes reales. FIN-018 permanece *Entregado v1.2 — en validación del CTO; diseño de sesión real de RC-0001 listo para autorización*.
- 2026-07-11 — **CTO: autoriza el diseño de RC-0001 para ejecutar** con participantes reales (4-6 sugeridos), sujeto a reclutamiento fuera del entorno de desarrollo. FIN-018 pasa a *Diseño de sesión real autorizado — en espera de reclutamiento*.
- 2026-07-11 — Auditor: señaló, antes de dar por lista la ejecución, la limitación de fondo de su entorno: sin canal en vivo (llamada/video/chat en tiempo real) para moderar personalmente a un participante humano — misma limitación de origen que motivó el walkthrough cognitivo de la v1.0 de `RC-0001`, ahora aplicada a la ejecución. Propuso al CTO dos rutas: (a) que quien modere en persona (Fundador/CPSAO/designado) siga el guion de §7.3 tal cual y entregue grabación/transcripción/notas para que el Auditor las documente en la ficha de §7.4 y consolide en §8, o (b) usar un canal en vivo del entorno si existiera uno no identificado todavía. Diseño y disposición del Auditor sin cambios; a la espera de que se defina el canal de entrada antes de la primera sesión.
- 2026-07-11 — **CTO: FIN-018 CERRADA.** El CPSAO reprogramó la ejecución de la sesión real de `RC-0001` como parte de una futura **RC integral** al final de toda la hoja de ruta UX (valida el recorrido completo por las 6 experiencias, no una pantalla aislada) — resuelve de paso la limitación de canal en vivo que señaló el Auditor, dado que esa RC integral se planificará con su propio mecanismo de ejecución. El diseño de `RC-0001-Inicio.md` v1.3 (guion, ficha de registro, indagación del razonamiento) queda preservado para reutilizarse en ese momento — no se descarta. Con la evidencia ya validada (8 piezas de las 4 iteraciones verificadas contra código/capturas; los 2 hallazgos válidos de la revisión de comprensión de primer nivel — "ciclo" y "Abono a capital" — corregidos y confirmados), se cierra `FIN-018`. Ver `docs/oficial/DEC-0018-Evolucion-Inicio.md` §14. **Habilita abrir la Experiencia de Salud.** FIN-018 pasa a *Cerrado*.
- 2026-07-11 — **CPSAO: reprograma la sesión real de RC-0001 como parte de una futura RC integral** al final de toda la hoja de ruta UX (Inicio→Salud→Presupuesto→Deudas→Simulador→Copiloto) — la comprensión real de un usuario depende del recorrido completo por la app, no de una pantalla aislada. Se suspende el reclutamiento aislado para Inicio; el diseño de `RC-0001-Inicio.md` v1.3 queda preservado, no descartado, para reutilizarse en esa validación integral. Pide continuar con la hoja de ruta ya establecida. **Consultado el Fundador sobre el cierre de FIN-018** (dado que `DEC-0018` condicionaba el cierre al resultado de esa sesión real): decisión **cerrar FIN-018 ahora**, con la evidencia ya validada (8 piezas + 4 iteraciones + RC-0001 walkthrough de primer nivel, ambos hallazgos válidos ya corregidos), dejando la sesión con participantes reales para la RC integral de cierre de hoja de ruta. Ver `DEC-0018` §14. **FIN-018 CERRADA.** Habilita abrir la Experiencia de Salud.
- 2026-07-11 — **CPSAO: apertura de `FIN-019` — Experiencia de Salud**, antes de solicitar `ARQ-019`. Objetivo obligatorio: la pantalla debe responder 3 preguntas (¿cómo está mi salud financiera hoy? ¿por qué obtuve ese resultado? ¿qué debería hacer para mejorarla?) — si alguna queda sin responder, la experiencia está incompleta. Mismo principio de Inicio (cada bloque justifica su existencia ayudando a entender o decidir). Requisito previo al ARQ: el Arquitecto debe responder explícita y argumentadamente "¿qué debe sentir un usuario al abandonar la pantalla Salud?" antes de diseñar. Principio de continuidad narrativa entre experiencias: Inicio responde "¿cómo estoy?"; Salud debe responder "¿por qué estoy así?".
- 2026-07-11 — Arquitecto: responde la pregunta de intención — "comprensión con agencia": el usuario debe salir sintiendo que su situación tiene explicación y tiene palanca ("entiendo por qué estoy así y sé qué mueve la aguja"), nunca calificado. Argumenta que esa emoción es el residuo natural de responder bien las 3 preguntas obligatorias (confusión si falla el "por qué", impotencia si falla el "qué hago", juicio si falla el tono de cualquiera), y la conecta con la continuidad narrativa: Inicio deja al usuario sabiendo "cómo estoy"; Salud debe dejarlo sabiendo "por qué estoy así y qué lo cambia" — el estado mental que necesitan las experiencias siguientes (orientado a actuar, no consolado ni asustado). Adelanta 4 consecuencias de diseño para `ARQ-019`: el Score nunca viaja sin sus causas visibles; todo rojo/amarillo lleva su palanca de acción al lado; el tono califica estados, nunca personas; el caso sin Score (cold-start) tiene su propia expectativa asignada, nunca se siente como exclusión. **CPSAO aprueba la intención como base del ARQ-019** y añade una directriz: no basta con explicar el Score — el usuario debe salir sabiendo cuál es **la acción de mayor impacto** que puede tomar para mejorarlo (al menos una, clara). Pregunta rectora para todo el diseño: "¿qué decisión concreta quiero que el usuario pueda tomar inmediatamente después de recorrer esta pantalla?" — si no es evidente, el diseño sigue evolucionando. Principio de la experiencia: "la mejor pantalla de Salud no es la que mejor explica el Score, es la que logra que el usuario quiera mejorar su salud financiera porque entiende que puede hacerlo." Arquitectura procede con captura de scroll completo + recorrido crítico del estado actual de Salud, y luego `ARQ-019` con la intención como §0.
- 2026-07-11 — Arquitecto: **recorrido crítico de Salud entregado como insumo de ARQ-019** (`docs/producto/capturas/revision-salud/RECORRIDO-SALUD-001.md` + 3 capturas reales de scroll completo: estado base, indicador rojo expandido, historial abierto). Evaluado contra las 5 preguntas del CPSAO, las 3 preguntas obligatorias de FIN-019 y la intención declarada ("comprensión con agencia"). **Veredicto del estado actual**: ¿cómo estoy? parcial (número+banda sin escala); ¿por qué? NO respondida (el Score viaja solo — los 4 pilares con pesos existen en el API y no se muestran, S1/S9); ¿qué hago? escondida (acciones y CTA del simulador viven tras "Toca para ver detalle", S4 — contradice el precedente 3-A de FIN-017). 9 observaciones documentadas SIN decisión (incluye: banda "Estable" pintada naranja-alerta S3; "v1" y porcentajes secos vs el lenguaje ya asentado en Inicio S2; cold-start mudo pese a que el API expone remainingDays S7; "Evolución" que entrega una fila plana S5) + 3 fortalezas a conservar (el contenido del detalle expandido es excelente material — el problema es la coreografía, no el contenido). Síntesis: "destapar y conectar antes que inventar". Pendiente: confirmación del CPSAO a la intención para avanzar a ARQ-019 con ella como §0.
- 2026-07-11 — Arquitectura: generado `docs/arquitectura/ARQ-0019-Experiencia-Salud.md` (FIN-019) con la **intención aprobada como §0** y la directriz del CPSAO incorporada (acción de mayor impacto + pregunta rectora + principio "que quiera mejorar porque entiende que puede"). **Hallazgo estructural del diseño: el ciclo es CERO backend y cero BD** — toda la materia prima ya está expuesta y auditada (pilares con pesos de FIN-004, coldStart.remainingDays, acciones/rangos de indicadores, y el motor de recomendaciones de FIN-007 cuyo priorityScore = impacto×urgencia×viabilidad es literalmente la definición de "mayor impacto" pedida). 7 piezas con alternativas comparadas: P1 pilares DENTRO de la tarjeta del Score con nombres llanos ("715 de 1.000", "Lo que más te frena: tu colchón"); P2 **"Tu jugada de mayor impacto"** desde la recomendación top del motor (con fallback local documentado); P3 interpretación siempre visible en lenguaje de Inicio + palanca solo donde duele + tap honesto "¿Cómo se calcula?"; P4 Score en verde Millo con banda como chip (el color-juicio sale del elemento dominante); P5 cold-start como estado de construcción con días restantes; P6 evolución con lectura narrativa; P7 cierre con puente al Copiloto. Composición integrada + respuesta explícita a la pregunta rectora (la decisión al salir = ejecutar/simular LA jugada). Criterios de aceptación §13 incluyen el test emocional de 5 segundos (¿calificado u orientado?) y captura de cold-start con usuario nuevo. FIN-019 pasa a *En auditoría — en espera de AUD-019*.
- 2026-07-11 — Auditor: generado `docs/auditoria/AUD-0019-Experiencia-Salud.md`. Verificó las 9 observaciones de `RECORRIDO-SALUD-001` contra las 3 capturas reales y línea por línea contra `HealthScreen.tsx` (todas confirmadas: naranja `colors.warning` para "Estable", "v1" visible, `pillars` expuesto en `types.ts` pero no renderizado, acciones/CTA solo tras `open` en `IndicatorCard`, evolución como lista plana, `coldStart.remainingDays` nunca usado, sin cierre tras el disclaimer). Confirmó que "cero backend" es cierto para 6/7 piezas y que el `priorityScore` de recomendaciones (P2) está genuinamente basado en `ΔScore` normalizado (`impactNorm = |scoreDelta|/IMPACT_SCORE_CAP`), no solo en urgencia/viabilidad. **Hallazgo elevado a crítico, acotado a P1**: el diseño recomendado colorea cada pilar con semáforo verde/amarillo/rojo, pero `ScorePillar` (types.ts) solo expone `value`+`status` (disponibilidad, no salud) — no existe ningún umbral de color por pilar en el backend ni se especifica uno en el ARQ; verificado en `score.util.ts` que `wealthPillar()` retorna un valor binario/plano (~70) para cualquier patrimonio positivo sin tendencia, la misma limitación que el propio Auditor señaló en `AUD-0004` y que `DEC-0004` aceptó como riesgo diferido *solo porque el pilar no se mostraba en ninguna pantalla* — mostrarlo ahora coloreado sin resolver esa limitación arriesga una calificación de color sin fundamento real, justo lo que la intención §0 de esta FIN busca evitar. P2-P7 sin hallazgos bloqueantes. Veredicto: **REQUIERE AJUSTES** (acotado a P1). FIN-019 pasa a *Auditado — REQUIERE AJUSTES acotado a P1; en espera de DEC-019*.
- 2026-07-11 — Arquitectura: **ARQ-0019 v1.1 — corrección de P1 entregada (ruta b de DEC-019)**. Pilares como barras de progreso 0–100 en color NEUTRO (mismo tratamiento los 4, relleno verde Millo sobre pista gris) — el semáforo queda reservado a los indicadores, que sí tienen niveles auditados desde FIN-004. Consecuencia derivada corregida por iniciativa propia: la línea "Lo que más te frena" tampoco puede salir de comparar pilares (heredaría el mismo problema — curvas de naturaleza distinta con wealthPillar cuasi-binario), así que se deriva del **peor indicador** (rojo→amarillo, niveles auditados) y SE OMITE si todo está en verde (patrón §29.1: nunca un juicio fabricado). Mejora futura registrada fuera de ciclo: refinar la curva de wealthPillar() (riesgo diferido de DEC-0004, ahora visible como magnitud neutra, no como juicio). Resto de P1 conservado tal como fue aprobado (nombres llanos, "715 de 1.000", retiro de "v1"). Criterio 1-bis añadido a §13 (cero semáforo por pilar verificable en captura). FIN-019 pasa a *Corregido — en confirmación puntual del CTO para habilitar IMP-0019*.
- 2026-07-11 — Desarrollo: **IMP-0019 emitido — FIN-019 entregada completa (7/7 piezas, cero backend)**. Referencia inmutable `e865640a2275945c5b2bb02cfc085bdaac3dd691` (un solo archivo de producto: HealthScreen.tsx). Verificado en vivo con datos reales: "715 de 1.000" + banda como chip + 4 pilares con barras neutras idénticas (62/90/63/70, ruta b — adaptación de presentación declarada: relleno blanco sobre pista translúcida por el fondo verde de P4); "Lo que más te frena: fondo de emergencia" derivado del peor INDICADOR; **"⭐ Tu jugada de mayor impacto" con la recomendación real del motor FIN-007** ("Aparta $1.066.500/mes… 3 meses de colchón en 4 meses") con CTA al simulador y fallback documentado; indicadores con "$N de cada $100" y palanca visible solo en amarillo/rojo con tap honesto "¿Cómo se calcula?"; **cold-start verificado con usuario recién registrado real** ("~60 días de historia" + 2 acciones disponibles hoy); evolución narrativa ("Tu primera medición: 715 — desde aquí construyes"); cierre con puente al Copiloto; disclaimer y gate Millo+ intactos. Juicio razonado contra la intención §0 en IMP §4 (con 3 reservas honestas: tap de profundización a confirmar en RC real; curva de wealthPillar diferida pero ahora visible como magnitud neutra; mapeo kind→escenario 3/4). Capturas de scroll completo en `capturas/fin-019/` (caso normal + cold-start). Suite 299/299. FIN-019 pasa a *Entregado — en VALIDACIÓN del Auditor y cierre del CTO*.
- 2026-07-12 — Arquitecto: **documento de comprensión del problema de FIN-020 entregado** (`docs/producto/COMPRENSION-FIN020-Presupuesto.md`), verificado contra el código real antes de responder. Hallazgo central verificable: el `available` de Presupuesto es ESTÁTICO (`budget.service.ts:106` — fijos − cuotas programadas, no resta el gasto variable del ciclo), lo que produce **dos "te queda" contradictorios en la app** (Inicio $6.092.801 vs Presupuesto $2.233.766 con la demo — misma promesa verbal, números distintos). Las 5 respuestas ancladas en datos que el producto ya conoce (fijos con día, cuotas con fecha, ciclo FIN-016, gasto diario por categoría, fondo de emergencia, gasto esencial, retos, recomendaciones): (1) el problema real es decidir un gasto con un número que miente — convertir "cuánto tengo" en "cuánto es mío de verdad, repartido en el tiempo que queda"; (2) experiencia propia por cadencia (diaria) y dirección temporal (prospectiva) — Inicio es foto, Salud es causa; además es la casa de los compromisos; (3) el usuario pasa de decidir mirando el pasado/saldo bruto a poder responder EN el momento de la compra; (4) evita gastarse la plata comprometida (el error que el producto ya observa con committedRatio e insights pero avisa tarde) y el error autoinfligido de los dos "te queda"; (5) sin ella Milla es espejo, no copiloto — se pierde la única vista prospectiva del dinero (respuesta preliminar al filtro §31). **Nota de alcance planteada sin decidir**: no existe modelo de metas explícitas — ¿bastan los objetivos implícitos (fondo 3–6m, deudas, reto) para la v1 o nace un concepto de meta (FIN mayor)? FIN-020 queda *Pendiente de evaluación del CTO/CPSAO para autorizar ARQ-020*.
- 2026-07-12 — Arquitectura: generado `docs/arquitectura/ARQ-0020-Experiencia-Presupuesto.md` (FIN-020) con las dos condiciones del CTO/CPSAO resueltas por diseño. **P1 — definición única de "Te queda" (§32)**: 3 alternativas comparadas; recomendada la conservadora "tuyo de verdad" (ingresos reales − gastos reales − compromisos PENDIENTES del resto del ciclo) porque nunca miente hacia arriba — el optimismo es el error #1 documentado; la proyección a fin de ciclo queda como concepto DISTINTO con nombre distinto si el DEC la quiere (jamás segundo "Te queda"). **P2 — fuente única (§32.3)**: `SpendableService` en el módulo budget inyectado por AMBOS endpoints (un solo cálculo con datos cargados una vez — la alternativa de util compartida se descarta porque la misma fórmula con datos cargados dos veces puede divergir por filtros: el bug renacería); efecto en Inicio declarado (el hero baja al volverse honesto — riesgo de percepción documentado con su mitigación). **P3** por-día (≈$X/día hasta el corte); **P4** protegido VISIBLE como línea de tiempo cronológica de fijos+cuotas pendientes (la resta deja de ser caja negra; "✓pagado" aproximado por fecha declarado); **P5** destino de lo libre vía recomendación top de FIN-007 (mismo patrón de la jugada de Salud, fallback igual) — cumple "decidir, no solo calcular"; **P6** el CRUD se conserva como casa de los compromisos, debajo de la zona de decisión. **Filtro §31 respondido en §5**: la capacidad irreemplazable es decidir el gasto de hoy sin sabotear el ciclo — ninguna otra experiencia mira hacia adelante dentro del ciclo, e Inicio no puede absorberla sin volver a la densidad que FIN-018 eliminó. Criterios §13 incluyen test de igualdad entre endpoints y grep anti-segunda-fórmula. FIN-020 pasa a *En auditoría — en espera de AUD-020*.
- 2026-07-12 — Arquitectura: **(1) P1 de ARQ-0020 corregida (v1.1, DEC-0020 §5)**: §4.1-bis política de fijos sin transacción vinculada — ADOPTADA (ii): todo fijo activo cuenta como comprometido hasta el CIERRE del ciclo, se pague o no (el sesgo posible apunta siempre hacia abajo — mismo criterio que eligió Alt A; la política (i) fecha-pasada⇒pagado se rechaza porque infla el disponible cuando el usuario no pagó); la fecha solo ordena la línea de tiempo, con etiqueta neutra "ya pasó su fecha" (corrige el "✓pagado" de v1.0); cuotas de deuda exentas (su pago SÍ es observable vía FIN-018); mejora futura registrada: `fixedItemId` en Transaction. §4.1-ter umbral de `interpretCashflow` — ruta (b): el 10% se rejustifica (heurística de holgura relativa independiente de la base; bajo Alt A avisa ANTES, dirección correcta para una definición conservadora; recalibrar sin datos reales sería inventar — compromiso en §13 de revisar tras la RC) con TEXTOS recalibrados (el rojo actual sería falso bajo Alt A: teQueda negativo puede ocurrir sin sobregasto — texto nuevo "Lo que viene comprometido supera lo que te queda", sin culpar). **(2) Volcado de conocimiento ejecutado (instrucción CPSAO)**: scripts de captura rescatados del scratchpad de sesión al repo (`frontend/scripts/captura/` con README de método, trampas y credenciales demo) + `docs/NOTAS-OPERATIVAS-ARQUITECTO.md` (oficio acumulado: dónde vive cada patrón, gap kind→escenario, navegación anidada, hilos abiertos con su registro) como Nivel 2 del arranque en frío del rol. FIN-020 queda *P1 corregida — en confirmación puntual del CTO para habilitar IMP-0020*.
